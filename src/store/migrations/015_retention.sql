-- 검수 2026-08-22 · B-08 / 제안#10 — **자라기만 하던 두 테이블**
--
-- `health_events` 와 `audit` 은 삽입만 있고 지우는 코드가 없었다. 세대는 이미 상한이
-- 있고(`dp/retention.ts`), 시크릿도 뒤늦게 얻었는데(`dp/secret-gc.ts`) DB 는 없었다.
--
-- ── 006 의 머리말을 정정한다
--
-- 006 은 *"이벤트 로그가 단일 정본이다"* 로 시작한다. **지금은 사실이 아니다.** replay 는
-- `projectHealth()` 로 대체됐고 정본은 `backend_health` 다. `health_events` 를 읽는
-- 프로덕션 코드는 하나도 없다 — 남은 것은 테스트뿐이다.
--
-- 006 파일 자체는 안 고친다. 러너는 이름으로만 적용 여부를 보므로(체크섬 없음) 이미
-- 적용된 배포에서는 고친 주석이 반영되지 않는다. **적용된 것과 적힌 것이 다른 상태**가
-- 주석 하나를 고쳐 얻는 것보다 나쁘다. 정정은 여기 남긴다.
--
-- 커서(`health_cursor`)는 그대로다. 남은 최대 seq 로 되감으면 번호가 재사용되고
-- §6.6 의 "번호 순서 = 커밋 순서" 가 깨진다 — 행을 지우는 것과 번호를 되돌리는 것은
-- 다른 일이다.

-- 나이로 지우는데 나이 인덱스가 없었다. 006 은 `(backend_key, seq DESC)` 만 만들었다 —
-- 읽는 축과 지우는 축이 다르다.
CREATE INDEX health_events_at_idx ON health_events(at);

-- `audit` 은 `audit_at_idx ON audit(at DESC)` 를 001 에서 이미 가졌다. 새로 만들지 않는다.
