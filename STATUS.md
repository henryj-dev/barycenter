# 현재 상태 정리 (2026-08-16, 49차 검수 뒤 — **연속 카운트 1**)

열두 번의 외부 검수를 지나온 시점의 정리다 (13차 진행 중). **무엇이 있고, 무엇이 증명됐고, 무엇이
열려 있는지**를 한자리에 적는다. 설계는 `DESIGN.md`, 테스트 목록은 `TESTS.md`,
실행은 `scripts/verify.sh` 다.

---

## 1. 지금 있는 것

```
src/         5,537 줄   20 파일
tests/       8,004 줄   conformance 17 파일
DESIGN.md    2,400+ 줄
커밋            52
```

### 실행되는 스위트

| 스위트 | 명령 | 통과 | 무엇을 지키는가 |
|---|---|---|---|
| typecheck | `npm run typecheck` | — | strict + `exactOptionalPropertyTypes` |
| 표면 | `node scripts/surface.mjs --check` | — | **계약이 움직였나** — 동결 카운터 |
| 모델 | `npm run test:model` | **13** | **교차를 생성해서** 속성 P0~P10 을 때린다 |
| 단위 | `npm test` | **366** | 렌더러·검증기·라우트·DP · 드라이버 · 기동 · export/import · CLI · SSE · GUI · **Certificates** |
| conformance | `npm run test:conformance` | **392** | **검수가 재현한 반례** (5~14차) · S14 선택형 거절 |
| 골든 | `npm run test:golden` | **44** | `nginx -t` + 런타임 프로브 (TLS 4건 · ACME 챌린지 7건 포함) |
| 엔진 사실 | `npm run test:engine` | **73** | 설계가 전제한 nginx 동작 (SKIP 2) |
| e2e | `npm run test:e2e` | **57** | 실물 nginx — http·tcp·udp·SNI 패스스루·**TLS 종단**. 일부는 컨테이너 안에서 에이전트를 돌린다 |
| 스파이크 | `spike/*/run.sh` | **91** | S1·S5·S7·S8·S11·S12·**S13**·S16·S17·S18·S19 |
| 저장소 | `npm run test:store` | **107** | **실물 PG** — changeset·plan·commit·제약 |

(수치는 2026-08-17 `./scripts/verify.sh` 실측이다. e2e 행이 둘로 갈려 하나는 14, 하나는
35 로 오래 어긋나 있었다 — 합쳐 실측값으로 고쳤다.)

단위와 conformance 의 대부분(DP 경로)은 **불변식 테스트이기도 하다** — `assertInvariants` 가
`DpAgent.serial()` 에 걸려 있어, 상태가 store 로 내려갈 때마다 I1·I3·I5 를 본다.

`./scripts/verify.sh` 가 전부 돌린다. **스위트 통과와 동결 가능은 다르다** —
게이트는 별도로 출력되고 `--freeze-gate` 는 미해소 항목이 있으면 non-zero 다.

### 모듈

| | 무엇 |
|---|---|
| `model/decode.ts` | **경계 해독기.** `unknown` → `Model`. 모르는 enum·키·강제변환 거부 |
| `model/provisional.ts` | `RawModel`(입력) / `Model`(프로토콜 판별 유니온) 두 층 |
| `validate/` | 의미 검증 — 참조 무결성·소켓 충돌·프로토콜 정합·엔진 제약 |
| `conf/` | AST → `nginx.conf`. fail closed |
| `route/compile.ts` | 라우트 우선순위 컴파일 (축소 계약) |
| `dp/operation.ts` | `ApplyOperation` · `ActivationEvidence` · `ApplyLease` |
| `dp/agent.ts` | 직렬 durable 상태기계. 좌표 CAS·예약·펜싱·승계 |
| `dp/apply.ts` | 크래시 저널 상태기계 |
| `dp/materialize.ts` | 세대 원자 게시 + manifest |
| `dp/store-fs.ts` | 파일 저장소 — 원자 교체·손상 탐지·버전 CAS·프로세스 락 |
| `dp/driver.ts` | `DataplaneDriver` 계약 + 참조 구현 |
| `index.ts` | **동결 대상 표면** |

---

### 오래 돌려 봤다 — 부채 셋이 실제로는 유계다 (2026-08-17)

이 제품이 10 초보다 오래 돌아 본 적이 없었다. e2e 는 전부 수십 초짜리이고 그 사이에는
아무것도 안 샌다. **15 분 · 설정 변경 147 회 · 백엔드를 죽였다 살리며** 돌렸다
(`npm run soak`).

```
                시작 →   끝    판정
generations       2 →   13     평탄 — 상한이 돈다
generationKb      9 →   63     평탄
agentStateKb      6 →  270     평탄 — 전환 64 개 상한이 실제로 잡는다
workers           2 →  3~6     진동. **누적이 아니다**
rssMb            78 →  ~188    증가 후 안정
트래픽 15,461 건 · 실패 2.6%
```

**아무것도 무한히 자라지 않는다.** 오래 달고 있던 성장 부채 셋(`completed` 원장 · 세대
디렉토리 · 워커 누적)이 **처음으로 숫자로** 유계임이 확인됐다.

#### 내가 두 번 틀렸고 두 번 다 무대 탓이었다

**① "무한히 자란다" 를 상한 아래 데이터로 결론냈다.** 5 분·45 회 실행에서
`agentStateKb` 가 6→182 로 선형이길래 무계라고 적었는데, 상한이 **전환 64 개**라
45 회는 애초에 상한에 닿은 적이 없었다. 147 회로 늘리니 270KB 에서 평탄해진다.
*측정 구간이 임계값을 안 지나면 그 측정은 임계값에 대해 아무 말도 못 한다.*

**② 트래픽 실패율 51% 도 계측기가 만든 것이었다.** 설정 변경으로 :999 의
`acceptProxyProtocol` 을 격번으로 켰는데 하네스는 평문을 던지고 있었다 — 절반이 실패하는
게 당연하다. 다른 포트 리스너를 켜고 끄는 것으로 바꾸니 2.6% 다.

**③ 워커 누적도 처음엔 무대가 답을 정하고 있었다.** 유휴 keepalive 연결 여덟 개를 쥐고
"안 자란다" 고 볼 뻔했는데, S11 의 P8 이 이미 실측해 뒀다 — **HUP 은 옛 워커의 유휴
keepalive 를 닫는다.** 붙들려면 *진행 중인* 요청이어야 한다. 느린 백엔드로 바꾸니 워커가
3~6 사이에서 진동하고, **그래도 누적은 없다.**

#### 그래서 §6.4 는 어떻게 되나

§6.4 는 *"디바운스는 옵션이 아니다"*, *"reload admission control"*, *"serving_generations
상한"* 을 못 박았다. 이 부하(5 초마다 설정 변경)에서 **셋 다 없어도 유계다.**
그러니 지금 숫자를 정해 넣지 않는다 — 근거 없는 값을 계약처럼 굳히는 것이 §11.4 가
자기 표에 대해 경고한 그것이다. **더 센 부하에서 다시 재고 그때 정한다.**

#### 남는 비용 둘 (수치가 생겼다)

- **쓰기마다 270KB 를 다시 쓴다.** durable 상태는 유계지만 `serial()` 이 매번 통째로
  쓰므로 apply 비용이 그 크기에 비례한다. 유계인 것과 싼 것은 다르다.
- **세대 전환이 2.6% 를 떨군다.** 15,461 건 중 408 건. 무중단이 아니다 — 멤버십 평면이
  있는 이유가 이것이고, 설정 변경은 여전히 reload 다.

---

### 관측을 짓는다 — 오래 돌려 보기 전에 (2026-08-17)

§6.4 가 *"리소스 알람: `serving_generations` 수, FD, 메모리, UDP 세션, conntrack"* 을
요구하는데 **하나도 없었다.** 그리고 이 저장소가 오래 달고 있는 부채가 전부 **"오래 돌면
자란다"** 부류다 — `terminal`·`completed` 원장, 세대 디렉토리, 워커 누적.

**자라는지 보려면 재는 자리가 있어야 한다.** 관측 없이 soak 를 돌리면 "안 죽었다" 까지만
알 수 있고 그건 측정이 아니다. 그래서 이걸 먼저 짓는다.

| | |
|---|---|
| `GET /metrics` | Prometheus 텍스트. **인증을 요구한다** — 풀 이름과 리비전이 나가는 자리라 `/healthz` 와 다르다 |
| 게이지 | 세대 수·바이트 · **durable 상태 파일 바이트**(원장 성장) · head · 리더 · 평면별 epoch · 미완 전환 · 헬스 3분류 · 미적용 plan · RSS |
| 카운터 | apply(phase 별) · 멤버십 적용/투영(평면별) · 헬스 전이 |
| 구조화 로그 | 한 줄에 JSON 하나. `console.*` 26 곳을 전부 걷어냈다 |

**원장 내부를 안 들여다본다.** 저장소 payload 는 불투명하다는 계약이고(9차 검수), 파일
크기가 그 계약을 안 깨면서 "자라는가" 에 답한다.

⚠️ **§6.4 목록의 절반이다.** FD·메모리는 프로세스 것만 알 수 있고(엔진 것이 필요하다),
UDP 세션·conntrack 은 커널을 읽어야 한다. 있는 척하지 않는다.

그리고 저장소 스위트를 순차 실행으로 바꿨다 — 둘이 각자 PG 를 세우다 부하로 흔들렸다.
**단독으로는 통과하고 전체에서만 깨지는 것**이라, 안 고치면 "가끔 깨진다" 로 넘어간다.

---

### 멤버십 평면이 알고리즘을 무시하고 있었다 (2026-08-17)

**1단계에서 내가 낸 구멍이다.** 멤버십 평면이 켜지면 upstream 이 `balancer_by_lua_block`
이 되면서 `ip_hash`/`hash` 디렉티브가 산출물에서 통째로 사라지는데, 밸런서 본문은
`math.random` 하나였다. 모델은 여전히 `source_ip_hash` 를 표현할 수 있었으므로
**필드는 있는데 아무도 안 지키는** 상태였다 — 이 저장소가 반복해서 잡아 온 그 부류다.

cut·replay 테스트를 쓰다가 잡혔다. 설정을 바꾸려고 알고리즘을 `source_ip_hash` 로 돌렸는데
`membershipOnly: true` 가 나왔다 — **산출물이 안 바뀌었다는 뜻이고**, 그건 알고리즘이
렌더에 아무 영향도 없다는 뜻이었다.

| 알고리즘 | 정적 `server` 줄 | 멤버십 평면 |
|---|---|---|
| `round_robin` | 엔진 기본 | dict 카운터(`incr`) — **워커 간 공유**. 워커 로컬로 두면 워커 수만큼 편향된다 |
| `source_ip_hash` | `ip_hash` / `hash $remote_addr` | `crc32($remote_addr) % n` |
| `hash <key>` | `hash $<var> consistent` | `crc32($<var>) % n` |

**consistent hashing 은 아니다.** 정적 경로의 `consistent` 는 peer 가 바뀔 때 재매핑을
최소화하는데 `% n` 은 목록이 바뀌면 거의 전부 재매핑된다. 멤버십이 자주 바뀌는 것이 이
평면의 이유이므로 **실제로 다른 계약**이고, S15(밸런서 품질)가 잴 축이다. 적어 뒀다.

#### cut·replay — 창을 논증으로 닫았다 (▲)

§6.5-2·4 는 스냅샷 시점의 HWM 을 기록하고 활성화 뒤 그 이후 이벤트를 재생하라고 한다.
**이벤트를 재생하는 대신 다시 유도한다** — 이 저장소의 리듀서는 델타가 아니라 *상태*에서
계산하므로(커밋된 모델 ∩ 현재 헬스) 활성화 직후 한 번 더 계산하면 결과가 같다. 순서를
지킬 것이 없으면 커서도 필요 없고, 이벤트 로그는 감사 기록으로 남는다.

**▲ 그 창이 닫혔다는 것은 논증이지 측정이 아니다.** 재투영을 통째로 빼는 변이를 넣어도
**아무 테스트가 안 깨진다** — staging 이 이미 옳으므로 차이가 나는 것은 staging 과 활성화
사이에 헬스가 바뀌는 경우뿐이고, 그걸 밖에서 결정적으로 만들 방법이 없다. 재는 것은 그
대신 유지되는 불변식이다: *설정 전환이 끝나면 슬롯이 지금 헬스와 일치한다.*

---

### v0.3 3단계 — 헬스 프로버 (2026-08-17)

> **커밋 기록 정정.** 이 작업은 `96b71e0` 로 올라갔는데 그 커밋 메시지에 `Pinned-by:` 가
> 빠져 있어 재현물 게이트가 막는다. **재현물은 `tests/store/health.test.ts` 다** — 아래에
> 적은 결함 둘(결과 연속 오산, 메모리 순번)을 그 파일이 결정적으로 잡는다. 이미 푸시된
> 이력이라 되쓰지 않고 여기 적어 둔다.

죽은 백엔드가 **reload 없이** 슬롯에서 빠지고 살아나면 돌아온다. `GET /health/backends` 가
판정을 드러내고 **`unknown` 을 숨기지 않는다** — 아직 못 잰 것과 산 것은 다르고, 그 구분이
없으면 프로버가 죽었는지 백엔드가 다 산 것인지 알 수 없다(§6.7).

**범위를 먼저 적는다**: TCP 연결 검사만이다. HTTP 상태 코드도, 프로토콜별 헬스체크도,
드레인 관측(S2)도 없다. 흉내내면 *"헬스체크가 있다"* 가 절반만 참이 된다.

§6.6 이 요구한 셋: **단일 리듀서**(소유가 다른 것과 경합이 없는 것은 다르다) ·
**커서는 `nextval` 이 아니다**(잠금 행 — `nextval` 은 커밋 순서와 달라 cut 이후 커밋된
낮은 번호가 영구 누락된다) · **판정 변경과 이벤트를 같은 트랜잭션에서**.

#### 실물이 낸 것 둘 — 둘 다 내가 잘못 센 것이다

**① 연속 횟수를 *상태의 연속*으로 셌다.** `healthy` 인 동안 실패가 몇 번 이어져도 매번
1 로 리셋돼 임계값에 **영원히 도달하지 못했다** — 프로버가 `ECONNREFUSED` 를 보고 있는데
판정은 `healthy` 로 굳었다. 세는 것은 **결과의 연속**이다.

**② 관측 순번을 프로세스 메모리에서 1 부터 셌다.** §6.6 의 ABA 방어(낡은 관측 버리기)가
재기동한 프로세스의 1 번을 저장된 50 번과 비교해 **자기 관측을 전부 버린다.** 메모리
카운터와 durable 판정을 같은 축으로 비교하면 반드시 이렇게 된다.

#### 변이가 세 번 무력했고, 세 번 다 무대를 고쳐야 했다

- ABA 방어 변이가 e2e 에서 안 잡혔다 — 카운터가 결국 따라잡아 **증상이 일시적**이라
  60 초를 기다리면 통과한다. 저장소 테스트에서 순번을 미리 심어 결정적으로 만들었다.
- `FOR UPDATE` 제거가 `Promise.all` 로는 안 잡혔다 — 트랜잭션이 겹칠지가 **운**이다.
  두 커넥션의 인터리빙을 직접 만들어 결정적으로 만들었다.
- 그리고 `if (false)` 변이 넷은 **컴파일이 안 됐다.** 안 도는 변이는 판별력의 증거가 아니다.

#### 그리고 `git checkout` 에 두 번째로 당했다

이번엔 **추적되지 않는 테스트 파일**이었다. `git checkout` 이 아무 일도 안 했는데
`|| true` 가 그걸 삼켰고, 내 확인은 주석에 있는 `FOR UPDATE` 문자열을 보고 "복구됐다" 고
답했다. **확인이 엉뚱한 것을 봤다.** 쿼리를 직접 세어 되돌렸다.

**아직 없는 것**: cut·replay(§6.5-2·4). 리듀서가 서빙 중인 **모든 epoch 에 매번 다시
투영**하므로(§6.5-5) 이벤트 로그를 재생하지 않는다 — 로그는 쌓지만 소비자가 없다.
세대 전환과 헬스 변화가 겹치는 좁은 창은 아직 안 닫혔다.

---

### v0.3 2단계 — **reload 없이 백엔드가 바뀐다** (2026-08-17)

이 제품의 이유다. S1 이 세 서브시스템 전부에서 실증한 경로를 REST 로 끝까지 몰았다.

```
백엔드 :12 → :11 로 교체
  트래픽      B12 → B11        ✔ 옮겨갔다
  master pid  19 → 19          ✔ 불변
  워커 기동   0 → 0            ✔ reload 없음
  활성 세대   r2-e1 → r2-e1    ✔ 전환 없음
  세대 수     2 → 2            ✔ 아티팩트 안 만듦
```

**판정은 "트래픽이 옮겨갔다" 가 아니다** — 세대를 만들고 HUP 을 보내도 그렇게 된다.
**안 일어난 일**을 재야 한다. 그리고 멤버십 전용인지의 판정 기준은 **렌더 산출물**이다:
백엔드가 conf 에 없으므로 백엔드만 바뀐 변경은 산출물이 바이트 단위로 같다.

#### 실물이 낸 것 셋

| # | 무엇 |
|---|---|
| ① | **부트스트랩에 dict 가 있어야 한다.** §6.5-1 은 HUP *전에* 적재하라는데 그 시점에 도는 것은 **옛 세대**다. 첫 apply 에서 그게 부트스트랩이므로 dict 와 admin 이 거기 이미 있어야 한다 — 모양이 capability 에 따라 달라져 셸이 못 쓴다. **데몬이 만든다**(`--write-bootstrap`) |
| ② | **재시작이 멤버십을 되돌렸다.** 세대 아티팩트로 복원했더니 그 뒤의 멤버십 전용 변경이 통째로 사라졌다 — :12 로 옮긴 백엔드가 :11 로 되살아났다. §6.4 가 경고한 그 모양이고, 정본은 **head 리비전**이다 |
| ③ | **멤버십은 lease 를 안 받는다.** lease 는 apply 실행권에 매달려 있는데, §6.5-6 은 *"prepare 동안에도 활성 epoch 는 헬스 갱신을 계속 받는다"* 고 한다 |

#### 그리고 내가 사고를 냈다

변이 검사를 되돌리면서 `git checkout src/bin/barycenterd.ts` 를 썼고, **그 파일의 미커밋
배선을 통째로 날렸다.** 49차에 배운 것이 *"도구가 본체를 건드리는 한 사고는 반드시 난다"*
였는데 이번엔 도구가 아니라 **내 손**이었다. 재구성해서 복구했고, 그 뒤로는 백업–복원
쌍으로만 변이시켰다.

변이 셋(멤버십 전용 판정 무력화 · HUP 전 staging 생략 · 기동 시 재적재 생략)이 각자의
테스트에 잡힌다. 처음 쓴 변이 넷은 **컴파일이 안 돼서** 아무 신호도 못 냈다 —
`if (false)` 가 타입 좁힘을 일으켰다. 안 도는 변이는 판별력의 증거가 아니다.

**남은 것 (3단계)**: 헬스 프로버 없음. §6.4 의 스냅샷도 절반(desired 만)이라 프로버가
죽었다고 본 백엔드는 재시작에 되살아난다. cut·replay 도 없다 — 헬스 이벤트 자체가 없어
재생할 것이 없다.

---

### v0.3 멤버십 평면 — 1단계: 렌더 (2026-08-17)

**v0.3 은 한 번에 안 끝난다.** 세 단계로 나눈다.

```
1단계 (여기)  멤버십 평면을 **렌더**한다 — 백엔드가 conf 가 아니라 shared dict 에 산다
2단계        staging·cut·replay 를 apply 에 붙인다 → **reload 없는 백엔드 교체**
3단계        헬스 프로버
```

**capability 로 켜진다.** `httpLua`/`streamLua` 가 없으면 지금까지의 정적 `server` 줄
그대로다 — 열등한 것이 아니라 **다른 계약**이고(백엔드가 바뀔 때마다 세대 전환 + reload),
기본이 꺼짐이라 기존 산출물은 한 바이트도 안 바뀐다. 방금 지은 capability 조회가 이걸
켠다.

세 가지를 지켰다: **자기 epoch 의 슬롯만 본다**(§6.5-1 — 그래야 HUP 뒤에도 옛 워커가
E-old 를 쓴다) · **슬롯이 없으면 끊는다**(§6.5-3 — 조용히 옛 peer 로 안 흐른다) ·
**epoch 리터럴을 렌더러가 굽지 않는다**(리터럴은 세대의 admin 조각에 산다 — `render_digest`
가 모델만의 함수여야 plan 이 렌더러 드리프트를 잡는다).

AST 에 `lua` 노드를 더했다. `directive` 로는 못 낸다(세미콜론이 붙는다), `lit` 으로도 못
낸다(개행이 제어 문자로 거부되고 **그 거부는 옳다**). 본문은 렌더러가 쓴 상수만 받고,
중괄호 균형을 검사한다 — 값이 아니라 **코드**라 인용으로 무해화할 방법이 없다.

#### E64 — `nginx -t` 는 Lua 를 **하나도** 검증하지 않는다

멤버십 평면은 `balancer_by_lua_block` 위에 서는데, §6.2 의 게시 전 검사는 `nginx -t` 다.
그 둘이 만나는 지점을 쟀더니 **하나도 안 잡는다.**

그러면 **밸런서가 깨진 세대도 "활성화됐다" 로 판정된다** — 세대 마커는 `return 200` 이라
Lua 와 무관하게 답하기 때문이다. 2단계에서 활성화 증거를 넓혀야 한다는 뜻이고, 그 사실을
지금 적어 둔다.

**처음 쟀을 때는 "content_by_lua 는 잡는다" 로 나왔고 거짓이었다.** 그 케이스만
`location` 밖에 블록을 둬서 거부 사유가 Lua 문법이 아니라 *"directive is not allowed
here"* 였다 — **거부를 보고 이유를 안 읽었다.** 컨텍스트를 맞춰 다시 재니 넷 다 통과다.

표면이 또 움직였다(`MEMBERSHIP_DICT`, 80 심볼). 동결 카운터 0.

---

### 엔진 capability 를 실제로 물어본다 — 그리고 실물이 결함 셋을 더 냈다 (2026-08-17)

`capabilities.ts` 는 `nginx -V` 를 해석하는 순수 함수로 오래 있었는데 **아무도 부르지
않았다.** 컨트롤 플레인이 `streamRealip: false` 를 상수로 가정했고, PROXY 신뢰 경계를 넣은
뒤로 **stream PROXY 수신이 엔진과 무관하게 항상 막혔다** — capability 로 좁힌다면서
capability 를 안 물어본 셈이다. 기동 때 조회해 `ConfigStore`·`ControlPlane` 에 넘기고
`/status` 로 드러낸다.

#### 배선이 관측되지 않아 테스트를 하나 더 지었다

변이(데몬이 조회 결과를 안 넘김)를 넣었는데 **아무것도 안 깨졌다.** 기본 이미지
(OpenResty)에는 `stream_realip` 이 없어 조회 결과가 보수적 기본값과 같기 때문이다 —
**그 엔진으로는 배선의 유무를 구분할 수 없다.**

그래서 **공식 nginx** 로 도는 e2e 를 따로 지었다(`v02-capability`). E0 이 말한 모듈 분리가
그대로다 — 공식 nginx 는 `stream_realip` 있고 lua 없음, OpenResty 는 반대. 이제 변이가
잡힌다.

#### 실물이 낸 결함 셋

| # | 무엇 | 어떻게 드러났나 |
|---|---|---|
| ① | `probeEngine` 이 **stdout 만 읽었다** | `nginx -V` 는 **stderr** 로 나온다. 함정을 주석에 적어 놓고 성공 경로에서 그대로 빠졌다 — 종료 코드가 0 이면 언제나 빈 문자열을 파싱했고 "모든 모듈이 없다" 는 **그럴듯한 거짓말**이 나왔다. 단위 테스트가 잡았다 |
| ② | **`real_ip_header` 를 stream 에도 냈다** | http 전용 디렉티브다. `"real_ip_header" directive is not allowed here` 로 **기동이 깨진다.** 기본 이미지로는 그 조합이 검증기에 막혀 **영원히 안 드러나는 자리**였고, 공식 nginx e2e 가 잡았다. stream 은 `set_real_ip_from` 만으로 게이트가 선다(E63.5·E63.6) |
| ③ | `/config/rendered` 가 **caps 없이 렌더**했다 | 저장은 됐는데 읽을 수가 없었다. API 가 `render()` 를 직접 부르지 않게 `store.renderAt()` 으로 옮겼다 — **렌더는 capability 를 아는 쪽에서만 한다** |

#### 그리고 조용히 실패하던 자리 하나를 더 닫았다

reload 재전송 상한을 넘겨 닫는 경로가 **사유 없이** 종단했다. `{"phase":"failed"}` 만
돌아와서 실제로 이번에 두 번 진단이 막혔다. 관측한 증거와 함께 사유를 남긴다.

`POST /operations/{id}/cancel` 도 냈다. **봉투를 보관한다**(005) — 재구성하면 정본 튜플이
달라져 abort 가 아무것도 못 지운다. 이미 활성화된 것은 취소가 아니라 **롤백**이다(§3.3).

---

### PROXY 신뢰 경계 — 설계가 "필수" 라고 적은 것이 없었다 (2026-08-17)

§4.7 은 `trusted_proxy_cidrs` 를 *"필수, 비어 있을 수 없다. 없으면 source IP 스푸핑"* 이라고
못 박아 뒀다. 그런데 모델에는 **불리언 하나**뿐이었다. 신뢰 경계 없이 PROXY 수신을 켤 수
있었고, **문서만 읽으면 있다고 믿게 되는 상태**였다 — §4.7 자체가 거짓이었다.

#### 재고 나서야 얼마나 나쁜지 알았다 (E63)

| 설정 | `$remote_addr` | `$proxy_protocol_addr` |
|---|---|---|
| realip 없음 | 실제 peer | **헤더가 말하는 값** |
| peer 를 신뢰 | 헤더 값 | 헤더 값 |
| peer 를 **불신** | **실제 peer** | 헤더 값 |

**`$proxy_protocol_addr` 는 어떤 경우에도 게이팅되지 않는다.** 신뢰 경계는 오직 realip 을
거친 `$remote_addr` 에만 걸린다. 그런데 렌더러는 `stream_realip` 이 없을 때 소스IP 해시를
**바로 그 변수로** 계산했다.

그게 옛 R18 이고, 근거는 *"모듈 없이도 실 클라이언트 IP 를 준다"* 였다. **그 문장은
참이다.** 빠진 절반이 *"그리고 그 값을 클라이언트가 정한다"* 였을 뿐이다. 열등한
대체물이 아니라 **틀린 대체물**이었고, 공격자가 자기를 원하는 백엔드로 몰 수 있었다.

#### 검수 회차의 답 둘을 뒤집었다

R18(`$proxy_protocol_addr` 로 해시)과 R22(혼합 공유 풀 거부) 를 다시 썼다. R22 가 막던
실패 모드(일반 리스너에서 변수가 빔)는 **사라졌다** — 해시가 언제나 `$remote_addr` 이기
때문이다. 대신 더 앞의 질문이 규칙이 됐다: **신뢰 경계를 걸 수 있는 엔진인가.**

지운 게 아니라 **왜 뒤집혔는지를 테스트 안에 적었다.** 앞선 답이 틀렸다는 것은 측정이
말한 것이지 내 취향이 아니다.

#### 바꾼 것

| | 지금 |
|---|---|
| 모델 | `acceptProxyProtocol?: { trustedCidrs: string[] }` — **불리언을 안 받는다**. 빈 목록도 거부 |
| 렌더 | `proxy_protocol` 과 `set_real_ip_from`/`real_ip_header` 가 **함께** 나간다 |
| 해시 | **언제나 `$remote_addr`** — capability 로 분기하지 않는다 |
| stream + 모듈 없음 | **검증기가 막는다.** 게시 전에 죽는 것보다 저장에서 막히는 편이 낫다 |
| DB | `accept_proxy_cidrs text[]`. **옛 `true` 는 안 옮기고 끈다** — 신뢰 경계를 지어낼 수 없다 |

변이 넷 전부 자기 테스트가 잡는다: 잠금 미출력 · 옛 불리언 관대 수용 · 빈 목록 통과 ·
stream 게이트 제거.

**표면이 움직였다** — `InboundProxyProtocol` 이 추가돼 79 심볼, 동결 카운터 0 으로 리셋.

**남은 것**: `real_ip_from_header` · `forwarded_header_policy` 는 아직 없다(v0.6).
그리고 **컨트롤 플레인이 엔진 capability 를 조회하지 않는다** — 보수적으로 `streamRealip:
false` 를 쓰므로 stream PROXY 수신은 지금 **항상** 막힌다. capability 프로브가 별도 과제다.

---

### v0.2 L4 — "지원한다" 와 "돈다" 사이를 건넜다 (2026-08-17)

모델도 검증기도 렌더러도 tcp·udp·SNI 패스스루를 **전부터 지원했다.** S1 이 세 서브시스템
전부에서 성립을 증명했고 골든이 `nginx -t` 를 통과시켰다. 그런데 **REST 로 넣어 실제
트래픽까지 가 본 적이 없었다** — v0.1 e2e 는 http 하나뿐이었고, 그 사이가 v0.1 배선에서
결함 넷을 낸 자리다.

```
tcp          :998 → 백엔드 :12
udp          :997 → 백엔드 :13   (dns 프리셋 — proxy_responses 1)
passthrough  :996 → SNI 로 갈린다 · TLS 를 종단하지 않는다
```

**패스스루는 인증서로 판정한다.** 본문만 보면 종단해서 프록시해도 같은 답이 나온다.
`a.test` 로 붙으면 백엔드 a 의 인증서가, `b.test` 면 b 의 것이 그대로 온다 — 그게
"종단하지 않는다" 의 **유일한** 증거다. 매칭 없는 SNI 도 SNI 부재도 거부된다(§4.1).

변이 넷으로 판별력을 확인했다 — `listen` 에서 `udp` 제거 · SNI 매칭 무시 · 매칭 없는
SNI 를 첫 풀로 · 소켓 겹침 탐지 무력화. **각각 자기 테스트가 잡는다.**

#### 이번 실패는 우리 것이 아니라 무대의 것이었다

UDP 백엔드를 `content_by_lua_block { ngx.say(...) }` 로 짰는데 죽었다:

```
[error] lua entry thread aborted: API disabled in the current context
        [C]: in function 'say' ... udp client: 127.0.0.1, server: 0.0.0.0:13
```

**TCP 에서는 멀쩡하고 UDP 에서만 막힌다.** `return` 디렉티브로 바꿨다. UDP 를 실제로 쏴
보기 전에는 안 드러나는 종류이고, 프로브가 처음 빨간 것은 좋은 신호였다.

#### 예제를 실제로 돌아가게 만들었다

`examples/l4.json` 을 두면서 데모 백엔드에 tcp :12 · udp :13 을 붙이고 compose 에 포트를
열었다. **문서에만 있는 예제는 예제가 아니다** — 배포에 붙여 셋 다 답하는 것을 확인했다.

**남은 것**: 포트 노출은 여전히 손이다(§11.3). 리스너를 만들 때마다 배포 설정에 한 줄을
더해야 밖에서 닿는다.

---

### CI — 게이트를 사람 손에서 뗐다 (2026-08-17)

`verify.sh` 는 지금까지 **내가 기억해서** 돌렸다. 게이트가 사람 손에 달려 있으면 언젠가
안 돈다 — 이 저장소는 규칙이 산문이라 안 지켜지는 것을 이미 겪었고(46차), 그때 답이
"게이트로 옮긴다" 였다. 같은 답을 한 층 위에 적용한다.

**CI 는 `./scripts/verify.sh` 를 그대로 부른다.** 자기만의 단계 목록을 들면 로컬과
갈라지고, 갈라지면 어느 쪽이 계약인지 아무도 모른다. **"전부" 의 정의는 한 군데에만
있어야 한다.** 잡(job)은 넷이다 — 빠른 신호(`--quick`) · 전체 · 재현물 핀(범위) ·
Quickstart.

#### 재현물 게이트가 커밋 하나만 보고 있었다

CI 에 걸면서 드러났다. `base..head` 를 통째로 diff 하고 **HEAD 의 메시지만** 읽었다.
로컬에서 한 커밋씩 검사할 때는 맞았지만 CI 가 푸시·PR 을 볼 때는 거짓이다.

실제로 재봤다 — 커밋 셋을 밀되 **가운데만 `src/` 를 재현물 없이 고치고 마지막에 핀이
있는 커밋**을 두면:

```
옛 게이트   종료코드 0   "건너뜀 — none — 주석만 고쳤다."     ← 놓친다
새 게이트   종료코드 1   "FAIL 529adcca — ... (가운데 커밋)"   ← 그 커밋을 짚는다
```

이제 `rev-list --no-merges` 로 **커밋을 하나씩** 각자의 부모에 대고 검사한다.

#### Linux 에서 실제로 돌려 봤다

CI 를 쓰고 "돌겠지" 하고 두면 첫 실행에서 빨개지고, 그러면 CI 를 못 믿게 된다.
docker-in-docker 로 리눅스 환경을 세워 **전체 스위트를 통째로 돌렸다** —
231 · 381 · 13 · 32 · 10 · 28 · 65 · 스파이크 다섯, **종료코드 0.** Quickstart 도 같은
환경에서 확인했다.

*(남는 위험: 그 검증은 arm64 였고 GitHub 러너는 amd64 다.)*

그 과정에서 `quickstart.sh` 가 **`curl` 이 없을 때 "데몬이 안 떴다" 고 거짓 진단**하는
것을 발견했다. 데몬은 멀쩡히 떠 있었고 못 물어본 것뿐이었다. 필요한 도구를 먼저 확인하고
없는 것을 이름으로 말하게 고쳤다 — **없는 것을 없다고 말하지 못하는 진단은 진단이 아니다.**

---

### 거짓 셋을 닫았다 — 리더 선출 · 롤백 경로 · 세대 보존 (2026-08-17)

제품화 초안을 낸 뒤 **문서에 적힌 것이 참이 아닌 자리**를 셋 찾아 닫았다.

**① 리더 선출 (§3.5).** `leader_token` 이 환경변수였다 — DP Agent 가 50 회차 동안 그
토큰으로 펜싱·승계·`superseded` 를 지었는데 **아무도 발급하지 않았다.** PG advisory lock +
전용 시퀀스로 발급한다. 못 잡은 인스턴스는 **죽지 않고 읽기로 서비스하며 대기**하고,
쓰기에는 503 `not_leader` 를 답한다(403 이 아니다 — 권한이 아니라 *여기서는* 못 하는 것).

락을 잡은 **뒤에** 토큰을 뽑는다. 반대로 하면 락을 못 잡은 인스턴스도 시퀀스를 태워
"토큰 = 리더가 몇 번 바뀌었나" 가 깨진다. 세션이 끊기면 즉시 리더가 아니다 —
`tryAcquire()` 가 참이었다는 *기억*이 아니라 지금 상태를 답한다.

**② 롤백 경로 (§5.3).** S8 이 인증서 세대 결박을, S19 가 clone+새 epoch 를 증명하고 DB 에
`rollback_of` 컬럼까지 있었는데 **부를 방법이 없었다.** `POST /api/v1/rollback` 을 냈다.
head 를 뒤로 옮기지 않고 그 시점 내용으로 **새 리비전**을 만들며, epoch 는 새로 뽑는다.
적용은 별도 경로를 만들지 않고 **`/apply` 가 그대로 소비할 plan** 을 남긴다 — 롤백만의
적용 경로를 따로 두면 정작 급할 때 쓰는 것이 가장 안 밟힌 경로가 된다.

**③ 세대 보존 상한 (§8.4).** apply 세 번에 `bootstrap r2-e1 r3-e2 r4-e3` 가 그대로 쌓이고
지우는 코드가 한 줄도 없었다 — **제품화 초안에 내가 낸 운영 결함이다.** §9.1.1 이 v0.1 에
배정한 것이 GC 원장이 아니라 *수동 상한*인데 그것도 안 넣었다. 활성·미완·`bootstrap` 은
보호하고, **이름 순이 아니라 시각 순**으로 자른다(`r10` 이 `r9` 보다 앞선다).

#### 변이가 잡아낸 것 둘

- **약한 변이를 골랐다가 속을 뻔했다.** `DELETE FROM pools` 를 지웠는데 아무것도 안
  깨지길래 "테스트가 무력하다" 고 볼 뻔했는데, 실은 그 리비전에 풀 변화가 없어서였다.
  종류마다 하나씩 더하도록 테스트를 넓히니 넷 중 셋이 잡힌다.
- **`DELETE FROM backends` 는 진짜 죽은 코드였다.** `pools` 의 CASCADE 가 이미 데려간다.
  지우고, 대신 **그 CASCADE 의존을 테스트로 못 박았다** — 나중에 FK 가 RESTRICT 로 바뀌면
  롤백이 조용히 옛 백엔드를 남기기 때문이다. (24차의 "도달 불가한 방어는 죽은 코드다".)

그리고 픽스처가 한 번 거짓말을 했다 — 시퀀스는 되돌리면서 `leadership` 표는 안 비워
PK 가 충돌했고, 증상은 "리더가 못 됐다" 였지만 원인은 **코드가 아니라 테스트**였다.
**되돌리는 것과 비우는 것은 짝이다.**

---

### 제품화 초안 — 남이 띄울 수 있는 물건이 됐다 (2026-08-17)

v0.1 완료 판정은 **테스트가** 통과시킨 것이지 사람이 띄울 수 있다는 뜻은 아니었다.
그 사이를 메웠다.

```
docker compose -f deploy/docker-compose.yml up -d --build
node dist/bin/bary.js apply examples/hello.json
curl :8999   →   hello from A:11
```

- `deploy/Dockerfile` — **엔진과 에이전트가 한 컨테이너**다. 쪼갤 수 없다(§3.2 · §11.1,
  링크 교체가 안 보인다는 실측). 런타임 이미지에는 `npm` 도 devDependency 도 없다.
- `deploy/docker-compose.yml` — PG + 데이터 플레인 + 예제 백엔드. API 는 **루프백에만** 묶는다.
- `src/bin/bary.ts` — CLI. 리소스별 하위 명령은 아직 없다. export/import 는
  `GET|POST /api/v1/config/export|import` 를 그대로 부른다. 매니페스트 문법을
  새로 만들지 않는다.
- `scripts/token.mjs` — 토큰 발급. **설정에는 해시만 들어간다.**
- `scripts/quickstart.sh` — **README 의 절차를 그대로 돌리고 확인한다.** 손으로 확인하는
  문서는 반드시 썩는다.

**만들면서 나온 것 셋.** `build.sh` 가 bash 전용이라 alpine 빌드에서 `exit 127`(이미지
안에서도 도는 스크립트인데 최소 셸을 가정하지 않았다). 런타임 이미지에 `npm` 이 없어 또
127 — 의존성 스테이지를 따로 뺐다. compose 에 처음 적은 토큰 해시는 **내가 지어낸 값**이라
실제로 계산해 넣었다.

README 의 "Status: not a product yet" 을 사실에 맞췄고, **아직 아닌 것**을 같은 자리에
적었다 — 리더 선출 없음(두 번째 인스턴스는 자기도 리더라고 믿는다) · 멤버십 평면 없음 ·
TLS·ACME·GUI 없음.

---

### v0.1 완료 판정 통과 — `curl :999 → A:11` (2026-08-16)

**제품 표면이 처음으로 생겼다.** 50 회차 동안 이미 지은 것의 정확성에만 썼는데, S19 로
block 등급 게이트가 다 열린 뒤 위를 쌓아 여기까지 왔다.

```
REST → PG(changeset·plan·commit) → render → materialize(세대) → 게시 → HUP
     → 활성화 증거 → 좌표 이동 → curl :999 == BACKEND_A_11
```

지은 것: `src/store/`(정본) · `src/control/`(배선) · `src/api/`(REST+토큰 인증) ·
`src/bin/barycenterd.ts`(데몬) · `scripts/build.sh`(배포 산출물).

**데몬은 DP 컨테이너 안에서 돈다.** 호스트에서 `current` 링크를 바꾸면 컨테이너가 못 본다는
것을 다시 실측했다 — §3.2 · §11.1 의 "같은 호스트" 가 이 뜻이고, 밖에 두는 구성으로는 이
테스트를 **통과시킬 수가 없다.**

#### 실물이 드러낸 결함 넷 — 전부 초록인 상태에서 살아 있었다

| # | 무엇 | 왜 안 잡혔나 |
|---|---|---|
| ① | `verifyGeneration` 이 **중첩 디렉토리를 못 읽는다** | 세대가 평평한 테스트만 있었다. §7.2 레이아웃은 처음부터 중첩인데 검사기가 디렉토리 이름을 파일 이름과 대조했다 |
| ② | `ApplyResult.failure` 를 **아무도 안 채운다** | 주석은 *"조용히 실패하지 않는다"* 인데 `resultOf` 가 안 실었고 `lastFailure` 는 **쓰기 전용 필드**였다. ① 을 진단 못 한 이유 |
| ③ | 컨테이너에서 **자기 락을 회수 못 한다** | pid 생존 판정. "pid 재사용은 잔여 경합" 이라 적어 뒀지만 §11.1 배포에서는 **확정**이다 — 데몬은 늘 pid 1 이라 재기동이 안 됐다 |
| ④ | 렌더러에 **활성화 마커 자리가 없다** | §6.3 이 요구하는 세대별 리터럴은 모델의 일부가 아니다. 세대 안 `admin/` 조각으로 뺐다 (E62) |

**단위 217 · conformance 381 · 골든 10 · 모델 13 이 전부 초록인 채로** 넷이 살아 있었다.
"모의로만 검증한 것은 실물에서 깨진다"(4차 검수)의 다섯 번째 실례다.

#### 그리고 렌더러 변경이 608 건 사이로 조용히 지나갔다

`render()` 에 `include admin/*.conf;` 를 넣고 http 블록을 **항상** 내도록 바꿨는데
**아무 테스트도 안 깨졌다.** 출력을 손으로 찍어 보고서야 먹은 것을 알았다 — 골든은
`nginx -t` 만 보고 단위는 부분 문자열만 본다. **렌더 출력의 모양을 붙잡는 테스트가 아예
없었다.** `tests/unit/render-admin.test.ts` 6건으로 메웠고 변이 셋으로 판별력을 확인했다.

#### 남은 것

- **리더 선출이 없다.** `leaderToken` 이 환경변수다. DP 가 그렇게 정교하게 방어하는 토큰을
  **아무도 발급하지 않는다** — v0.2 의 첫 숙제.
- 멤버십 평면 없음(v0.3) → 백엔드 하나만 바뀌어도 **전 세대 전환 + HUP** 이다.
  `impact.requiresReload` 는 항상 true 이고, 그렇게 적어 뒀다.
- `428` 은 쓸 자리가 없다. 조건부 헤더를 *요구하는* 엔드포인트가 없다 — 직접 리소스 편집이
  들어오는 v0.4 에 함께 온다.
- §9.1.1 의 **B(API·DB)는 여전히 동결 전이다.** 구현이 *끝나고* 고정한다.

---

### S19 — 마지막 block 등급 게이트를 열었다 (2026-08-16)

50차까지는 **이미 지은 것의 정확성**에만 썼다. 착수 게이트 중 손도 안 댄 것이 12 개 남아
있었고 그중 **S19 만 block 등급**이었다 — 떨어지면 설계를 다시 해야 한다. 제품을 짓기 전에
답이 나와야 하는 유일한 항목이라 이것부터 열었다.

**질문:** S8(인증서를 세대에 결박)과 S11(epoch 재사용 금지)이 **롤백에서 동시에 성립하는가.**
세대에는 epoch 가 구워져 있으니 옛 세대를 다시 게시하면 epoch 가 재사용되고, epoch 를 새로
쓰려면 새 세대를 만들어야 하는데 그러면 옛 인증서를 어떻게 가져오느냐가 남는다.

**성립한다** (16 PASS / 0 FAIL). 옛 자료를 새 세대로 clone 하고 epoch 리터럴을 다시 구우면
워커는 새 epoch(E30)를, TLS 는 옛 자료(gen1)를 든다. 롤백 세대에서 멤버십 갱신도 계속 먹는다
— **롤백은 얼어붙은 스냅샷이 아니다.**

**진짜 값은 clone 을 잘못하는 두 방식이다.**

- `cp -r` 로 세대를 통째로 베끼면 **epoch 리터럴이 딸려온다.** 워커는 E10, 컨트롤 플레인은
  E40 을 믿고, E40 에 무엇을 staging 해도 **영영 안 닿는다.** 잘못된 peer 를 고르는 게
  아니라 *옛 peer 를 고집하는* 실패라 **트래픽은 정상으로 보이면서 헬스가 죽는다.**
  나이브한 구현이 정확히 이 모양이 되기 쉽다.
- 인증서를 symlink 로 clone 하면 **평소에는 멀쩡하다가** GC 가 옛 세대를 회수하는 순간
  다음 reload 가 실패한다. 열린 fd 로 트래픽은 계속 흐르므로 트래픽만 보면 모른다.

→ **롤백 clone 은 재렌더 + 바이트 복사여야 한다.** v0.6 에서 `activation_epoch` 는 clone
대상이 아니라 **렌더 입력**이다.

**프로브의 판별력을 변이로 확인했다.** 배치 A 에서 재렌더를 conf 복사로 바꾸면 3 건이,
인증서 복사를 symlink 로 바꾸면 1 건이 빨개진다. 그리고 처음 짰을 때 넣었던
`S19.same_digest` 는 **이빨이 없어서 걷어냈다** — 이 스파이크의 admin 에 digest 로직이 한 줄도
없으니 "같은 digest 라 거부됐다" 가 나올 경로가 아예 없고, **통과할 수밖에 없는 체크는
PASS 수만 부풀린다.** 그 축은 엔진이 아니라 DP 층의 질문이고 거기서 이미 본다.

---

### Certificates 화면을 연다 — 만료는 자료에서 읽는다 (2026-08-19)

v0.6 의 둘째 장. 설정에 적힌 날짜를 그리면 클라이언트가 알람을 속일 수
있다. `GET /certificates` 가 자료에서 `expiresInDays` 를 낸다. 자료가
없으면(ACME 발급 전) `facts: null` 이고 **빼지 않는다.**

주문·챌린지 상태는 그리지 않는다. `acme_orders` 는 운영 상태고 읽기
엔드포인트가 없다. 의도는 `acme` 필드다. 없는 주문을 있는 척하지 않는다.

---

### Routes 화면을 연다 — 엔진이 보는 순서다 (2026-08-19)

v0.6 의 첫 장. 컴파일러는 렌더가 이미 쓴다. GUI 가 사용자 priority 를
그대로 그리면 엔진과 다른 순서를 보여 준다. `compileHostRoutes` 가 낸
순서와 그림자 경고를 그대로 올린다. 오류가 있으면 순서를 비운다 —
반쯤 그린 순서는 거짓말이다. 패스스루는 호스트 매치 클래스가 아니라서
컴파일하지 않는다.

Kit 은 아직 없다. `/routes` 도 같은 `index.html` 폴백이다.

---

### Pools 화면을 연다 — 안 잰 것과 죽은 것은 다르다 (2026-08-19)

v0.5 세 화면의 마지막. 헬스는 스냅샷과 `health` 델타다. `/health/backends` 를
다시 치지 않는다. 관측이 없으면 `unknown` — 프로버가 아직 안 본 것과
`unhealthy` 를 섞으면 기동 직후 멤버십이 통째로 비어 보인다.

드레인 진행률은 그리지 않는다. inflight/sessions 를 재는 경로가 없다.
없는 숫자를 그려 두면 화면이 계약을 만든다.

세 경로가 됐다. Kit 은 아직 없다. 화면과 라우터 이주를 한 커밋에 넣지 않는다.

---

### 헬스 델타를 연다 — 폴링하지 않는다 (2026-08-19)

Pools 화면을 그리려면 헬스 변화가 스트림에 있어야 한다. `health_events` 는
이미 판정 변경과 같은 트랜잭션에 쓰이는데, SSE 로는 안 나갔다. 화면을 먼저
열면 `/health/backends` 를 폴링하게 된다.

스냅샷에 지금 `health` 를 실는다. `/status` 는 그대로다 — 4-way 에 헬스 표를
섞지 않는다. 판정이 바뀌면 `health` 델타 `{backendKey, state}` 다. 프로버가
꺼진 배포(멤버십 평면 없음)에서는 델타가 없다. 드레인·inflight 는 없다 —
없는 숫자를 그리지 않는다.

---

### Listeners 화면을 연다 — head 모델이고 다시 재지 않는다 (2026-08-19)

v0.5 의 둘째 장. Pools 를 먼저 열면 헬스 델타가 SSE 에 없다. 폴링하거나
없는 이벤트를 있는 척해야 한다. Listeners 는 `GET /api/v1/listeners` 와
이미 있는 revision 델타로 된다.

그 목록은 head 다. 커밋됐지만 적용 전이면 아직 nginx 에 없는 소켓이
들어 있다. 그걸 join 으로 표시하고, head 에서 빠졌지만 엔진에 남은 것은
impact 의 `removed` 로 leave 다. 충돌 판정은 검증기가 이미 했다 — 화면에
두 번째 엔진을 두지 않는다.

경로는 `/listeners` 다. Kit 은 아직 없다. 확장자 없는 폴백이 같은
`index.html` 을 내고, `pageOf` 가 자리를 가른다.

---

### Plan·Impact 화면을 연다 — 폴링하지 않고 영향을 보여 준다 (2026-08-18)

SSE 의 첫 소비자. §10 의 세 화면 중 Plan·Impact 한 장만 연다. 라우트가 하나라
SvelteKit 은 아직 없다 — Vite + Svelte 5 로 빌드하고 `barycenterd` 가 `gui/build`
를 같은 출처에서 낸다. CORS 를 열지 않기 위해서다.

페이지는 토큰 없이 열린다. `/api/v1/*` 는 그대로 Bearer 다. EventSource 는
Authorization 을 못 붙이므로 화면은 fetch 스트림 + `pullSse` 다. 스냅샷과
revision/apply 델타가 오면 `GET /status` 로 pending 을 읽고 `GET /plans/:id`
의 `impact` 를 문장으로 접는다. diff 가 아니다. `requiresReload` 가 있으면
reload 가 필요하다고 말한다.

`guiRoot` 가 없으면 예전과 같다 — `GET /` 도 토큰을 묻는다. `BARY_GUI` 를
줬는데 그 경로가 없으면 기동이 멈춘다. 없는 화면을 있는 척하지 않는다.

---

### SSE 를 연다 — GUI 는 폴링하지 않는다 (2026-08-18)

v0.5 의 첫 조각. §10 이 "GUI 는 폴링하지 않는다" 고 적어 두고 `/api/v1/events` 는
표에만 있었다. 화면을 그리기 전에 그 구멍을 메운다. 폴링하는 화면을 먼저 지으면
계약이 화면을 따라간다.

연결이 열리면 `snapshot` 으로 지금 `status` 를 주고, 커밋·import·롤백은 `revision`
델타, apply 는 `apply` 델타를 낸다. 하트비트는 SSE 주석이다 — 클라이언트가
이벤트로 세면 안 되는 신호다. 인증은 다른 읽기와 같다. `/healthz` 처럼 열지 않는다.

화면은 Plan·Impact 가 이 스트림을 구독한다. Listeners · Pools 는 아직 없다.

---

### changeset 단계를 나눈다 — apply --plan 은 새로 열지 않는다 (2026-08-18)

`bary apply <파일>` 은 한 바퀴를 통째로 돈다. 그러면 plan 을 보고 멈출 수가 없고,
import 가 남긴 pending_apply 를 적용할 명령도 없다.

나눈 것: `changeset new|patch|plan|show`, `commit --plan`, `apply --plan`.
`--plan` 은 changeset 을 열지 않는다. 롤백 plan 은 changeset 이 없으므로
`commit --plan` 이 거절한다 — 그쪽은 `bary rollback` 이다.

파일 한 장짜리 `plan`/`apply` 단축은 남겼다. 없애면 지금 돌아가는 스크립트가 끊긴다.

---

### export/import 를 연다 — 같은 장을 두 번 넣어도 같다 (2026-08-18)

v0.4 의 첫 조각. CLI 에 리소스별 하위 명령은 아직 없다. 있는 것은 §5.5 가
요구한 **안정 키 매니페스트** 다.

export 는 spec 만 낸다. `key` 가 옆자리이고 id/revision 은 없다. 모델에 UUID 가
없으므로 remap 표는 만들지 않았다 — 만들 것이 없다. import 는 한 장의
changeset 이다. 차이가 없으면 changeset 을 안 만든다. 그게 "두 번 넣어도
결과가 같다" 의 강한 쪽이다.

merge 는 매니페스트에 있는 것만 맞춘다. replace 는 없는 키를 지운다. YAML 은
안 만든다 — CLI 가 API 와 다른 모양을 들면 둘이 갈라진다.

`bary export` / `bary import` 가 `GET|POST /api/v1/config/export|import` 를
그대로 부른다. nginx 적용은 여전히 `apply` 의 몫이다. import 가 적용까지 하면
그 경로만 덜 검증된다.

---

### 기동에서 핀을 집어넣는다 — apply 경로는 그대로다 (2026-08-18)

로더와 키트는 테스트가 소비자였다. 이제 `barycenterd` 가 기동에서 같은 길을 간다.

`BARY_DRIVER_PINS` 가 없으면 아무 것도 안 집어넣는다. `GET /status` 의 `driver` 는
`{ loaded: false }` 다. 핀이 있으면 목록 전체를 검사하고(X7 — 고르지 않은 핀의
apiVersion 도 기동을 멈춘다) 고른 이름만 로드한다. 로드된 capability 는 status 에
그대로 보인다. 엔진 쪽 S14 표는 `engine.nativeDns` 로 따로 드러낸다 — 둘을 섞지
않는다. 드라이버가 제공하지 않는 것을 엔진이 한다고 덮어쓰면 GUI 가 다시 거짓
선택지를 그린다.

**설정 평면은 `LocalDataplaneDriver` 다.** 참조 드라이버는 applyConfig 가 아니다.
그걸 갈아 끼우는 계약을 지금 만들면 구현하지 않은 ABI 를 고정하는 것이다.

---

### 로더의 소비자를 만든다 — 참조 드라이버와 호환성 키트 (2026-08-18)

로더만 있고 소비자가 없으면 §9.1 이 경고한 상태였다. 첫 소비자는
`drivers/reference.mjs` 다. 코어가 그걸 정적으로 import 하지 않는다. 테스트가
`loadDriver` 로 경로를 집어넣고, `capabilitiesFromDriver` 가 내용을 읽는다.

참조는 엔진을 재지 않는다. 그래서 `nativeDns.available: false` 다. 없는 능력을
있다고 하지 않는다. available: true 를 내려면 S14 표와 **같아야** 한다 —
`nxdomain=retry` 같은 선택형은 `DriverContractError` 다.

사내 레포는 `node scripts/driver-compat.mjs <entry>` 로 같은 키트를 자기
엔트리에 댄다. 코어를 수정하지 않는다. 그게 v0.7 완료 판정("사내 레포가 코어
수정 없이 빌드·로드")의 절반이다.

`BackendDiscovery` 는 아직 안 연다. 표면 테스트가 `Discovery` 를 빼 둔 이유가
그대로다 — 받는 쪽이 없다.

---

### 로더를 연다 — 설정의 이름을 그대로 import 하지 않는다 (2026-08-18)

v0.7 의 다음 조각. §9.3 은 allowlist · sha512 · apiVersion 을 문장으로만 갖고 있었다.
TESTS X5–X7 도 표만 있었다. 그걸 `src/dp/loader.ts` 로 옮겼다.

설정은 이름을 고른다. 그 이름이 이미지에 pin 된 목록에 있을 때만 파일을 연다.
바이트가 핀과 같을 때만 `import()` 한다. apiVersion 이 코어와 다를 때는 모듈을
호출자에게 주지 않고, 목록에 틀린 핀이 하나라도 있으면 `assertDriverPins` 가
기동을 멈춘다. 쓰지 않을 핀이라며 건너뛰면 그게 곧 조용한 degrade 다.

변별력: 평가 시 던지는 모듈을 만들어 두고, X5·X6·핀 X7 에서는 그 예외가 **안**
나온다는 것으로 "열지 않았다" 를 잰다. 해시만 다르고 내용이 맞는 파일은
`integrity_mismatch` 이지 성공이 아니다.

**아직 아닌 것.** 이 로더로 외부 패키지를 집어넣는 배선, 참조 구현 키트,
`BackendDiscovery`. 로더만 있고 소비자가 없으면 §9.1 이 경고한 그 상태다.

---

### DataplaneCapabilities 를 연다 — S14 가 선택형이 없다고 했다 (2026-08-18)

S14 가 남긴 사실은 문서에만 있었다. `dnsResolve` 는 “엔진이 `resolve` 를 하는가” 라는
boolean 하나뿐이고, `on_nxdomain`/`on_timeout` 은 DESIGN 표에만 있었다. 그 상태로
CLI·GUI 를 열면 **엔진이 무시하는 선택지**를 보여주게 된다. 그게 S14 가 막으려던 것이다.

그래서 모델에 선택형을 넣지 않았다. 넣은 것은 사실의 표다.

```
NATIVE_DNS_FAILURE_MODES = {
  nxdomain: 'drop_peer',   // 502
  servfail: 'keep_last',
  timeout:  'keep_last',
}
```

`resolve` 가 없으면(`available: false`) 표 자체를 안 준다. 없는 경로의 정책을 그릴 이유가
없다. 해독기는 `dns`·`on_nxdomain`·`on_timeout` 을 `unknown_field` 로 거절한다.

`DataplaneDriver.capabilities()` 는 아직 없다. 구현하지 않은 메서드를 인터페이스에 올리면
§9.1 이 막으려던 일이 다시 난다. 조립 함수 `dataplaneCapabilitiesOf` 가 첫 소비자이고,
로더가 설 때 메서드가 따라온다.

이건 `src/` 를 움직인다. 표면 동결 카운터는 0 으로 돌아간다 — 이번 일의 목적이다.

**아직 아닌 것.** 로딩 하드닝(X5–X7) · 참조 구현 키트 · `BackendDiscovery` 계약.
S14 「기존 세션」과 h3/`transportOf()` 는 이번에도 손대지 않는다.

---

### S12 가 빨개졌다 — 그리고 그건 계측기였다 (2026-08-18)

같은 게이트에서 **S12(block 등급)** 도 빨개졌다. `next=failed` — 크래시 복구 뒤에 던지는
후속 gen-3 오퍼레이션이 실패했다는 것이고, 그건 **실행권이 안 놓였다**는 뜻으로 읽힌다.
설계 재작업 등급이다.

**추측하지 않고 반복했다.** 네 번 돌려 이렇게 나왔다:

| 회차 | 실패 지점 |
|---|---|
| 게이트 | #10 · #25 |
| 단독 1 | #8 · #35 |
| 단독 2 | #18 · #37 |
| 단독 3 | 없음 (통과) |

**회차마다 다른 지점이다.** 그리고 이 판별법은 러너 자신이 적어 뒀다 — *"고정된 로직
결함이면 같은 지점이 나온다."* 게다가 실패한 회차에서도 `link` 와 `served` 는 **매번
gen-2** 였다. 세상은 멀쩡했다. 통과한 회차는 복구 재시도가 지점당 100/100(한 번에 성공),
실패한 회차는 63~65 였다 — **부하 의존**이다 (기계 load average 6~8).

#### 처방을 절반만 줬었다

복구 단계는 이미 같은 병을 앓고 고쳤다: 러너가 활성화 증거를 예산 안에 못 보면 `failed`
라고 말하는데 세상은 이미 수렴해 있다(S7). 그래서 **유계 재시도**를 넣었다.

**후속 단계에는 그걸 안 줬다.** 한 방에, 그것도 **러너의 말만 보고** 판정하고 있었다.

그래서 판정을 **러너의 말에서 세계로 옮겼다.** §12.0 이 요구하는 것은 *"다음 오퍼레이션이
막히지 않는다"* 이고, 그건 **gen-3 가 실제로 서빙되는가**다. `phase` 문자열은 그 사실의
보고일 뿐이고 보고는 늦을 수 있다. 보고를 재면 계측기를 재게 된다 — S16 에서 `s_client`
의 `Protocol :` 줄로 물린 그 형태다. 유계 재시도도 복구와 똑같이 줬다.

**약화가 아니다.** 오히려 빈 `phase=`(러너가 출력조차 못 낸 회차)까지 정직하게 판정된다 —
러너가 죽어도 세상이 gen-3 로 갔으면 실행권은 놓인 것이다.

#### 판별력은 src 변이로 확인했다

`apply.ts` 의 **종단 재진입에서 실행권을 안 놓게** 고쳤다(14차가 고쳤던 바로 그 결함).
스윕이 잡는다. 그리고 새 출력이 **진단적**이다:

```
#25 reload:after → phase=(3회) ... next=(3회) next_link=generations/gen-2 next_served=gen-2
```

`next_link`·`next_served` 가 gen-2 에 멈춰 있다 — **세계가 안 움직였다.** 옛 `next=failed`
한 단어로는 "막혔다" 와 "보고가 늦다" 를 구분할 수 없었다. 이제 구분된다.

고친 판으로 같은 부하에서 두 번 돌려 둘 다 5/0 이다.

> ⚠️ **옛 실패들이 전부 계측기였다고 단정하지는 않는다.** 옛 출력에 `next_link`·
> `next_served` 가 없었으므로 소급 판정이 불가능하다. 말할 수 있는 것은 둘이다:
> 옛 계측기는 두 경우를 **구분할 수 없었고**, 새 계측기는 같은 부하에서 초록이다.

---

### 핀 게이트가 한 발 늦다 — 그것이 방금 위반을 놓쳤다 (2026-08-18)

S20 을 태우려고 게이트를 돌렸더니 **직전 커밋**이 빨갛게 나왔다: 세대 보존 커밋이
`src/` 둘을 바꾸면서 `Pinned-by:` 표식을 안 달았다.

**커밋 전에 게이트를 초록으로 봤는데도 그렇다.** `verify.sh` 는 핀 검사를 `HEAD~1` 기준으로
돌린다 — 그 시점의 HEAD 는 *그 전* 커밋이므로, **내가 지금 만들 커밋은 구조적으로 검사
대상이 아니다.** 위반은 언제나 다음 사이클에 잡히고, 그때는 이미 push 된 뒤다.

표식이 없었을 뿐 재현물 자체는 있었다(`tests/unit/retention.test.ts` 의 시간 보호 넷).
그래도 규칙 위반은 위반이다. 그리고 **push 된 커밋을 고치려고 강제 푸시하지 않는다** —
공유 이력을 되쓰는 것이 표식 하나보다 위험하다. 앞으로 갚는다.

#### 그 한 발을 훅으로 메운다

이 저장소는 이미 *"규칙이 산문이라 안 지켜지므로 게이트로 옮긴다"* 를 했다(48차). 그런데
그 게이트가 **한 박자 늦게** 도는 자리에 있었다. `core.hooksPath` 로 걸린 커밋된 훅 층이
이미 있으니(`scripts/git-hooks/`), 거기에 `commit-msg` 를 더했다 — 커밋이 만들어지기
**전에** 표식의 존재와 형식을 본다.

**`pinned.mjs` 를 대신하지 않는다.** 훅은 표식이 *있는지*만 본다. 표식이 가리키는 테스트를
부모 트리에 대고 돌려 빨간지 확인하는 것은 여전히 게이트의 몫이다 — 커밋마다 테스트를
돌리면 아무도 안 쓴다. `--no-verify` 로 우회되고, `core.hooksPath` 를 안 건 클론에서는
아무 일도 안 일어난다. **경계가 아니라 앞당김이다.**

**훅이 도는지를 게이트가 잰다.** 안 그러면 "있는데 안 도는" 층이 된다. `pre-commit` 쪽
테스트도 여태 게이트 밖에 있었으므로 함께 걸었다.

#### 여기서도 판별력 없는 단언을 하나 썼다

변이 넷 중 셋은 잡혔는데 **④(스테이지 대신 작업 트리를 본다)가 안 잡혔다.** 「미스테이지
src 수정은 안 센다」 테스트가 **추적되지 않는** 새 파일을 썼기 때문이다 — `git diff` 는
untracked 를 안 보여주므로 변이가 아무것도 안 바꾼다. 추적된 파일 수정으로 바꾸니 잡힌다.
다섯 번째다.

---

### S14 — 폴백이 실제로 무엇을 하는지 (2026-08-18)

v0.7 의 다음 항목. §7.3 은 대안 B(엔진 네이티브 DNS 디스커버리)의 범위를 **표로 적어 두기만**
했지 재 본 적이 없었다. 특히 *"NXDOMAIN 과 timeout/SERVFAIL 의 네이티브 동작이 다르며 우리
모델의 선택형과 1:1 대응하지 않는다"* — 이건 주장이었다. 사실로 바꿨다.

**통제 DNS 를 직접 짰다.** `timeout` 을 만들려면 **응답을 안 주는** 서버가 필요한데, 서버를
죽이면 ICMP port unreachable 이 돌아가 timeout 이 아니라 즉시 실패가 된다 — 다른 실험이다.
dnsmasq·CoreDNS 로는 그 침묵을 못 만든다. 그래서 와이어 포맷을 손으로 짰다(`der.ts` 와 같은 이유).

#### 결정적인 것: 실패 모드 셋이 갈린다

| DNS 응답 | 엔진 동작 | error.log |
|---|---|---|
| **NXDOMAIN** | **peer 를 뺀다 → 502** | `resolved (3: Host not found)` |
| **SERVFAIL** | 마지막으로 알던 peer 를 계속 쓴다 | `resolved (2: Server failure)` |
| **무응답(침묵)** | 마지막으로 알던 peer 를 계속 쓴다 | (조용하다) |

**NXDOMAIN 만 fail-closed 다.** 그리고 셋 다 **설정할 수 없다.** 즉 우리 모델의
`on_nxdomain`/`on_timeout` 선택형은 대안 B 에서 **표현 불가능**하고, 드라이버 capability 로
그 사실을 드러내야 한다 — 조용히 무시하면 GUI 가 없는 선택지를 보여주게 된다.

그 밖에 A×(HTTP·TCP·UDP) 분산, AAAA 단독, SRV 의 대상·포트, `valid=` 가 레코드 TTL 을 덮는
것을 쟀다. **8 개 중 7 개가 안정적으로 통과한다.**

**남은 하나는 「기존 세션」이고, 통과시키지 않았다.** 한 회차에서 안 끊기는 것을 봤지만
(줄 수 4→17, 마지막이 여전히 `be-a-17`) **재현이 안 된다** — 긴 연결이 아예 안 서는 회차가
있다. 그건 "연결이 끊겼다" 가 아니라 **"못 쟀다"** 이고, 판정 메시지도 그렇게 갈라 놓았다.
관측된 방향은 §7.3 표의 *"`quiesced`·deadline·강제 종료가 안 된다"* 와 맞아떨어지지만
— 엔진은 **기다려 주되 재촉할 수 없다** — 한 번 본 것을 사실로 적지는 않는다.

**그래서 S14 는 게이트에 안 넣는다.** 7/8 이면 넣을 만해 보이지만, 빨간 하나를 알면서
넣으면 빨강을 무시하는 법을 익힌다 (S20 과 같은 판단이다). `./spike/s14/run.sh` 로 돌린다.

#### 이 회차에 계측기로 네 번 물렸다

1. **`echo | nc` 가 응답을 못 읽는다.** busybox nc 는 stdin 이 EOF 면 나가 버린다. tcp·srv 가
   빈칸으로 나왔고 하마터면 "stream 이 안 된다" 로 읽을 뻔했다. 같은 실행의 다른 측정에서
   tcp 가 값을 받은 것이 단서였다 — 엔진 결함이면 거기서도 빈칸이어야 한다.
   `engine_facts.sh` 의 `SPROBE` 가 이미 `{ ...; sleep 1; } | nc` 를 쓰고 있었다.
2. **`docker exec` 안에서 `&` 로 띄운 긴 연결이 exec 와 함께 죽었다.** "연결이 안 섰다" 가
   아니라 "내가 죽였다" 였다. `-d` 로 띄운다.
3. **TTL 판정에 판별력이 없었다.** *"http 가 stream 보다 먼저 바뀐다"* 는 두 폴링의 **우연한
   시차**로도 참이 된다 — `valid=` 를 떼는 변이가 그대로 통과했다. 그래서 대조를 뒤집었다:
   레코드 TTL 을 2초로 짧게, `valid=` 를 20초로 길게. **20초를 기다린다면** 그건 TTL 이 아니라
   `valid=` 를 따른 것이고 다른 해석이 없다. 변이에서 2초로 내려오는 것을 확인했다.
4. **그 고침이 다른 측정을 부쉈다.** `valid=20s` 를 **전역 resolver 에** 걸었더니
   a_records·dns_failure·session 이 함께 빨개졌다 — 다른 측정들이 "몇 초면 재조회된다" 를
   전제로 서 있었기 때문이다. 계측기 하나를 고치다 나머지를 부순 것이고, TTL 측정만 맨 뒤로
   빼서 **별도 conf·별도 인스턴스**로 격리했다.

그리고 **겹쳐 돌리는 실수를 이 회차에 두 번 했다.**

한 번은 스파이크 둘끼리다. 같은 컨테이너·네트워크 이름을 쓰므로 나중 실행의 `cleanup` 이
앞 실행의 엔진을 죽였고, 그 실행의 빨강 셋을 하마터면 엔진 결함으로 읽을 뻔했다.
이제 러너가 **겹침을 감지하면 거부한다.**

두 번째는 **S14 가 아직 도는 중에 전체 게이트를 시작한 것**이다. 결과는 요란했다 —
golden 이 빨개지고 스파이크 아홉이 한꺼번에 깨졌으며, S17 은 컨테이너 안에서 구운
인증서 파일이 **아예 없다**고 했다. 코드가 그 사이 바뀐 것이 없으니 환경이 유일한
설명이었고, 실제로 그랬다(load average 11). **`apk add` 가 지금은 되는지 직접 확인해서**
그 가설을 좁혔다 — 된다. 남는 원인은 겹침뿐이다.

S12 가 같은 교훈을 이미 줬는데(*"게이트를 직렬화한다"*) 스파이크에는 잠금을 넣고 정작
**나 자신은 같은 실수를 반복했다.** 잠금이 스파이크 안에만 있고 내 손끝에는 없다.

---

### S20 — h3 는 되는데, 그래서 h3 를 안 넣는다 (2026-08-18)

v0.6 이 끝났으니 v0.7 의 첫 항목인 S20 을 열었다. §12.0 이 *"h3 클라이언트를 구하는
것이 첫 과제"* 라고 적어 둔 그 과제부터 풀었다 — `ymuski/curl-http3`(quiche 0.18)이
있고, 엔진도 `--with-http_v3_module` + **OpenSSL 3.5.7** 이라 선다. 서버측 QUIC API 가
들어온 것이 OpenSSL 3.5 라, 이 조합이 아니었으면 스파이크 자체가 성립하지 않았다.

**계측기를 먼저 잡았다.** `curl --http3` 은 실패하면 조용히 h2/h1 로 내려간다 — 그걸로
재면 "h3 가 된다" 가 아니라 "무언가로 붙었다" 를 재게 된다. S16 에서 `s_client` 의
`Protocol :` 줄에 똑같이 물렸었다. `--http3-only` 만 쓰고 `%{http_version}` 으로 판정하되,
**그 플래그가 진짜 강제하는지를 먼저 쟀다**: TCP 전용 서버에 던져 실패하는 것을 확인하고,
같은 서버에 h2 는 붙는 것으로 "네트워크가 죽은 게 아니다" 를 갈랐다.

**7/8 이 통과했다.** h3 로 실제 요청이 오가고(`$server_protocol` = `HTTP/3.0`), 한 포트에서
h1.1·h2·h3 가 공존하고, Alt-Svc 로 **실제 승격**이 일어나고(헤더 존재만이 아니라 2회차가
h3 로 간다), reload 중 진행 중인 h3 요청이 **안 끊긴다**.

#### 깨진 하나가 결정적이다

②「UDP 점유가 §4.5 겹침 검증에 잡힌다」가 깨졌다. 우리 검증기가 못 잡고 — `transportOf()`
가 전송을 **하나만** 돌려주므로 https 리스너를 tcp 로만 예약한다 — **엔진도 못 잡는다.**

여기서 한 번 자책할 일이 있었다. 처음엔 컨테이너가 "떴다" 는 것만 보고 *"둘 다 조용히
떴다 → 패킷이 갈린다"* 라고 적었다. **그건 관측이 아니라 추측이다.** 누가 답하는지를
안 쟀다. 다시 재니 실제 모양은 이랬다 (E65 로 고정했다):

- `nginx -t` **통과** — 정적 검사로 못 잡는다
- 런타임 **안 죽음**, UDP 소켓이 **둘** bind (quic 단독 대조군은 하나)
- 데이터그램은 **stream 이 먹는다**, h3 는 **rc=28 로 죽는다**
- `error.log` **경고 0 줄**

운영자가 볼 수 있는 신호가 하나도 없다. "거절당한다" 였으면 차라리 나았을 결말이다.

**그래서 §12.0 의 축소 규칙을 그대로 적용한다 — h3 는 모델에서 계속 뺀다.** 지금 상태가
이미 그러하므로 코드는 안 바뀐다. 바뀐 것은 **근거**다: 전에는 "안 재 봐서" 뺐고, 이제는
**"열려면 검증기가 먼저 다중 전송을 표현해야 해서"** 뺀다.

S20 은 게이트에 안 넣는다. 빨간 걸 알면서 넣으면 빨강을 무시하는 법을 익히게 된다.
대신 **엔진 사실 E65 로 갈라 넣었다** — 충돌 부분은 h3 클라이언트 없이도 재지므로
게이트 안에서 초록으로 산다. 사실이 썩지 않는다.

#### 계측기가 또 거짓말했다 (여섯 번째)

E65 를 처음 돌렸을 때 값은 셋 다 예상대로인데(`udp소켓=2, 응답='udp-stream', 경고=0`)
판정만 빨갰다. `grep -c` 는 못 찾아도 **"0" 을 찍고 종료코드 1** 을 주는데 거기에
`|| echo 0` 을 붙여 놨다 — 값이 `"0\n0"` 이 되어 `-eq` 가 깨졌다. 판정식이 값을 이긴
경우이고, 이 저장소에서 여섯 번째다.

---

### 대조군이 동전 던지기였다 — S18 이 스스로 빨개져서 알려줬다 (2026-08-18)

보존 변경을 게이트에 태웠더니 **엉뚱한 데가 빨개졌다**: S18 의 `nonce.control`.

이건 `badNonce` 재시도를 **끈** 대조군이다. 재시도가 없으면 실패해야 하고, 그래야
②(재시도 켬, 12 회 전부 성공)가 무언가를 증명한다. 그게 "0 회 실패" 로 나왔다.

**설정 버그가 아니었다.** `nonceRetries ?? 5` 이므로 0 은 0 으로 산다 — `||` 였다면
0 이 5 로 살아났겠지만 아니었다. 남는 원인은 하나뿐이다: **확률.**

여기서 한 번 더 틀렸다. `PEBBLE_WFE_NONCEREJECT=20` 을 "20% 거절" 로 읽고 `0.8¹² = 6.9%`
라고 계산했는데, **고쳐 놓고 세 번 돌려 보니 그 숫자가 틀렸다.** 첫 실패가 나온 주문
번호가 **3·13·14·7** 이었다. 기하분포로 `p̂ = 4/37 ≈ 0.11` 이고, 옛 12 회 루프의 거짓
빨강은 `0.89¹² ≈ 25%` — **네 번에 한 번**이다. 네 번의 재실행 중 **둘이 옛 코드였으면
빨간색**이고, 그게 곧 관측된 근거다. 상수의 이름(`20`)을 믿고 확률을 계산한 것이 잘못이었다.

가장 아픈 부분은, 이 러너가 바로 그 논리를 **알고 있었다**는 것이다. 같은 시나리오
안에 *"재시도를 끈 클라이언트로 그냥 `register()` 를 부르면 20% 확률로 거기서 죽고,
그러면 시나리오가 무작위로 빨개진다"* 라고 적혀 있다. 준비 단계에는 그 방어를 걸어
놓고 **정작 재는 루프에는 안 걸었다.**

**고정 횟수를 늘리지 않았다.** 늘려도 동전 던지기는 남는다. **관측될 때까지 돌리고
상한에서만 실패**하도록 바꿨다. 상한은 실측 `p̂ = 0.1` 이 아니라 **그 절반(0.05)을 가정해**
300 으로 잡는다 — 표본이 셋뿐인데 p̂ 을 믿고 조이면 방금 밟은 함정을 한 번 더 밟는다.
`0.95³⁰⁰ ≈ 2.4e-7`. 흔한 경우엔 열 번쯤에 끝나므로 **오히려 빠르다.**

**판별력은 변이로 확인했다.** 대조군에서 `nonceRetries: 0` 을 떼자 300 회 동안 badNonce 가
한 번도 안 잡혀 빨개졌다 — 상한이 장식이 아니라 실제 판정이라는 뜻이다.

②(재시도 켬)에도 같은 계산이 남아 있다 — 주문당 실패 확률 `0.2⁶ = 6.4e-5`, 12 회면
`7.7e-4`. 천삼백 번에 한 번이라 그대로 둔다. **없앤 게 아니라 크기를 재고 남겼다.**

---

### 세대 보존이 시간을 본다 — 부채의 나머지 절반 (2026-08-18)

앞 회차가 `worker_shutdown_timeout` 을 열어 **전제만** 놓았다. 이제 보존이 그걸 읽는다.

**개수 상한은 우연한 보호였다.** S13 이 실측한 대로 마커로는 옛 워커를 셀 수 없으니,
오래 사는 연결(WebSocket)을 든 워커가 전환 10 회를 넘겨 살아남으면 그 세대가 지워진다.
그때 무슨 일이 나는지도 이미 실측돼 있다(S8): **트래픽은 계속 흐르고 다음 reload 가
깨진다.**

#### 새 상태를 안 만든다

세대 i 는 **세대 i+1 이 만들어진 순간** 비활성이 된다. 그러니 비활성 시각은 디스크의
mtime 에서 나온다 — `serving_generations` 원장을 따로 두면 **그 원장이 실제와 어긋날
자리**가 생긴다. §8.4 가 refcount 를 버리고 "참조 테이블이 정본" 으로 간 것과 같은 판단이다.

여유를 두 배로 잡는다. 상한은 *워커가 죽는 시각*이지 **publish→HUP 사이 지연까지 덮지
않는다.**

#### 없는 보호를 있는 척하지 않는다

`worker_shutdown_timeout` 이 없는 배포에는 **이 보호가 없다.** 기본값을 넣어 "대충
안전하게" 만들 수도 있었지만 안 했다 — 그러면 상한이 없는 배포가 보호받는다고 **믿게**
된다. 변이 실험에서 그 유혹을 확인했다: 기본값 60초를 넣자 "창을 안 주면 보호가 없다"
단언이 빨개졌고, **기존 테스트 둘도 함께 깨졌다.**

변이 셋이 각자 잡힌다: 시간 보호 제거 / 창과 무관하게 항상 보호(상한이 사라짐) /
창을 안 줘도 기본값 사용.

---

### `worker_shutdown_timeout` — S13 이 남긴 부채를 갚는다 (2026-08-18)

S13 이 *"모르는 것을 유계로 바꿀 수 있다"* 까지 재고 끝났다. 그 필드를 실제로 열었다 —
그리고 **켤 때의 대가를 먼저 쟀다.**

| 설정 | in-flight 요청 |
|---|---|
| 없음 (기본) | **끝까지 간다** — 느린 요청이 200 으로 완료 |
| `2s` | **응답 없이 죽는다** — `curl exit=52` (empty reply), 본문 0 바이트 |

**502 도 부분 응답도 아니다.** 클라이언트는 네트워크 장애와 구분할 수 없고, 비멱등
요청이면 부작용이 이미 일어났을 수 있다. 그래서 기본은 **안 내는 것**이다.

#### 모델에 전역 설정 자리를 처음 열었다

`Model.engine` 이 생겼다. **일부러 좁다** — nginx main 컨텍스트에는 수십 개 디렉티브가
있지만 여기 여는 것은 **재 보고 대가를 아는 것**뿐이다. 자유 문자열 주입 같은 탈출구를
열면 렌더러가 보장하는 것이 통째로 무너진다(`cipher_preset: custom` 을 없앤 것과 같은
이유).

#### 아직 안 한 것

**세대 보존이 이 값을 안 읽는다.** 상한이 걸린 배포에서도 GC 는 여전히 숫자 상한만 본다.
시간 기반 보존이 다음이고, 그때 이 필드가 전제가 된다 — 지금은 **전제를 놓았을 뿐**이다.

테스트는 "설정이 나간다" 가 아니라 **"켜면 무슨 일이 일어나는가"** 를 잰다. 변이 양방향
(안 적어도 항상 냄 / 적어도 안 냄)이 각각 2 건씩 깬다.

---

### S13 — 워커 레지스트리를 마커로 짓는 길이 막혀 있었다 (2026-08-18)

v0.6 의 마지막 게이트다. 합격 기준 셋 중 **하나는 이미 구조적으로 성립**하고 있었고,
하나는 **길 자체가 막혀 있었다.**

#### 이중 감소는 표현할 수가 없다

§8.4 가 v1·v2·v3 의 refcount 설계가 어떻게 깨지는지 적어 뒀다(영구 누수 / 이중 감소).
우리는 그 결론대로 **별도 카운터를 안 둔다** — root 를 그때그때 정본에서 모은다. 감소시킬
카운터가 없으므로 이중 감소도 누락도 **만들 수가 없다.** 크래시가 남길 수 있는 최악은
"이번에 안 지웠다" 이고, 다음 스윕이 다시 집는다.

#### 마커로는 옛 워커를 못 센다

이 게이트 이름이 "마커·워커 레지스트리" 인 이유는 §6.3 의 세대 마커로 그걸 세려는 계획이
있었기 때문이다. **안 된다.**

옛 워커에 in-flight 를 걸어 살려 둔 뒤 마커를 40 회 두드렸더니 **새 세대만 답했다.**
HUP 뒤 옛 워커는 **리스닝 소켓을 닫고** in-flight 만 처리하므로 새 요청이 옛 워커에
절대 안 간다. S7 의 A4.3 이 가리킨 자리이고, 여기서 길이 끝난다.

nginx 는 "어느 워커가 어느 세대인가" 를 안 알려준다. 알 수 있는 것은 **몇 개 살아
있는가** 뿐이다.

#### 그래서 모르는 것을 유계로 바꾼다

`worker_shutdown_timeout` 이 잔존 창에 상한을 건다 — 실측(2초 상한에서 워커 3 → 2).
그러면 GC 는 *"어느 세대를 쓰는가"* 를 몰라도 **"이 시간이 지나면 아무도 안 든다"** 를
쓸 수 있다. 모르는 것을 없애는 대신 **유계로 바꾸는** 수법이다.

#### 지금 상태를 정직하게

**코드에 `serving_generations` 가 아예 없다.** 보호 목록은 방금 만든 것·게시된 것·미완
오퍼레이션뿐이고, 옛 워커가 든 세대는 **숫자 상한(10개)으로만 우연히 보호된다.**

대가를 적어 뒀다 — 오래 사는 연결(WebSocket)을 든 워커가 세대 전환 10 회를 넘겨 살아남으면
그 세대가 지워질 수 있고, 그러면 **트래픽은 계속 흐르고 다음 reload 가 깨진다**(S8 실측).
`worker_shutdown_timeout` 을 노출하는 것이 다음 회차의 일이다. 켜면 in-flight 가 잘리므로
기본값으로 둘 수 없고, 그건 트래픽 정책이라 모델 필드가 필요하다.

**S13 의 실패 규칙이 "GC 보수화" 였는데, 실제로 보수적인 쪽이 답이었다.**

---

### OCSP stapling — 짓지 않기로 했다 (2026-08-18)

§4.6 후보 타입에 `ocsp_stapling: boolean` 이 있었다. **재고 나서 뺐다.**

**① 우리 하네스의 어떤 CA 도 OCSP URL 을 안 준다.** 자체서명도, S18 이 쓰는 Pebble 도
AIA 가 **아예 없다**(`infoAccess: null`).

**② nginx 는 조용히 무시한다.** AIA 없는 인증서에 `ssl_stapling on` 을 켜면 `nginx -t` 는
**통과하고** 경고만 남는다(`"ssl_stapling" ignored, issuer certificate not found`).
클라이언트에게는 `OCSP response: no response sent` 가 간다.

즉 지금 넣으면 **설정에는 있는데 아무 일도 안 하고 아무도 못 잰다** — 이 저장소가 반복해서
잡아온 그 모양이다.

**③ 그리고 주 CA 가 OCSP 를 접었다.** Let's Encrypt 가 **2025-05-07 에 인증서에서 OCSP
URL 을 제거**하고 **2025-08-06 에 응답자를 껐다**. 이유는 프라이버시고 대체는 CRL 이다.
우리가 방금 지은 ACME 발급 경로의 기본 CA 가 OCSP 를 안 주므로, `ssl_stapling on` 은
그 경로에서 **정의상 no-op** 다.

**되살릴 조건을 함께 적었다.** OCSP 를 계속 서비스하는 CA 를 지원하게 되고 스테이플이
실제로 나가는 것을 잴 수 있게 되면 다시 본다. **판정 도구는 이미 있다** —
`openssl s_client -status` 가 스테이플 유무를 구분한다(실측). 없는 것은 CA 쪽이다.

이건 "나중에" 가 아니라 **결정**이다. S9(SNI 3분기 관측)를 "현행 유지" 로 닫은 것과 같은
종류 — **잴 수 없는 것을 넣으면 켰다는 사실만 남는다.**

---

### S12 간헐의 진짜 원인은 내 운용이었다 (2026-08-18)

S12 가 게이트 안에서만 또 빨개졌다. 앞서 "부하 의존" 으로 진단하고 §12.0 기준대로
**상한 안의 중복 cycle** 을 허용하도록 고쳤는데도 재현됐다.

이번 실패 기록은 달랐다: `phase=(3회)` — **세 번 다 RESULT 줄이 없었다.** 복구 러너가
판정을 못 낸 게 아니라 **아예 돌지 못하고 죽었다.**

원인을 보니 코드가 아니라 **내가 게이트를 두 개 겹쳐 돌리고 있었다.** 앞 회차 게이트가
끝나기 전에 다음 회차 게이트를 띄웠고, 도커 컨테이너·CPU 를 두 벌이 나눠 쓰면서 러너가
자원 부족으로 죽은 것이다. 세션 앞부분에서 **같은 실수로 컨테이너 이름 충돌**을 한 번
겪고도 다시 했다.

직렬화하고 컨테이너를 치운 뒤 S12 는 5/5 로 통과했다.

**교훈은 새롭지 않다.** 게이트가 빨간데 코드에서 원인을 못 찾으면 **하네스와 실행 환경을
먼저 본다.** 이 세션에서 계측기가 답을 바꾼 것이 다섯 번째다 — s_client 의 `Protocol:`
줄 · manifest digest 손계산 · reload grep 패턴 · TLS1.3 cipher 파서 · 그리고 이번의
게이트 중복 실행.

---

### HSTS — 되돌릴 수 없는 유일한 설정 (2026-08-18)

이 제품의 다른 기본값들과 성질이 다르다. h2 는 잘못 켜도 끄면 그만이지만 **HSTS 는
클라이언트 쪽에서 되돌릴 수 없다** — 인증서가 깨지면 사용자에게 우회 수단이 없고, 설정을
되돌려도 이미 나간 헤더는 회수가 안 된다. 그래서 **기본이 꺼짐**이다.

**preload 는 검증기가 막는다.** 목록이 브라우저 빌드에 구워져 빼는 데 수개월이 걸리므로,
`max-age` ≥ 1년과 `includeSubdomains` 없이는 저장이 안 된다.

#### nginx 의 함정: `add_header` 는 상속이 아니라 대체다

location 에 `add_header` 가 **하나라도** 있으면 상위 server 의 것이 **전부 사라진다.**
실측:

| 위치 | HSTS |
|---|---|
| 자기 `add_header` 없는 location | 나온다 |
| 자기 `add_header` 있는 location | **안 나온다** |
| 500 응답 (`always`) | 나온다 |

렌더러는 지금 location 에 `add_header` 를 하나도 안 내고 **그 사실에 기대고 있다.**
골든이 그 불변식을 지킨다 — 응답 헤더 기능을 라우트에 붙이는 사람은 거기서 걸린다.

`always` 도 같은 맥락이다. 없으면 2xx/3xx 에만 붙는데, **인증서가 깨져 5xx 를 내는
동안이 바로 downgrade 를 막아야 하는 순간**이다. 변이로 확인했다 — `always` 를 빼면
에러 응답 단언이 빨개진다.

변이 넷이 각자 잡힌다: `always` 제거 / `includeSubDomains` 누락 / preload 요구조건 무시 /
HSTS 미출력.

---

### 암호군 정책 — 그리고 변별력 0 인 테스트를 두 번 썼다 (2026-08-18)

§4.6 이 *"버전된 정책 참조. 자유 문자열이 아니다. **TLS1.2 이하와 TLS1.3 산출물을
분리한다**"* 고 적어 둔 것을 구현했다. 분리가 왜 필요한지는 재 보면 보인다:

```
ssl_ciphers ECDHE-RSA-AES128-GCM-SHA256
  → TLS1.2 는 그것,  TLS1.3 은 TLS_AES_256_GCM_SHA384   (전혀 다른 것)
```

**`ssl_ciphers` 는 TLS1.3 에 안 걸린다.** 1.3 은 `ssl_conf_command Ciphersuites` 가 정하고,
둘 다 server 별이다.

#### 계측기를 또 의심해야 했다

처음 잰 결과는 TLS1.3 칸이 **비어 있었다.** "1.3 이 아예 안 붙는다" 로 읽힐 뻔했는데,
원인은 파서였다 — TLS1.3 은 들여쓴 `Cipher  :` 블록을 안 내고 `New, TLSv1.3, Cipher is …`
로만 찍는다. 이 세션에서 계측기가 답을 바꾼 **네 번째**다.

#### 그리고 변별력 0 인 테스트를 **두 번** 썼다

골든이 3/3 초록이었는데, 변이로 `ssl_ciphers` 와 `ssl_conf_command` 를 **둘 다 통째로
빼도 그대로 통과했다.**

- `/^ECDHE-/` — nginx 기본값(`HIGH:!aNULL:!MD5`)도 ECDHE 를 고른다
- `/^TLS_/` — 모든 1.3 suite 가 그렇게 시작한다

**"우리가 정한 것"과 "엔진 기본값"이 구분되는 자리를 찾아야 했다.** 찾은 것은 *우리가
뺀 것을 요구했을 때*다:

| `-cipher AES128-SHA` (비-PFS·CBC) | 결과 |
|---|---|
| 우리 정책 | **거절** |
| nginx 기본값 | AES128-SHA 로 협상됨 |

그걸로 바꾸자 `ssl_ciphers` 제거 변이가 2 건을 깬다.

#### 안 잡히는 것 하나는 안 잡힌다고 적었다

`ssl_conf_command` 제거는 **여전히 안 잡힌다.** `modern-2026` 의 1.3 목록이 OpenSSL
기본값과 같아서 협상 결과가 안 바뀌기 때문이다. 그래도 내는 이유는 **엔진 기본값이
바뀌어도 우리 설정이 안 바뀌게** 하기 위해서고, 그 사실과 한계를 테스트 주석에 적었다 —
S19.same_digest 를 걷어냈던 것과 다른 판단이다. 거기서는 그 축을 잴 경로가 아예 없었고,
여기서는 **이유가 미래에 있다.**

---

### SNI↔Host 불일치 — HTTP/2 를 켠 것이 이걸 앞당겼다 (2026-08-18)

§4.6 이 *"둘이 다를 수 있다는 사실 자체를 모델이 인정해야 한다"* 고 적어 뒀다. 재 보니
왜인지 보인다 — 테넌트 둘을 세우고 엇갈리게 보내면:

```
SNI=a.test + Host=b.test  →  인증서 a.test  /  응답 TENANT-B
```

handshake 는 SNI 로, 요청은 Host 로 server 를 고른다.

**그 자체로 권한 상승은 아니다** — 클라이언트가 처음부터 SNI=b 로 붙을 수 있었다.
위험은 운영자가 *"a 의 인증서를 받았으면 a 의 트래픽"* 이라고 가정할 때 생기고,
**HTTP/2 가 그 가정을 깬다**: 브라우저는 인증서가 덮는 다른 오리진에 같은 커넥션을
재사용한다(RFC 7540 §9.1.1). **지난 회차에 h2 를 켠 것이 이 항목을 앞당겼다.**

#### 기본은 막지 않는 것이다

`reject_421` 은 **명시적으로 켜야** 한다. 막는 쪽을 기본으로 하면 SNI 를 안 보내는 옛
클라이언트가 끊긴다 — 그리고 이 회차에서 제일 신경 쓴 것이 *"정당한 트래픽을 안
끊는가"* 다. 잘못 켜면 서비스가 죽는 자리라, 경계 셋을 실측해서 못 박았다:

| 입력 | 결과 | 왜 |
|---|---|---|
| `Host: a.test:443` | 통과 | `$host` 가 **포트를 뗀다** |
| `Host: A.TEST` | 통과 | `$host` 가 **소문자로 내린다** |
| SNI 없음 | 통과 | 비교할 것이 없다 |

렌더는 `map` + `if` 다. nginx 의 `if` 는 변수 하나만 보므로 두 값을 `:` 로 이어 붙여
**정규식 역참조**로 같은지 본다.

⚠️ `if` 는 **rewrite 단계**라 location 선택보다 앞이다 — ACME 예약 라우트 때 한 번
물린 그 자리다. 가드가 걸리면 그 server 의 어떤 location 도 안 돈다. 그래도 문제가 안
되는 이유는 http-01 검증이 평문 80 으로 오고 이 가드는 **불일치일 때만** 걸리기 때문이다.

변이 셋이 각자 겨눈 단언을 깬다: 가드 미출력 / SNI 없음도 막기 / 역참조 제거(일치도 막힘).

---

### SecretStore 청소 — 내가 만든 부채를 갚는다 (2026-08-18)

자동 갱신을 붙이면서 **저절로 자라는 것**이 하나 생겼다. 갱신은 새 버전을 만들 뿐 옛
것을 안 덮으므로(§8.3) 인증서 하나가 90 일마다 버전 하나씩 쌓는다. **그리고 그 버전들은
개인키를 담고 있다.**

세대는 이미 상한이 있는데(`retention.ts`) 시크릿은 없었다. 자동 갱신 전에는 사람이 올릴
때만 늘었으니 안 보였다 — **자동화가 부채를 스스로 채우기 시작한 것**이고, 그건 내가
지난 회차에 만든 것이다.

#### 답이 정해져 있지 않던 질문

`config_revisions` 는 **영원히 남고**(불변 스냅샷) 롤백은 아무 리비전이나 겨눌 수 있다.
그 둘을 그대로 두면 **어떤 자료도 지울 수 없다.** §8.4 가 GC root 표에 *"롤백 보존 기간
안의 세대"* 라고 적어 둔 것이 이 뜻이고, 기간을 정해야 지울 수 있다.

정했다: **최근 20 리비전이 참조하는 자료는 남긴다.** 그 밖으로의 롤백은 자료가 없어
**실패한다** — 조용히 최신 버전으로 물러나지 않는다(§8.3 의 `@latest` 금지). 롤백이
거짓말을 하느니 실패하는 편이 낫고, 실패는 apply 전 `prepare` 에서 난다.

#### 비대칭을 설계에 넣었다

남기는 쪽으로 틀리면 디스크가 조금 더 쓰인다. **지우는 쪽으로 틀리면 서비스가 죽는다** —
S8 실측: 활성 인증서를 지워도 열린 fd 로 트래픽은 계속 흐르고 **다음 reload 가 깨진다.**
그래서 장치를 셋 뒀다: root 는 **호출자가 모아서 주고**(모듈이 추측하지 않는다), 이름당
최소 2 개는 root 가 아니어도 남기고(**root 계산이 틀렸을 때의 안전망**), 갓 만든 것은
안 지운다(주문이 아직 참조를 DB 에 안 적었을 수 있다).

root 는 넷이다: 최근 N 리비전의 자료 · 디스크의 세대가 든 자료 · ACME 계정 키 · 진행
중이거나 최근에 끝난 주문의 자료·키.

#### 변이 하나가 `removed` 뒤에 숨어 있었다

`keys/` 를 인증서 이름으로 잘못 훑는 변이를 넣었는데 **8/8 통과**했다. 추론으로는 잡혀야
했다. 관찰해 보니 그 변이는 `removed` 를 안 바꾸고 **`failed` 로만** 나타났다 —
`store://keys@web` 삭제가 하위 0500 디렉토리 때문에 `ENOTEMPTY` 로 실패한 것이다.

`failed` 를 단언하지 않고 있었다. **"지우려다 실패한 것" 은 지워야 할 걸 못 지운 것이거나
지우면 안 될 걸 건드린 것**이고, 둘 다 봐야 한다. 단언을 넣자 잡혔다.

그리고 이번에도 **변이가 적용됐는지부터 확인**해야 했다 — 첫 시도에서 `python` 치환에
assert 를 안 걸어서, 통과가 "변별력 없음" 인지 "치환 실패" 인지 몰랐다.

---

### HTTP/2 — 계획에 없던 것이 아니라, 있는데 안 하고 있었다 (2026-08-18)

*"http/1, 1.1, 2, 3 다 되느냐"* 는 물음에 코드를 봤더니 **HTTP/1.x 만** 됐다. 그리고
그건 계획의 부재가 아니라 **구현의 누락**이었다 — §4.1 이 `http2` 를 HttpsProfile 필드로
적어 뒀고, §7.6 이 버전 핀까지 못 박아 뒀고, capability probe 가 `http_v2_module` + 1.25.1
을 **탐지까지 하고 있었다.** 아무도 그 플래그를 안 읽었을 뿐이다.

이 저장소가 반복해서 잡아온 *"필드는 있는데 아무도 안 읽는다"* 의 또 한 사례이고, 이번엔
**capability 쪽**에서 났다.

#### 실측이 문서를 뒤집었다

§4.1 은 `http2` 를 https 전용으로 두면서 이유를 *"타입상 http 에서 표현 불가"* 라고 적어
뒀다. **거짓이다.** 평문에 `http2 on;` 을 내면 h2c 가 실제로 동작한다:

```
curl --http2-prior-knowledge http://…  → HTTP/2
curl (평범하게)                        → HTTP/1.1
```

그래도 https 전용으로 **유지**하되 이유를 바꿔 적었다: 브라우저는 h2c 를 안 쓰므로,
평문에 열어 두면 켜 놓고 아무 일도 안 일어난다. 배제는 그대로지만 **근거가 참이 됐다.**

#### 렌더 규칙도 실측에서 나왔다

`http2` 는 **server 별로 걸린다** — 같은 리스너에서 `a.test`(on) 는 `h2` 를,
`b.test`(off) 는 `http/1.1` 을 협상한다. `ssl_protocols`(S16)와 같은 성질이라 같은 자리에
낸다. http 블록에 한 번만 내는 길도 되지만 안 골랐다: server 별 재정의가 http 레벨을
덮는지 **또 재야 하고**, S16 이 정확히 그 자리에서 한 번 물었다.

#### 판정은 ALPN 으로 한다

conf 에 `http2 on;` 이 있다는 것과 클라이언트가 h2 를 받는다는 것은 다르다. nginx
1.25.1 부터 기본이 **off** 라, 이 줄을 안 내면 **클라이언트가 h2 를 제안해도 http/1.1 로
떨어지고** 그건 트래픽을 봐야만 보인다. 골든이 실제 엔진에 붙어 ALPN 을 읽는다.

기본은 **켜는 것**이다 — 요즘 HTTPS 에서 h2 없이 서비스하는 것은 사실상 결함이다. 다만
**명시적으로 켜 달라고 했는데 엔진이 못 하면 검증기가 막는다.** 기본값이 못 걸리는 것은
에러가 아니다(그러면 h2 를 못 내는 엔진에서 모든 https 리스너가 저장 불가가 된다) —
그건 capability 로 관측된다.

변이 셋이 각자 잡힌다: 안 내기 / 기본을 끄는 쪽으로 / capability 검사 끄기.

#### HTTP/3 는 계획에 넣고 게이트를 세웠다 (S20)

`listen … quic reuseport` 는 `nginx -t` 를 통과하고 **UDP 소켓도 실제로 열린다**(TCP 와
같은 포트에 공존). `Alt-Svc` 도 나간다. **그런데 h3 로 실제 붙는 것은 못 쟀다** — 엔진
이미지의 curl 이 HTTP/3 를 모른다. *"설정이 선다"* 와 *"서빙된다"* 는 다르고, **h3
클라이언트를 구하는 것이 S20 의 첫 과제**다.

설계에 미치는 영향 셋을 §4.9 에 적었다. 특히 **QUIC 는 UDP/443 을 점유하므로 §4.5 겹침
검증기가 그걸 udp 예약으로 세야 한다** — 지금은 https 를 tcp 로만 예약해서 같은 포트의
udp 리스너와의 충돌을 못 본다. v0.7 이다.

---

### 발급이 트래픽에 닿는다 — 그리고 게이트 둘이 간헐이었다 (2026-08-18)

러너는 인증서를 받아 `issued` 로 끝낸다. **거기까지는 nginx 가 아무것도 모른다** —
주문 상태는 리비전에 안 살기 때문이다(009). 마지막 고리를 놓았다.

**게시는 정상 경로로 간다** (changeset → plan → commit → apply). 그래야 인증서 교체가
다른 설정 변경과 같은 방식으로 검증·기록·롤백되고, plan 의 impact 가 미리 말한다.

**"게시했다" 상태를 안 만든다.** 비교가 곧 판정이다 — 인증서의 `materialRef` 가 주문의
`issuedRef` 와 다르면 아직 안 간 것이다. 상태를 하나 더 두면 그 상태와 실제가 어긋나는
경우가 생기고, 이 저장소는 그걸 여러 번 겪었다.

⚠️ **대가를 적어 둔다.** 인증서 경로에 버전이 들어가므로(§7.2) 교체는 렌더 산출물을 바꾸고
따라서 **세대 전환과 reload** 를 부른다 — 전환당 트래픽 **2.6%**(소크 실측). 챌린지
서빙에는 reload 가 없지만 **교체 자체는 없앨 수 없다.** 갱신 주기가 그 비용의 빈도다.

#### 이름이 약속한 것을 안 재던 테스트

`하나가 실패해도 다음 것을 계속한다` 를 **항목 하나**로 재고 있었다. 하나면 `break` 와
`continue` 가 구분되지 않는다 — 변이로 `break` 를 넣어도 통과했다. 인증서 둘(하나는 자료가
사라진 것)로 고치자 잡혔다.

#### 게이트 둘이 간헐이었다 — 둘 다 내 하네스

**S18 `nonce_norety`.** 재시도를 끈 클라이언트로 `register()` 를 try 밖에서 불렀다.
거부율 20% 에서 **등록 자체가 20% 확률로 죽는다.** 이 시나리오가 재는 것은 *주문*이지
등록이 아니다 — 등록만 재시도하게 고쳤다. 3/3 안정.

**S12 sweep.** 단독으로는 3/3 통과인데 게이트 안에서만 빨개졌다. 이유를 남기게 고치고
다시 돌리자 나왔다: `phase=failed` 인데 **링크도 서빙 세대도 gen-2** — 세상은 수렴했고
러너가 활성화 증거를 예산(25×100ms) 안에 못 본 것이다. S7 이 다룬 판정 지연이고, 부하를
나눌 때 드러난다.

**그런데 §12.0 의 합격 기준은 "최종 세대가 정확하고 중복 cycle 이 상한 이내" 이고
exactly-once 를 요구하지 않는다.** 첫 복구가 `activated` 여야 한다고 단언한 것은 **기준보다
엄격했다.** 상한(3회) 안에서 다시 돌게 고쳤고, 부하 없을 때는 지점당 정확히 1.00 cycle 이다.
느슨하게 푼 것이 아님을 확인하려고 락 회수 변이를 다시 넣었더니 여전히 3 건이 빨개진다.

---

### ACME 갱신 러너 — 원장과 프로토콜을 잇는다 (2026-08-18)

S18 이 프로토콜을, 앞 회차들이 챌린지 서빙과 원장을 세웠다. 러너는 그 사이를 잇고
**한 틱에 한 걸음씩** 옮긴다 — 한 호출로 끝까지 몰면 실행권을 든 채 몇 분을 붙잡고, 그동안
죽으면 어디까지 갔는지 아무도 모른다. `ApplyRunner` 가 저널로 하는 일과 같다.

#### 모델이 답 못 하던 질문 하나

**아직 발급 못 받은 인증서를 어떻게 선언하는가.** `Certificate.materialRef` 가 필수라
"인증서를 받으려면 인증서가 있어야 한다" 였다. 자료를 선택으로 바꾸고 `acme` 의도를
더했다. 대신 **자료 없는 인증서는 바인딩할 수 없다**(검증기) — 렌더러가 낼 것이 없다.
그래서 첫 발급은 두 단계다: ① 의도만 커밋 → 발급 ② 발급된 참조로 바인딩.

자체서명 임시 인증서를 끼우는 길도 있었지만 안 골랐다. **잘못된 인증서가 잠깐 실제로
제시되고**, 그 잠깐이 얼마인지는 발급 성공 여부에 달려 있다 — 실패하면 영원이다.

#### 키를 어디에 두는가

CSR 에 서명한 키와 발급된 인증서는 **짝**이어야 하는데, 키는 CSR 때 필요하고 인증서는 그
뒤에 온다. 그 사이에 죽으면 발급된 인증서가 짝을 잃고 **새 주문을 내야 한다** — CA 의
레이트리밋이 그걸 센다.

그런데 `SecretStore.put` 은 한 쌍을 요구한다(§7.2). 자리표 인증서를 지어내 짝을 맞추려다
멈췄다 — **어디에도 제시되지 않을 인증서를 만들어 두면 언젠가 제시된다.** 대신 스킴을
갈랐다: `key://` 는 인증서 없는 키다. `parseRef` 가 `store://` 만 받으므로 **섞이는 것이
표현 불가능**하고, DB CHECK 도 스킴으로 갈라 둔다.

#### 테스트가 결함 둘을 잡았다

- **계정을 키가 아니라 디렉토리 URL 로 찾고 있었다.** 증상은 "주문이 안 열린다" 였다.
  설정이 가리키는 것은 계정 이름이지 URL 이 아니다 — URL 을 적게 하면 CA 를 바꿀 때 모든
  인증서를 고쳐야 한다.
- **claim 시점의 스냅샷을 믿고 있었다.** 방금 저장한 키 참조가 `order` 에 없어서
  `markIssued` 가 빈 문자열을 썼고, **DB CHECK 가 잡았다.**

CA 는 가짜로 둔다. 여기서 묻는 것은 "CA 가 규격대로 답하는가" 가 아니라 **"CA 가 이렇게
답하면 우리가 뭘 하는가"** 이고, 실물로는 그 분기를 부를 수가 없다 — Pebble 에게 "이번엔
invalid 를 답해라" 를 시킬 방법이 없다. 원장은 실물 PG 다.

변이 셋이 각자 잡힌다: 모르는 사실을 "갱신 필요" 로 읽기 / 발급 뒤 챌린지 미정리 /
정리 중 하나가 실패하면 멈추기.

#### 게이트가 한 번 반쪽이었다 — 그리고 채웠다

커밋 시점에는 **도커 이미지가 사라지고 레지스트리 DNS 가 안 돼서** 실물 PG·e2e·스파이크를
못 돌렸다. 그대로 "검증됐다" 고 적지 않고 커밋 메시지와 여기에 반쪽이라고 적었다.

**환경이 돌아온 뒤 다시 돌렸고 전부 초록이다** (exit 0): 단위 288 · conformance 391 ·
저장소 **98** · 골든 23 · e2e 60 · 엔진 사실 73 · 스파이크 아홉.

---

### ACME 주문 상태기계 — CA 가 안 해 주는 것을 우리가 든다 (2026-08-18)

S18 이 프로토콜을, 앞 회차가 챌린지 서빙을 세웠다. 남은 것은 **언제 무엇을 다시 시도할
것인가** 이고, S18 이 실측한 대로 그건 CA 가 안 해 준다 — **버려진 주문을 CA 는 안
치운다**(`pending` 으로 남는다).

#### 첫 결정: 이건 설정이 아니다

주문·챌린지를 `config_revisions` 에 안 넣는다. `backend_health` 와 같은 부류다.

- 리비전은 **불변**인데 주문은 상태가 계속 바뀐다
- 리비전에 들어가면 갱신 한 번마다 새 리비전이 생기고, **롤백이 "그때 진행 중이던 주문" 을
  되살린다** — 되돌릴 대상이 아니다
- 설정 diff 에 "주문이 processing 이 됐다" 가 섞이면 plan 의 impact 가 거짓말이 된다

설정에 남는 것은 `certificates.material_ref` 뿐이다. **발급과 게시는 다른 사건**이고,
주문이 성공하면 그 참조를 바꾸는 changeset 을 만든다.

#### DB 가 지는 것

| 규칙 | 어떻게 |
|---|---|
| 인증서당 살아 있는 주문은 하나 | **부분** 유일 인덱스. 전체 유일이면 재시도가 불가능해진다 — 끝난 주문은 기록으로 남아야 한다 |
| 결과 없이 `issued` 가 될 수 없다 | CHECK. "성공했는데 결과가 없다" 를 표현 못 하게 |
| 종단 상태는 실행권을 못 든다 | CHECK. 들고 있으면 아무도 못 집는다 |
| 놓지도 않은 것을 치웠다고 못 적는다 | CHECK (`cleaned_at` 은 `placed_at` 뒤에만) |
| 개인키가 안 들어온다 | 컬럼 자체가 없다. 계정 키도 SecretStore 참조다 (§4.8 — 예외를 하나 두면 그게 규칙이 된다) |

#### 실행권·백오프·포기

리더가 둘일 수 있는 순간(§3.5 승계)에 같은 주문을 둘이 몰면 **nonce 가 서로를 깨뜨리고**
챌린지를 두 번 수락한다. `FOR UPDATE SKIP LOCKED` 로 집는다 — 기다리게 하면 스케줄러 틱이
서로를 막는다.

lease 는 **시간 기반**이다. 죽은 리더를 영원히 기다릴 수 없기 때문이고, 대가("죽은 줄
알았는데 살아 있는" 창)는 CA 쪽 멱등성이 흡수한다.

백오프는 지수이되 **상한이 있다**(60s → 1h). 무한히 늘리면 사람이 고친 뒤에도 몇 시간을
기다리게 되고 그건 "포기" 와 구분되지 않는다 — 포기는 `abandoned` 라는 이름이 따로 있다.
8 회에서 멈추고 **사람이 봐야 한다.** 계속 재시도하면서 아무 말도 안 하는 것이 더 나쁘다.

#### 고아는 "주문이 끝났나" 로 물으면 영영 안 걸린다

S18 이 실측한 것이 여기서 설계를 정했다. CA 가 `pending` 으로 남기므로 주문 상태로 물으면
안 걸린다. **`placed_at` 을 기록해서 "놓을 예정" 과 "놓았다" 를 구분하고**, 끝난 주문의
자료는 즉시·진행 중인 주문의 자료는 오래됐을 때 고아로 본다.

변이 다섯이 각자 잡힌다: 백오프 제거 / 유일 인덱스 제거 / 실행권 미확인 / 시간 기반 고아
판정 제거 / `markIssued` 가 실행권을 안 놓음. 네 번째는 처음에 쿼리를 깨뜨려 4 건이
빨개졌는데, **그건 변별력이 아니라 고장**이라 문법을 지킨 채 의미만 바꿔 다시 쟀다 —
그러자 정확히 한 건이었다.

**아직 스케줄러가 없다.** 갱신 시점 정책·레이트리밋 헤더 해석·EAB·dns-01 프로바이더는
다음 회차다.

---

### ACME 챌린지 — reload 없이 서빙된다. 그리고 예약 라우트가 도달 불가였다 (2026-08-18)

S18 은 **프로토콜**이 도는지를 쟀다. 제품이 되려면 챌린지를 **nginx 가** 서빙해야 하고,
그 방식이 갱신 비용을 정한다.

**토큰을 conf 에 실으면 갱신 한 번에 세대 전환이 한 번이다.** 대가는 실측돼 있다 —
전환당 트래픽 **2.6%** 손실. 인증서가 여럿이고 90 일 주기면 곱해진다. 그래서 shared
dict 에 넣고 `location ^~ /.well-known/acme-challenge/` 로 낸다. 멤버십 평면이 백엔드에
대해 푼 문제와 같은 문제, 같은 수법(S1).

골든이 **안 일어난 일**을 잰다: 토큰을 적재해도 **마스터 PID 와 워커 기동 수가 안 변한다.**
"서빙된다" 만으로는 부족하다 — HUP 을 보내도 그렇게 되기 때문이다.

#### 렌더는 옳았는데 도달할 수가 없었다

예약 라우트를 넣고 돌렸더니 **모든 요청이 `000`** 이었다. conf 를 봐도 멀쩡하고
`nginx -t` 도 통과한다.

원인은 default server 의 거절을 **server 레벨 `return 444`** 로 내고 있던 것이었다.
nginx 의 server 레벨 `return` 은 **rewrite 단계**에서 실행되고, 그건 **location 선택보다
앞이다.** 같은 server 안에 다른 location 이 아무리 있어도 도달하지 못한다.

`location / { return 444; }` 로 옮겨 고쳤다 — 의미는 같고("안 맞으면 끊는다"), 더 긴
접두사가 이제 이길 수 있다. **v0.1 부터 있던 렌더 형태였고, 지금까지 아무도 그 뒤에
뭘 놓으려 한 적이 없어서 안 드러났다.**

#### 가로채기

`^~` 는 정규식을 막지만 **더 긴 접두사는 못 막는다** — location 은 최장 접두사가 이긴다.
사용자가 `/.well-known/acme-challenge/foo` 를 내면 CA 의 요청이 거기로 가고, 증상은
"챌린지 검증 실패" 다. 인증서 설정을 아무리 봐도 이상이 없다. 검증기가 막는다
(`acme_path_reserved`).

변이 셋이 각자 잡힌다: 예약 라우트 미출력 / server 레벨 return 으로 되돌리기(도달 불가
재현) / 가드 끄기.

**ADR-ACME 를 §8.2 에 적었다.** S18 과 이 회차가 잰 것만 확정하고, 주문 durable 스키마·
갱신 스케줄러·레이트리밋 백오프·EAB·dns-01 프로바이더는 **미정으로 남겼다** — 지금
정하면 안 짜 본 것을 규범으로 굳힌다.

---

### S18 — ACME 가 실물 CA 로 돈다. 의존성은 그대로 하나 (2026-08-17)

§8.2 는 스스로를 **비규범**이라고 적어 뒀다: *"엔티티·상태·정책은 ADR-ACME 가 확정하기
전까지 구속력이 없다."* S18 은 그 ADR 이 딛고 설 사실을 만드는 게이트고, "v0.6 전에
실행한다" 고 못 박혀 있었다.

**Pebble 로 잰다.** Let's Encrypt staging 은 네트워크·레이트리밋·실제 DNS 가 필요하고,
무엇보다 **badNonce 를 일부러 낼 수 없다.** Pebble 은 못되게 구는 것이 기능이라
`PEBBLE_WFE_NONCEREJECT` 로 nonce 를 정해진 비율로 거절한다 — RFC 8555 §6.5 의 재시도
경로를 실제로 밟게 하는 유일한 방법이다.

#### 의존성 0 으로 짓는 값

ACME 는 finalize 에 **CSR** 을 요구하는데 Node 에는 CSR 을 만드는 API 가 없다
(`X509Certificate` 는 읽기 전용). 셋 중 하나를 골라야 했다.

| 방법 | 대가 |
|---|---|
| npm 패키지 | §11.2 의 "런타임 의존성은 `pg` 뿐" 을 깬다 |
| `openssl` 셸아웃 | 엔진과 달리 **CP 호스트**에 바이너리 의존이 생긴다 — 컨테이너 이미지 계약이 바뀐다 |
| 직접 인코딩 | ASN.1 을 조금 알아야 한다. **다만 필요한 범위가 아주 좁다** |

마지막을 골랐다. CSR 하나에 필요한 것은 SEQUENCE·INTEGER·BIT STRING·OID·SAN 확장뿐이고
키 타입도 우리가 정한다(P-256). 넓은 ASN.1 라이브러리가 아니라 **이 한 장을 만드는 코드**
다. 판정은 `openssl req -verify` 에게 맡긴다 — 내가 만든 것을 내가 파싱해서 확인하면
같은 오해를 두 번 하는 것이다.

#### 스파이크가 진짜 결함을 하나 찾았다

`nonceRetries` 를 RFC 문구 그대로 **1** 로 뒀더니 `happy` 가 finalize 에서 `badNonce` 로
죽었다. RFC 8555 §6.5 의 *"한 번은 반드시 재시도"* 는 **하한이지 운영값이 아니다** —
거부율 20% 에서 한 요청이 두 번 연속 거절될 확률이 4% 인데 발급 한 바퀴에 요청이 15 개
남짓이라, 어딘가에서 걸릴 확률이 절반에 가깝다. 기본값을 5 로 올렸다.

#### ADR-ACME 가 알아야 할 사실 넷

- **와일드카드 authz 에는 http-01 이 아예 없다** (실측: `dns-01, dns-account-01,
  dns-persist-01`). §8.2 의 "와일드카드는 dns-01 만" 이 CA 쪽에서도 그렇다.
- **버려진 주문은 `pending` 으로 남는다.** CA 가 치워 주지 않는다 → 고아 정리는 **우리
  상태로 몰아야** 한다. §8.2 의 "cleanup 보장 + 주기적 고아 스캔" 이 선택이 아닌 이유다.
- 토큰을 안 서빙하면 주문이 **유한 시간에 `invalid`** 로 확정된다. 재는 것은 "실패한다"
  가 아니라 **"실패가 확정된다"** 다 — 영영 `pending` 이면 상태기계가 멈춘다.
- 발급된 인증서가 **우리 CSR 의 키와 맞는지**까지 봐야 한다. 200 을 받은 것과 쓸 수 있는
  인증서를 받은 것은 다르다.

#### 변이

| 변이 | 어디서 잡히나 |
|---|---|
| ES256 을 DER 로 서명 (Node 기본값) | 전 시나리오가 `malformed: error in cryptographic primitive` |
| thumbprint 필드 순서를 RFC 7638 과 다르게 | `happy` 만 `invalid` — 챌린지 검증이 **항상** 실패하는데 CA 는 왜인지 안 말해 준다 |
| SAN dNSName 을 constructed 태그로 | `openssl` 이 SAN 을 아예 못 읽는다 |

**아직 제품이 아니다.** 이건 프로토콜 클라이언트이고, 주문·챌린지의 durable 상태와
갱신 스케줄러는 ADR-ACME 가 정한 뒤에 붙는다. 지금 섞으면 ADR 이 정해지기 전에 스키마가
굳는다.

---

### S12 — 마지막 block 등급 게이트를 열었다 (2026-08-17)

block 등급은 셋이었다(S8·S11·S12). **떨어지면 설계를 다시 해야 하는 것들**이고, S12 만
스파이크가 없었다 — 이 문서에 판정 기록도 없었다.

`review5-crash-points.test.ts` 가 이미 지점 15 개 × 직전/직후를 훑는다. 그런데 **거기서
죽는 방식은 예외**다: 힙만 버려지고 파일시스템은 정상 종료한 상태로 남는다. S12 가 묻는
것은 *"진짜로 죽으면"* 이라, `process.abort()` 로 실물 `FileStore`·실물 `FsEffects`·실물
nginx 위에서 38 개 지점을 훑었다.

**38/38 에서 복구가 같은 세대로 수렴한다.** 링크와 서빙 세대가 일치하고, 죽은 주인의 락이
회수되며(6차 반례 ⑤), 복구가 쓰는 reload 는 지점당 0.68 회로 유계다. 49 초.

#### 계측기가 세 번 거짓말했다

이번에도 틀린 결론의 출처는 전부 하네스였다.

1. **manifest digest 를 셸에서 손으로 계산했다.** preflight 가 전부 거절해서 baseline 이
   `failed` 로 나왔다 — 다행이다. 계산이 우연히 맞았다면 **아무 일도 안 일어난 채**
   "전 지점에서 복구가 잘 된다" 는 초록이 나왔을 것이다. 실물 `materializeGeneration`
   으로 바꿨다.
2. **reload 를 세는 grep 이 형식과 안 맞았다.** 38 회 복구에 0 회. 그건 "유계" 가 아니라
   아무것도 안 센 것이다. 그래서 `S12.metric` 을 넣어 **지표부터 검증**한다 — S16 이
   같은 이유로 `S16.instrument` 를 들고 있다.
3. **폴 정책을 프로덕션보다 17 배 빡빡하게 줬다** (3×50ms vs 25×100ms). 없는 실패가
   생겼고, **회차마다 다른 지점**을 지목했다(#2·#15·#24 → #10). 그 비결정성이 단서였다 —
   고정된 로직 결함이면 같은 지점이 나온다.

그리고 nginx 는 스윕 내내 살아 있는 별개 프로세스다. 한 번 gen-2 로 reload 되면 prefix 를
되돌려도 계속 gen-2 를 서빙하므로, 뒤쪽 회차의 "수렴했다" 가 **앞 회차 덕**이 된다.
회차마다 엔진을 되돌린다.

#### 변이 하나가 처음에 안 잡혔다

죽은 주인의 락 회수를 끄면 `S12.sweep`·`S12.lock` 이 함께 빨개진다. 그런데
`finishOperation` 이 실행권을 영영 안 놓게 만든 변이 — 6차 반례 ④ 가 *"이게 빠지면 좌표가
영구히 잠긴다"* 고 적어 둔 바로 그것 — 는 **5 PASS 로 그냥 통과했다.**

복구가 *같은* 오퍼레이션만 다시 돌리기 때문이다. 같은 신원은 자기 실행권에 안 막힌다.
**잠김은 다음 오퍼레이션에서만 보인다.** 회차마다 gen-3 를 한 번 더 밀게 고치자 잡혔다.

교훈이 새롭지는 않다. *통과하는 스파이크는 무엇을 잡는지 확인하기 전까지 아무 말도 하지
않는다.*

---

### v0.6 1단계 — TLS 종단이 섰다. 그리고 갱신이 조용히 무효였다 (2026-08-17)

`https` 는 한동안 **일부러 없는 프로토콜**이었다. §4.6 이 그 자리에 적어 뒀다:
*"렌더러가 TLS 종단을 내지 못하는데 타입으로 제공하면, v3 처럼 `protocol: 'https'` 가
평문 `listen 443;` 으로 렌더된다."* 되살리는 조건은 S16·S17 통과였고, 둘 다 열었다.

**S17(인증서 선택, 10 PASS)이 렌더 규칙 둘을 내려 줬다.**

| 실측 | 규칙 |
|---|---|
| `server_name *.wild.test` 가 `deep.x.wild.test` 를 삼켜 와일드카드 인증서를 제시한다 (E22.2) | 와일드카드는 **`~^[^.]+\.suffix$` 앵커 정규식**으로 낸다. 나이브한 형태가 곧 **SAN 미커버 제시** — 합격 기준이 겨눈 그 실패다 |
| `default_server` 가 없으면 모르는 SNI 가 **첫 블록의 인증서**를 받는다 (E32 의 TLS 판) | TLS 리스너마다 반드시 낸다. 멀티테넌트에서 이건 테넌트 간 누수다 |

그리고 `server_name ~*` 는 `nginx -t` 가 거절한다 — `~*` 는 map 전용이고(E21), 패스스루
SNI map 의 문법을 그대로 옮겨 쓰면 죽는다. 대소문자는 nginx 가 SNI 를 내려서 비교한다.

**S16(SNI 별 policy, 5 PASS)은 `override` 를 살렸다.** 비-default server 의
`ssl_protocols` 가 실제 handshake 에 걸리고, default 를 엄격하게 뒤집어도 비-default 가
이긴다. 실패했다면 §12.0 규칙에 따라 그 필드를 **없앴을** 것이다.

#### 계측기가 한 번 답을 뒤집었다

S16 을 처음 잰 지표가 `s_client ... | sed 's/Protocol *: *//'` 였다. 그 줄은 **s_client 가
자기 설정을 찍는 것**이라 서버가 alert 70 으로 끊어도 그대로 `TLSv1.3` 이 나온다.
그 지표로는 **모든 조합이 통과로 보였고**, "http 레벨 `ssl_protocols` 조차 안 먹는다" 는
있을 수 없는 결론이 나왔다 — 그 말이 안 되는 것이 계측기를 의심한 **유일한 단서**였다.
handshake 성립 여부로 바꾸자 답이 뒤집혔다. 그래서 S16 프로브는 자기 계측기를 먼저
검증한다(`S16.instrument`).

#### 재면서 찾은 것 셋

**① 인증서를 갱신해도 렌더 산출물이 글자 하나 안 바뀌었다.** 경로가 `certs/<key>/` 뿐이라
digest 가 같고, apply 는 "산출물이 안 바뀌었다" 를 멤버십 전용 전환의 근거로 쓴다(§6.5).
그래서 **갱신이 세대를 안 만들고 조용히 성공했다** — 옛 인증서가 계속 제시되는데 응답은
`activated` 다. 참조는 §4.8 대로 버전 고정이었는데 **그 버전이 렌더까지 안 내려와서**
아무 효과가 없었다. 이 저장소가 반복해서 밟은 *"필드는 있는데 아무도 안 읽는다"* 의 한
판이고, **e2e 가 잡았다** — 골든도 단위도 못 잡는다. 경로에 버전을 넣어 고쳤다.

**② `modelAt` 이 스냅샷을 캐스팅했다.** `r['model'] as Model` 은 *지금의* 모양을 옛
스냅샷이 갖고 있다고 가정한다. 컬렉션 셋을 더하자 v0.6 이전 리비전으로 롤백하면
`undefined.map` 으로 500 이 났다. 캐스팅은 그 순간까지 아무 말도 안 한다 — 해독을
거치게 했다.

**③ `http_route_listener_is_http` 가 https 리스너를 막았다.** 001 의 CHECK 가 그때의
세상("http 라우트는 http 리스너에만")을 굳히고 있었다. TLS 를 종단하고 나면 평범한
HTTP 다. 넓히되 버리지는 않는다.

#### 무엇이 증거인가

골든(실제 엔진, 4건)이 SNI 별 인증서·다중 라벨 차단·default 폴백·override 를 재고,
**변이 셋을 전부 잡는다**: 바인딩 무시 / 나이브 `server_name` / `default_server` 제거 —
마지막은 모르는 SNI 가 옆 테넌트 인증서를 받는 것을 그대로 재현했다.

e2e(전체 스택, 7건)는 REST 로 끝까지 몬다. **판정은 "롤백이 성공했는가" 가 아니라
"롤백 뒤에 제시되는 인증서가 옛 것인가"** 다 — 이름만으로 참조했다면 여기서 여전히 새
인증서가 나오고, 그러면서 200 도 잘 나온다. 개인키가 안 나가는 것은 응답 **전체 문자열**로
검사한다(§8.1); 필드만 보면 다른 필드로 새는 것을 못 잡는다.

#### 대가와 남은 것

**표면 동결 카운터 2 → 0.** 타입 여섯(`Certificate`·`TlsPolicy`·`SniCertificateBinding`·
`HttpsListener`·`RawTlsBinding`·`TlsVersion`)을 더했으므로 기준을 옮겼다. 13차가 준
"여러 적대적 회차 동안 표면 무변동" 기준은 처음부터 다시 센다 — **A 게이트가 그만큼
멀어졌다.** 기능을 넣은 값이다.

**안 한 것을 적어 둔다.** ACME(S18) · HSTS · OCSP stapling · `CipherPolicyRef`(암호군
정책) · SNI 와 Host 불일치 정책(`reject_421`) · 인증서 만료 감시 · SecretStore GC ·
CLI/GUI 의 인증서 표면. §4.6 이 이름을 준 것들 중 지금 도는 것은 `min_version`·
`max_version`·`default_certificate` 셋뿐이다.

**그리고 개인키는 평문이다.** `FsSecretStore` 도 세대 안(`certs/<key>/<version>/`)도
평문이고, 보호는 파일 권한(0400)과 "메인 DB 가 아니다" 뿐이다. **암호화가 아니다** —
KMS·Vault 드라이버는 같은 인터페이스 뒤에 따로 붙는다. 세대를 보관하는 만큼 옛 개인키도
디스크에 남는다(GC 상한이 덮지만, 그 사실 자체는 남는다).

---

## 2. 증명된 것

**엔진 사실 61건**이 바닥이다. 설계가 "nginx 는 이렇게 동작한다"고 전제한 것을 실제
엔진으로 확인했고, **세 건은 실측이 설계를 뒤집었다**(`ipv6only` 기본값 · location 은
longest-prefix · `default_server` 부재 시 첫 server 로 감).

**실물 nginx e2e 14건.** 그중 6건은 **DP 컨테이너 안에서** 에이전트를 돌린다 —
`FileStore`·`DpAgent`·`FsEffects`·`ApplyRunner` 가 전부 실물이다. 호스트에서 심볼릭
링크를 교체하면 컨테이너가 못 본다는 것을 두 번 실측했고, 그래서 설계가 말한 배치
(§11.1)가 맞다는 것이 확인됐다.

**conformance 232건은 전부 남이 재현한 반례다.** 우리가 상상한 시나리오가 아니다.

---

## 3. 열두 번의 검수 — 무엇을 배웠나

| 회차 | 무엇이 나왔나 |
|---|---|
| 1~3 | 설계 자체. 좌표 분리, 리더 펜싱, 이중 평면 |
| 4 | **거짓 신호.** 단위 테스트가 틀린 엔진 사실을 고정하고 있었다 |
| 5 | 녹색 상태에서 반례 7건. 소유권 예약 부재가 다섯 증상의 공통 원인 |
| 6 | 반례 7건. `protocol: 'https'` 가 **평문 렌더** — 4차 지적의 재발 |
| 7 | 6차 수정(`activeOperation`)이 만든 **영구 교착** |
| 8 | 7차 수정(`fence` 승계)이 만든 구멍 + **내가 적은 "안전 경계"가 거짓** |
| 9 | 8차 수정(`lease`)이 **타입이 아니라 주석**이었다 |
| 10 | 9차의 수렴 도입이 **종단 뒤를 안 봤다** |
| 11 | 10차의 `reconcile` 이 **포인터만** 되돌렸다 |
| 12 | 11차의 `deadline` 정리가 **신임 오퍼레이션을 훼손**했다 |

### 곡선이 수렴하고 있는가 — 모른다

**7~12차가 전부 직전 회차 수정이 만든 구멍이었다.** 여섯 번 연속이다.

```
6차 ③ 고침 → activeOperation      → 7차 ① 영구 교착
7차 ① 고침 → fence 가 상태 변경    → 8차 ②③
8차 ① 고침 → lease                → 9차 ① lease 는 주석일 뿐
9차 고침   → 수렴 도입             → 10차 ① 종단 뒤 재검증 없음
10차 고침  → reconcile             → 11차 ① 포인터만 되돌림
11차 고침  → deadline              → 12차 ① 예산 정리가 남을 훼손
```

**회귀는 확실히 잡힌다.** 12차 ④ 를 고치다 만든 회귀를 9차 테스트가 즉시 잡았다 —
232건을 쌓아 온 값이 거기서 나온다. 그런데 **새로 만드는 구멍의 발생률이 줄고 있다는
증거는 없다.** 매 회차 1~5건이 꾸준히 나온다.

가능한 해석은 둘이다.
- **수렴 중이다.** 반례가 점점 좁고 특수해지고 있다(12차는 "예산 초과 × 리더 교체 ×
  진행 중인 신임 저널" 같은 3중 교차다). 그렇다면 계속 돌리는 것이 맞다.
- **접근이 잘못됐다.** 분산 상태기계를 사후 반례로 깎고 있는데, 이건 **불변식을 먼저
  적고 그것을 검사**해야 하는 종류의 문제일 수 있다.

13차 검수에 이 질문을 직접 물었다. 답은 **"수렴 중이라는 증거가 없고 접근을 바꿔야
한다"** 였다 — 불변식을 먼저 명시하고, 단일 effect supervisor 와 reducer 를 두고,
deterministic scheduler 와 property-based 검사로 때리라는 것.

### 그래서 잴 수 있게 만들었다 (13차 뒤)

처방을 통째로 따르지는 않았다. 검수 ⑤("`Effects`·`ApplyLease`·저널 phase 를 표면에서
빼라")는 **표면을 크게 움직이는 일**인데, 같은 검수가 준 동결 기준이 "표면이 안 움직일
것" 이다. 지금 하면 카운터가 0 으로 리셋된다. 재작성 없이 되는 둘을 먼저 했다.

**(A) 표면 계측기** — `scripts/surface.mjs` · `SURFACE.txt` · `verify.sh` 게이트.
`src/index.ts` 가 내보내는 것의 폐포를 해시한다. "계약이 움직였나" 가 인상 비평이 아니라
숫자가 됐다. 계측기 자체를 뮤테이션으로 확인했다 — 표면 타입 필드 추가는 잡고, 중첩
타입만 바꿔도 잡고(폐포), 내부 `DpAgent` 메서드 추가는 **안 잡는다.**

재보니 표면은 **12차 이후 안 움직였다.** 13차가 바꾼 것은 전부 내부였다.

**(B) 실행 가능한 불변식** — `assertInvariants` 를 `DpAgent.serial()` 에 걸었다.
상태가 store 로 내려가는 유일한 통로라, **기존 451 개 테스트가 전부 불변식 테스트가
된다.** 새 시나리오는 한 줄도 안 썼다.

```
I1 최신 토큰만 변이   실행권·예약이 최신 토큰을 넘지 않는다     뮤테이션 18 건 검출
I3 되돌아가지 않는다  토큰·좌표·저널 seq 단조                   뮤테이션 12 건 검출
I5 intent 는 fenced   기록된 게시가 최신 토큰보다 높을 수 없다  뮤테이션 79 건 검출
I6 기준 후보가 조용히 사라지지 않는다 (14차에 우리가 찾았다)    뮤테이션 10 건 검출
```

**I6 이 중요한 이유.** 14차에 고친 반례 넷이 전부 I1·I3·I5 밖이라 "게시는 외부 효과라
상태 술어로 표현 안 된다" 고 넘겼는데, 그중 하나는 **끌어올 수 있었다.** 사고가 난 자리는
"게시가 무엇인가" 가 아니라 **"기준 후보가 어떻게 사라지는가"** 였고 그건 순수한 상태
전이다. 넣고 그 버그를 되돌려 보니 **내가 쓴 반례 말고도 명시적 단언이 없던 시나리오
세 개에서 잡혔다** — 반례 하나를 고정하는 것보다 넓다.

이음매라고 다 못 재는 것이 아니었다. 못 덮은 것은 외부 효과 **자체**이지 그 효과가
상태에 남기는 자국이 아니었다.

현 코드에서 **위반 0.**

### 검수가 준 불변식 하나가 틀렸다

**"terminal 이면 holder 0"** 을 넣으니 86 개가 터졌다. 버그가 아니라 설계다 — 종단
저널을 **먼저 쓰고** 실행권을 나중에 반납한다. 반대로 하면 자기 종단 기록이 자기 소유권
검사에 막힌다. 옳은 형태는 시점 불변식이 아니라 **활성 속성**("종단이면 언젠가
반납된다")이고, 그건 conformance 가 이미 짚는다(6차 ④ · 12차 ⑤).

"동일 baseline 재관측만 converged" 는 외부 관측과 시간이 들어가 상태 술어가 아니다.

**다섯 중 셋만 참이고, 하나는 여기서 잴 수 없고, 하나는 틀렸다.** 적어 보지 않았으면
몰랐을 것이다.

### 그리고 나는 재현 실패를 반증으로 썼다

13차 ①④ 를 "재현되지 않는다" 고 적었다. **둘 다 진짜였다.** 내 시나리오가 약했던
것이다. 코드를 읽고 다시 짜니 그대로 나왔다.

- **①** `#reconcileOnce` 가 **리더 토큰을 전혀 안 보고 있었다.** 신임이 fence 하고
  자기 세대를 게시한 뒤에도 옛 드라이버의 reconcile 한 번이 그걸 옛 세대로 덮으며
  `repaired` 를 돌려줬다. `exclusiveApply` 는 **인스턴스 안의** 큐라 두 리더 사이에서는
  아무것도 막지 못한다. 막을 수 있는 것은 durable 한 토큰뿐이다.
- **④** 라운드를 소진하면 **아무것도 관측하지 않고** `converged` 를 돌려줬다.
  "지금은 우리 차례가 아니다" 와 "수렴했다" 는 다른 말이다.

교훈은 하나다 — **한 번 짜 본 시나리오가 안 나왔다고 없는 것이 아니다.** 반증하려면
그 자리의 코드가 왜 안전한지를 말해야 하고, 말할 수 없으면 아직 모르는 것이다.

### 그리고 이번에도 직전 수정이 구멍을 만들었다

13차 ② 를 고치며 승격을 `finishOperation({promote})` 로 접었는데, `recover()` 의 종단
경로에 `promote` 를 안 넘겼다. `activated` 저널을 쓰고 반납 전에 재시작하면 복구가
기준을 **승격하지 않고 지웠다** — 활성화는 일어났는데 되돌릴 곳이 없다고 답한다.
14차 검수가 코드 추적만으로 지목했고, 재현해 보니 맞았다. **여덟 회차 연속이다.**

### 곡선 — 처음으로 잴 수 있는 신호가 생겼다

```
표면 무변동        13차 · 14차 연속 — 반례 4 건을 고치는 동안 계약이 한 번도 안 움직였다
불변식 위반        0 / 456 테스트
13차 반례 실재율   3 / 5   (내가 처음 판정한 1/5 는 틀렸다)
직전 수정이 만든   여전히 1 건 — 여덟 회차 연속
```

**표면 신호는 진짜다.** 오늘 고친 넷이 전부 내부 수정으로 끝났다. `ReconcileResult` 에
새 변종을 만들 뻔했지만 `diverged` 가 이미 맞는 말이었다 — "우리 기준과 실제가 다르고
우리가 고치지 못했다".

**불변식 신호는 반쪽이다.** 위반을 하나도 못 찾았는데, 오늘 고친 넷이 **전부 I1·I3·I5
밖의 '점'** 이었다. 게시는 외부 효과라 상태 술어로 표현되지 않는다. 불변식이 그물인
영역과 아닌 영역이 갈렸다는 뜻이고, 아닌 영역이 하필 사고가 나는 곳이다.

### 세 번째 축 — 그래서 스케줄러를 만들었다

14차 판정은 **"deterministic scheduler + property-based 검사에 지금 투자해야 한다"** 였고,
논거가 맞았다. 계측기 둘이 재는 축이 각각 하나뿐이다.

```
표면 해시   시그니처 축
불변식      store 에 저장되는 상태 축
스케줄러    "언제 누가 끼어드는가" — 최근 결함이 전부 여기 있었다
```

`tests/model/` 이 그것이다. yield 지점은 store save 전후와 모든 Effects 호출 전후이고,
DFS 150 + 시드 고정 무작위 350 으로 훑는다. 결정적이라 실패하면 선택열 그대로 재현된다.

**손으로 짠 반례와 다른 점은 하나다 — 교차를 내가 고르지 않는다.** 열네 라운드 동안
내가 상상한 교차만 검사해 왔고, 13차 ①④ 를 재현 못 한 것도 그래서였다.

### 스케줄러가 찾은 것 (셋 다 손으로는 못 짰다)

| | 무엇 | 결과 |
|---|---|---|
| ① | 낡은 러너가 신임의 게시 **의도**를 덮는다 | 게시는 lease 가 막지만 의도는 그 앞에 내려간다 |
| ② | 비종단 저널이 고아가 되면 복구가 `not_reserved` | **그 전환이 영구히 막힌다** |
| ③ | 남이 한 평면을 걷어차면 `stage` 거부가 밖으로 나간다 | 저널이 `published` 로 굳고 **영구히 막힌다** |

②③ 은 "막힌다" 를 3 회 반복 호출로 직접 확인했다. 셋 다 고쳤다.

### 그리고 검수가 준 속성이 또 하나 거짓이었다

**"비종단 journal 에는 동일 identity 의 holder 가 있다"** 는 시점 불변식으로 성립하지
않는다 — 저널과 실행권이 서로 다른 임계 구간에서 움직인다. 13차의 **"terminal 이면
holder 0"** 과 같은 병이다. **두 번 다 같은 실수를 검수가 했고 두 번 다 넣어 보고 알았다.**
원하는 것은 그 상태가 막히지 않는 것이므로 회복 가능성으로 다시 썼다.

### 하네스가 거짓말을 한 적이 있다

`Promise.allSettled` 가 `InvariantViolation` 을 통째로 삼켜서, 일부러 버그를 넣어도
모델이 초록이었다. **뮤테이션을 안 돌렸으면 못 봤다.**

그리고 DFS 만으로는 부족했다. 가장 늦은 분기를 밀며 훑으므로 예산 안에서는 꼬리만 본다 —
정작 중요한 것은 "리더가 **언제** 바뀌는가" 같은 앞쪽 선택이다. 무작위 표본이 잡았다.

모델의 구멍도 하나 있었다. 두 평면이 늘 성공해서 **부분 커밋이 안 만들어졌고**, 그 축의
뮤테이션을 통째로 놓쳤다. 한 평면만 abort 하는 행위자를 넣자 **바로 ③ 이 나왔다.**

**초록은 "안전하다" 가 아니라 "여기까지는 못 깼다" 이다.** 큰 시나리오는 "상한에서
끊었다" 가 찍힌다 — 다만 **매번은 아니다.** '두 오퍼레이션이 같은 좌표' 처럼 작은 것은
`전부 훑었다` 가 나온다(18차 검수가 이 부정확을 지적했다). 어느 쪽인지는 매 실행에
찍히므로, 초록을 읽을 때 그 줄을 같이 봐야 한다.

### 16·17차 — 적대자를 안에서 만들었고, 그게 일을 했다

외부 검수 CLI 가 다섯 회차 연속 죽거나(공급자 필터) 사용량 한도에 막혔다. **적대자를
밖에서 못 구하면 안에서 만들어야 한다.**

**뮤테이션 스윕**(`scripts/mutate.mjs`) — 규칙으로 439 개를 생성한다. 손으로 고르면
내가 의심한 곳만 검사한다. 첫 140 개에서 **점수 76.4%**, 생존자를 셋으로 갈랐다.

- **진짜 틈** — `admit` 재요청 캐시 다섯 자리가 통째로 미검사였다. 기존 재요청 테스트는
  **좌표가 아직 그 자리에 있을 때**만 봐서, 뒤따르는 검사가 같은 답을 내면 캐시 없이도
  통과했다. 갈리는 자리는 **좌표가 지나간 뒤**다 — 중복 배달은 네트워크가 정한다.
  그리고 재시도의 **성공** 경로가 통째로 비어 있었다(실패 쪽만 12차 ⑤ 가 짚었다).
- **동치** — 반납이 네 자리(`failAll` · `abortConfig` · 루프 머리 · `releaseHolderSlots`)에
  있어 한 자리를 지워도 안 빨개진다. 계약을 짚고 **자리를 전부 지웠을 때** 확인한다.
- **도달 불가** — 라고 적었다가 **틀렸다**(아래).

반례를 채운 뒤 같은 시드로 다시 돌리니 **80.0%** 가 됐다.

**그리고 스윕이 커밋 둘을 오염시켰다.** 배경으로 돌려 놓고 `git add -A` 를 해서 변이
두 개가 섞여 들어갔다. `finally` 는 그 프로세스만 지킨다 — **도구가 본체를 건드리는 한
사고는 반드시 난다.** 별도 worktree 로 옮겼다.

### 17차 (Claude Code · Fable 5) — 반례 넷, 전부 재현

CLI 가 막혀 에이전트로 돌렸다. 완주했고 **내 주장 둘을 반증했다.**

| | 무엇 | 결과 |
|---|---|---|
| A | 16차 규칙과 I6(b)가 **서로 반대를 명령**한다 | ✅ 재현·수정 |
| B | `supersede` 승격이 P1 을 어긴다 | ✅ 재현 — **속성이 틀렸다** |
| C | "좌표 CAS 도달 불가" 는 **내 오독** | ✅ 재현·수정 |
| D | 고아 저널을 종단 기록 없이 덮는다 | ✅ 재현·수정 (검수는 미확인이라 했다) |

**A 가 제일 무겁다.** 뜻 없는 후보 + `activated` 저널이면 복구·fence·apply 가 전부 그
자리에서 던지고, 좌표는 epoch 2 인데 **수렴이 `converged(gen-A)`** 라고 답한다. 막힌
것보다 이게 나쁘다 — 호출자에게 "손 떼도 된다" 고 말한다. 16차에 적은 근거("기준이
없으면 `no_baseline` 으로 드러난다")가 **이전 기준이 있을 때 틀렸다.**

**B 는 속성이 틀린 경우다.** P1 이 `pendingActivation`·`lastActivated` 까지 봤는데, 그
둘은 **일어난 일의 기록**이라 적힌 토큰은 역사지 지금 쓰는 사람이 아니다. `fence` 승계가
후보를 승격시키는 정당한 경로를 위반으로 세고 있었다. `lastPublishIntent` 만 남겼다 —
그건 "지금 하려는 일" 이라 현재 리더의 주장이다.

**C 는 내가 스윕 결과를 오독한 것이다.** 생존을 "도달 불가" 로 결론내고 코드에 적었는데,
길이 있었다. **못 찾은 것과 없는 것은 다르다** — 13차에서 재현 실패를 반증으로 쓴 것과
같은 실수를 도구 결과에 대해 반복했다.

### 15차 — 오래 열려 있던 둘을 닫았다

**① lease 를 타입이 강제한다** (11차 ⑤, 다섯 회차 만에).

9차부터 "타입이 강제하지 못한다. 주석으로 부탁하는 것일 뿐" 이라고 적어 왔다. **인자로는
맞는 말이었다** — TypeScript 는 인자를 덜 받는 함수를 대입시키므로 `async publish() {}` 도
`Effects` 를 만족했다. `v01-surface` 테스트가 그 사실을 증명하는 용도로 있었다.

**반환 타입으로는 막힌다.** `assertValid()` 만이 만드는 표(`Checked`, unique symbol)를
부작용이 돌려주게 하면, 표를 얻으려면 확인을 부를 수밖에 없다.

```ts
async publish(record, lease) {          // Promise<Checked>
  const ok = lease.assertValid();       // 여기서만 표가 나온다
  await this.swapSymlink(record);
  return ok;
}
```

**표면이 움직였다 — 카운터가 2 → 0 이다.** 13·14차가 둘 다 "표면을 지금 옮겨라" 고 했고,
이건 그중 가장 작고 목적이 분명한 이동이다. 카운터를 잃는 값은 치를 만하다고 판단했다.

⚠️ 확인과 부작용 **사이의 await** 는 여전히 못 막는다. 표는 "불렀는가" 만 강제한다.

**② fsync 순서 · 락 원자성을 검증했다** (6차 E 의 마지막 항목, 열 회차 만에).

커널을 끌 수 없으므로 커널이 주는 것과 우리가 지킬 것을 나눴다. 락은 **프로세스 여덟 개**로
진짜 경쟁시켰다. 순서는 깨졌을 때 남는 모양(빈 파일 · 잘린 JSON · 변조)을 만들어 놓고
정본으로 받아들이지 않는 것을 봤다. **syscall 이 디스크에 닿았는지는 못 잰다** — 순서
검사는 소스 텍스트를 보는 것이고, 그 한계를 적어 뒀다.

### 그리고 같은 병이 한 층 아래에 있었다

14차에 "boolean 을 없앴다" 고 적었다. 실제로는 신뢰를 boolean 에서 **호출자가 준 평면
집합**으로 옮긴 것뿐이었다. 같은 id 로 평면 하나만 담아 끝내면 부분 활성화가 기준으로
올라갔다 — 15차 검수가 지목했고 재현했다.

문제는 boolean 이 아니라 **호출자가 무엇을 아는지에 기댄 것**이었다. 완결의 뜻을 후보와
함께 적는다(`pendingEpochs`). 이제 옮길 곳이 없다.

### 33차 — 정지 조건 충족. 검수 시리즈를 멈춘다.

```
수정 유도 결함 연속  0 · 0     ← 두 회차 연속. 조건 충족.
새 행동 결함         없음
```

33차가 폐위 절 주변을 여러 축으로 쳤고 **재현되는 반례가 없었다.** 그리고 32차가 넣은
두 테스트가 실제로 하중을 받는 것을 뮤턴트로 확인했다 — 특히 **절을 "레거시 전용" 으로
좁히는 뮤턴트**를 넣으면 CE-32-A 하나만 빨개지고, 그 테스트를 지우면 **599 전부 초록**
이다. 즉 32차가 막으려던 함정이 실재했다.

#### 멈추는 근거

27~31차의 병(수정이 다음 결함을 만드는 것)의 진원지였던 폐위 절이 **두 방향 모두**
겨눈 테스트로 잠겼다 — 넓히면 되감김 테스트가, 좁히면 이름 테스트가 잡는다.

```
반례 회차          33 회 (5~33차)
누적 구멍          24 개 — 그중 수정이 만든 것 다수
전 게이트          초록 (unit 217 · conformance 347 · 모델 13 · e2e 14 · engine 61 · spike 42)
표면               78 심볼 · 동결 카운터 0 (26차에 계약을 옮기며 리셋)
```

#### 마지막 계측 — 스윕 84.5%

```
523 개 생성 · 110 개 실행 (seed 33033) · 죽었다 93 · 살았다 17 · 84.5%
```

**26차 계약 이동 이후 첫 측정**이다(그 전까지 스윕은 낡은 지도를 보고 있었다).
이력 중 두 번째로 높다 — 86.4(23차) · **84.5(33차)** · 81.8 · 76.4 · 80.7 · 80.0 · 76.4.
시드마다 표본이 달라 대략치라는 단서는 그대로다.

생존자 17 중 셋이 또 **신원 비교**다(`agent.ts` 1106·1008, `driver.ts` 416).
23차부터 매 회차 같은 자리들이 찍힌다 — `completed` 캐시가 지우고 있고 **그 캐시가
아니라 비교가 지켜야 할 것들**이다. 가지치기 체크리스트가 이 목록의 근거다.

#### 멈춘 뒤에도 남는 위험 — 여섯

1. **스윕이 26차 이전 지도를 본다** — 계약 이동 이후 미측정이었다(이번에 재측정했다).
2. **I6(b)·I7 은 검출력 0** — 장식 불변식 둘. **논증에 쓰면 28차의 재판이 된다.**
3. **`completed` 무한 성장** — 가지치기와 신원 비교는 **한 커밋**이어야 한다(23차 규칙).
   v0.3 전에 반드시. 지금 여섯 자리가 이 캐시에 우연히 기대고 있다.
4. **`dirty` 의 `intent` 오독** — 폐위 출신일 때 `intent` 는 낡은 세대다. 그것을 복원
   목표로 읽으면 **운영자가 손으로 되감는다** — 코드가 안 하려고 폐위한 그 일이다.
   docstring 에 적었다.
5. **영원히 사는 옛 writer 는 수렴 불능** — 리더 선출의 몫이라는 설계 전제는 v0.2
   컨트롤 플레인 구현 때 검증 대상이다.
6. **CE-32-A 상태의 표면 도달 가능성 미확인** — agent 층에서는 성립하고 driver 층
   경유로는 못 만들었다. **없다고 증명한 것이 아니다.** v0.3 이 표면을 넓힐 때 재검.

#### 검수를 재개할 조건

멈춤은 영구가 아니다. **다음 중 하나면 재개한다.**

```
· 표면(SURFACE.txt)이 움직일 때 — 계약을 옮기는 변경
· `completed` 가지치기를 넣을 때 — 신원 비교 여섯 자리를 같은 커밋에서
· v0.2(컨트롤 플레인) 또는 v0.3(멤버십)에 진입할 때 — 표면이 넓어진다
· 폐위 절 · `candidateArrived` · `movedByMe` 를 만질 때 — 24 개 구멍 중 다수의 진원지
```

**이 목록이 없으면 멈춤은 방치가 된다.** 그래서 적는다.

### 32차 — 연속 0. 그리고 여섯 회차 동안 쓴 이름이 거짓이었다

```
수정 유도 결함 연속  0  ← 첫 번째. 멈추려면 한 회차 더.
새 행동 결함         0
계측 구멍            2 (CE-32-A · CE-32-B)
```

**31차 수정이 새 구멍을 안 만들었다.** 32차가 다섯 축으로 공격해 전부 재현으로 확인했고,
31차가 적은 주장(표면 무변동 · `dirty` 는 기존 계약 · 부작용 0/봉쇄 0/되감김 0)도 전부
실측으로 참이었다. **폐위 뒤 출구도 확인됐다** — `dirty` 에서 `applyConfig(gen-C)` 하면
`activated` → `converged`. 운영자가 갈 곳이 있다.

#### 그러나 이름이 거짓이었다

27~31차 내내 이 문제를 **"26차 이전 writer 가 남긴 상태"** 라고 불렀다. **거짓이다.**
폐위 절은 `by === undefined` 를 **안 본다** — 서명이 온전한 지금 세상에서도 혼합 저자 +
지연 finalize 면 같은 상태가 만들어지고, 폐위가 없으면 **세상 되감김**이다.

즉 31차는 서명 경로의 상(上)급 되감김을 **우연히** 함께 고쳤고, 그것을 겨눈 테스트는
0 개였다. **주석과 커밋이 "레거시 창의 셋째 결과" 라고 서술하므로, 다음 회차가 절을
그 이름에 맞춰 좁히는 순간 상급 되감김이 전 스위트 초록인 채 돌아온다.**

**이름을 잘못 붙이면 다음 사람이 그 이름을 믿고 좁힌다.** 그래서 넣은 테스트는 수정을
지키는 것이 아니라 **이름을 지킨다.** 코드 주석에도 "좁히지 마라" 를 적었다.

#### 마이그레이션은 부결

31차가 권한 "창을 마이그레이션으로 없애기" 는 **전제가 깨졌다** — 폐위가 레거시 전용이
아니므로 창을 없애도 절은 영구히 필요하다. 마이그레이션이 죽일 수 있는 것은 원장 폴백
한 줄뿐인데, 32차 실측으로 그걸 봉인하면 **창에서 가장 흔한 경우(전 평면 자기 커밋)의
승격이 `dirty` 로 격하**된다 — 퇴행이다. 게다가 로드 시점 변환은 읽기 경로가 쓰기를
하게 되고, 첫 쓰기 시점이면 판정 시점이 비결정이 된다. `by` 소급 날조는 26차가 닫은
닮음 병을 서명 위조로 재도입한다.

#### 계측 구멍 둘을 닫았다

```
CE-32-A  폐위 절의 서명 경로 보호 — 겨눈 테스트 0 개였다
CE-32-B  원장 폴백의 판별 방향 — 조건을 true 로 넓혀도 574 초록이었다
```

둘 다 **살아 있는데 아무도 안 지나가는 판정 자리**였다(I6(b)·I7 과 같은 부류).
넣고 뮤턴트로 갈리는 것을 확인했다.

### 31차 — 세 번째 결과를 만들었다

```
수정 유도 결함 연속  0 유지 (리셋)
상(上)               1 (CE-31 — 되감김이 나머지 창에서 재발)
```

**30차 수정이 스물네 번째를 만들었다. 다섯 회차째 같은 창이다.**

30차는 뿌리를 정확히 적었다 — *"판정이 불능인데 결과 선택지가 승격/폐기 둘뿐이다."*
**그리고는 셋째 결과를 만들지 않고 나머지를 폐기 쪽에 배정했다.**

```
27차 봉쇄 → 28차 폐기 통과 → 되감김(전체 창)
         → 29차 부분 승격 + 나머지 봉쇄 → 30차 나머지 폐기 통과 → 되감김(나머지 창)
```

**이름을 붙인 것과 고친 것은 다른 일이다.** 뿌리를 정확히 진단한 회차가 그 진단을
따르지 않았다.

#### 셋째 결과: 기준 폐위

폐기가 왜 되감김인가 — 폐기는 **낡은 `lastActivated` 를 정답 권위로 남긴다.**
좌표·저널·terminal 이 전부 새 세대를 말하는데 기준만 옛것이면, 수렴은 기준을 정답으로
삼아 **서빙 중인 세대를 되감고 `repaired` 라 답한다.**

선언한 전 평면이 목표 좌표에 **도착했는데**(I3 에 의해 좌표는 앞으로만 가므로 이것은
**세상이 기준을 지나쳤다는 증명**이다) 그것을 내 공로로 셀 수 없으면 — 승격도 아니고,
옛 기준을 권위로 남기는 것도 아니다. **기준이 더 이상 권위가 아니라고 적는다.**

그러면 수렴의 **기존 분기**가 받는다: 기준이 없고 게시 의도가 있으면 `dirty`.
부작용 0 · 봉쇄 0 · 되감김 0, 그리고 진단이 정직하다. **표면은 안 움직인다** —
`dirty` 는 이미 계약에 있었다. 31차의 설계 권고를 그대로 썼다.

#### 그리고 내 수정에 같은 병을 적용해 봤다

폐위 절을 **넓게** 만든 뮤턴트(도착 여부 무시)가 573 전부 통과했다 — `positionsReached`
가 미검사였다는 뜻이다. 겨눈 테스트를 썼는데 **그것도 안 갈렸다**: 커밋이 하나도 없으면
후보 자체가 안 생겨 두 분기를 아예 안 지나간다. **픽스처가 통과시킨 것**이다(15차의 그것).
후보는 있고 도착은 못 한 상태로 고쳐서야 갈렸다.

다섯 회차째 앓은 병("검증한 부분집합에서 조용히 일반화")을 **내 수정 자신에게** 적용한
것이 이번 회차에서 제일 값있는 일이었다.

#### 남은 선택지 — 창 자체를 없애기

31차가 더 싼 쪽을 권했다: 이 규칙을 **일회성 마이그레이션**으로 쓰고 payload 에 버전
표식을 찍어 `by === undefined` 분기를 죽이는 것. 폴백을 영구히 두면 원장 폴백이 id 만
보는 비교 자리를 하나 더 늘린 채 남는다(우연한 방패 부류). **다음 회차의 판정으로
넘긴다** — 26차에 계약을 옮길 때와 같은 방식이다.

### 30차 — 같은 창을 양쪽으로 오갔다

```
수정 유도 결함 연속  2 → 0 리셋
상(上)               1 (CE-30 — 봉쇄가 돌아왔다)
```

**29차 수정이 스물세 번째를 만들었다.** 29차의 논증은 *"이제 `finalizeCandidate` 가
`terminal` 원장으로 제대로 승격하므로 그 상태 자체가 안 생긴다"* 였다. **이 전칭 명제가
거짓이다.** 원장 폴백이 승격하는 것은 **후보가 모든 평면을 자기 전환 키로 활성화한**
경우뿐이고, 29차가 넣은 테스트 둘이 정확히 그 부분집합만 만든다.

**검증한 부분집합에서 전체 창으로 조용히 일반화했다** — 다섯 회차째 같은 병이다.

혼합 저자(내가 http, 남이 stream — 26차가 "가장 흔한 패턴" 이라 적은 재시도)에서는
폴백이 false 를 답하고, 면제는 뺐으므로 I6(b)가 발화해 **다시 영구 봉쇄**다.

```
27차  arrived 에 서명 대조    → 봉쇄        (CE-28)
28차  I6(b) 면제로 봉쇄 해제  → 세상 되감김 (CE-29)
29차  원장으로 부분집합 승격  → 나머지 봉쇄 (CE-30)
```

**30차가 뿌리를 짚었다: 판정이 불능인데 결과 선택지가 승격/폐기 둘뿐이다.**
셋 다 같은 창을 양쪽으로 오간 것이지 원인을 고친 적이 없다.

#### 이번엔 전제를 고쳤다

I6(b)는 *"저널이 `activated` 인데 후보가 승격 없이 사라졌다"* 를 위반으로 봤다 — 즉
**저널의 주장을 전제로 삼았다.** 그런데 26차 이전 writer 의 저널은 닮음으로 판정해
쓴 것이라 **거짓일 수 있다.** 혼합 저자인데 `activated` 라고 적힌 상태가 실재한다.
그러면 후보는 정당하게 승격되지 않는데 (b)가 발화해 봉쇄가 된다.

이제 **좌표가 도착을 증명할 때만** 발화한다. (b)가 원래 잡으려던 버그("도착했는데
승격을 안 했다")는 그대로 잡고, 저널이 거짓말한 경우는 통과한다. 그리고 승격을 결정하는
판정과 승격을 강제하는 불변식이 **같은 함수**(`candidateArrived`)를 쓴다 — 자리가 둘이면
갈린다(CE-26-A 가 그것이었다).

**그러나 (b)의 이빨을 되찾아 준 것은 아니다.** 재 봤다 — 통째로 꺼도 572 전부 초록이다.
바뀐 것은 잘못 발화하던 것이 멈춘 것뿐이다. **되찾았다고 적고 싶었지만 재 보니
아니었다** — 이 문장을 적는 것이 이번 회차에서 제일 중요한 일이다.

### 29차 — 가격표를 세 번 틀렸고, 세 번 다 가격표를 겨눈 단언이 없었다

```
수정 유도 결함 연속  1 → 2
상(上)               1 (CE-29 — 세상 되감김)
```

**28차 수정이 스물두 번째를 만들었다. 그리고 이번엔 상(上)이다.**

27차 수정이 서명 없는 좌표에서 **봉쇄**를 만들었고(CE-28), 28차가 I6(b) **면제**로 그
봉쇄를 풀었다 — "판정을 못 하면 비관적으로 두고 앞으로 간다" 는 근거였다.

**그 "앞으로" 가 세상을 되감았다.** 기준을 안 올린 채 통과하면 저널·terminal·좌표가
전부 "gen-B 가 활성화됐다" 고 말하는데 기준만 gen-A 다. **수렴은 기준을 정답으로
삼는다** — 실제로 서빙 중인 gen-B 를 gen-A 로 되감고 `repaired` 라고 답한다. 게시와
HUP 이 실제로 나간다. 되돌릴 수 없는 연산이다.

```
26차 가격표  "비관적으로 failed 로 판정"      실제: 영구 봉쇄        (28차가 발견)
28차 가격표  "비관적으로 두고 앞으로 간다"    실제: 세상 되감김      (29차가 발견)
```

**세 번 다 가격표를 겨눈 단언이 없었다.** CE-28 테스트는 "던지지 않는다 · fence 가
된다" 만 봤지 **약속한 것이 실제로 무엇인지**를 안 봤다. 그 미검사 구간에서 CE-29 가
나왔다. 단언을 넣었다.

#### 병은 "판정을 못 한다" 고 적고 재료를 안 찾아본 것이다

`terminal` 원장의 키가 `operationId:transitionId:plane` 이다 — **닮음이 아니라 신원**이고,
서명과 독립이며, 이 창 **이전부터** 있었다. 서명이 없으면 그것을 읽는다. 그러면 기준이
제대로 올라가고 되감김이 안 난다.

**면제는 뺐다.** 면제는 봉쇄를 푸는 우회였고 그 우회가 되감김을 열었다 — **증상을 끄는
대신 원인을 고쳤다.** 서명 대신 원장을 읽는 폴백은 CE-27 을 다시 열지 않는다: 거기서는
남이 옮긴 평면에 대해 후보의 전환 키로 된 `terminal` 기록이 **없다.**

#### 그리고 I6(b)는 장식이었다

29차가 실측했다 — **(b)를 통째로 꺼도 569 전부 초록**이고, 27차 HEAD 에서도 564 초록이다.
28차가 면제의 안전 근거로 든 두 측정("P11 이 잡는다", "승격 차단 뮤턴트 112 건")은
**승격 기능의 백업**을 잰 것이지 (b) 의 검출력을 잰 것이 아니었다.

**근거를 적었지만 그 근거가 재는 대상이 달랐다.** 28차에 "전수로 셌다" 며 자축한 바로
그 회차에서, 정작 그 회차의 핵심 논증이 엉뚱한 것을 재고 있었다.

### 28차 — 내가 매긴 가격표가 거짓이었다

```
수정 유도 결함 연속  0 → 1     ← 다시 났다
중(中) 이상          1 (CE-28) ← 전제 안에서는 상(上) 급
```

**27차 수정이 스물한 번째를 만들었다.** `arrived` 에 서명 대조를 넣으면서, **서명 없는
좌표**에서는 `arrived` 가 무조건 거짓이 됐다. 그러면 후보가 승격 없이 지워지고 저널은
`activated` 라 I6(b)가 발화한다. `assertInvariants` 는 저장 **앞에서** 던지므로
**아무것도 안 써지고 매 호출 같은 자리에서 죽는다.**

```
recoverConfig  → InvariantViolation
fence('11')    → InvariantViolation   ← 신임 리더도 못 들어온다
abortConfig    → InvariantViolation   ← 운영자 포기 경로까지
reconcileConfig → unfinished("복구를 부르면 끝난다")  ← 그 처방이 거짓이다
```

**가장 나쁜 것은 내가 그 대가를 미리 적어 뒀다는 점이다.** 26차에 `by?` 를 옵셔널로
두며 이렇게 썼다 — *"구버전에서 이월된 열린 저널이 종단에서 **비관적으로 `failed` 로
판정**될 수 있는 1 회성 창"*. **그 가격표가 거짓이었다.** 실제 가격은 비관이 아니라
**영구 봉쇄**였다. 대가를 적는 것과 그 대가가 맞는지 확인하는 것은 다른 일이다 —
27차에 배운 "재현했다는 것과 그 수정을 지킨다는 것은 다른 일이다" 의 사촌이다.

**고친 방향**: 두 기록이 서로 반대를 말할 때(저널은 "활성화했다", 좌표는 "누가 놨는지
모른다") **던져서 멈추는 것은 답이 아니다.** 판정을 못 하면 비관적으로 두고(기준을 안
올린다) 앞으로 간다 — 약속한 그대로. I6(b)가 서명 없는 좌표에서는 발화하지 않는다.

면제가 위험하지 않은 근거: (b) 가 잡으려던 진짜 버그는 "commit 이 서명을 제대로 안
쓴다" 인데 **그건 P11 이 잡는다**(측정했다). 그리고 승격을 통째로 막는 뮤턴트를 넣으면
**112 건이 빨개진다** — 면제는 좁다.

#### 그리고 또 하나, 같은 병

28차가 부수로 짚었다. 26차에 `health` 의 서명 **보존** 절을 넣으며 근거를 주석에
적어 놓고 — *"여기서 자기 신원을 쓰면 정당한 활성화를 오살한다"* — **테스트를 안
겨눴다.** 훔치게 바꿔도 565 전부 초록이었다. 27차에 배운 그것이 **한 회차 만에 또**
나왔다. 고정했고 뮤턴트로 확인했다.

세 회차 연속 같은 자리에서 미끄러진다:

```
26차  재현했는데 수정을 안 겨눴다        → 27차가 실측으로 짚음
27차  같은 실수를 health 절에서 반복      → 28차가 실측으로 짚음
26차  대가를 적었는데 확인을 안 했다      → 28차가 재현으로 짚음
```

#### 그래서 이번엔 반응하지 않고 전수로 셌다

세 번 반응적으로 고쳤으니, 26~28차에 넣은 절을 **전부 뮤테이션**했다. 결과가 나빴다.

```
by 의 leaderToken 정규화      살았다
movedByMe 의 epoch 절         살았다
authoredBy 의 transitionId    살았다
authoredBy 의 leaderToken     살았다
```

**넷 다.** 26차에 `authoredBy` 를 만들며 *"`ownsJournal` 과 같은 삼중 비교다 — id 만으로는
fence 뒤 같은 id 재사용을 못 가른다"* 고 적어 놓고, **실제로 일하고 있던 것은
`operationId` 하나뿐이었다.** 세 축 중 둘이 장식이었고 아무도 몰랐다.

비교를 **직접** 겨눈 테스트 넷을 넣었다 — 저널·실행권을 거쳐 가면 그 기계들이 먼저 막아
정작 비교가 일하는지 못 본다(15차의 "픽스처가 통과시킨 것" 과 같은 함정이다).
이제 셋이 죽는다. **쓰기 쪽 정규화는 동치로 분류했다** — `authoredBy` 가 비교 때 양쪽을
정규화하므로 쓰기 쪽을 빼도 결과가 같다. 비교 쪽 정규화를 빼면 죽는 것을 확인했다.

**공통점은 "적었으니 됐다" 로 넘어간 것이다.** 주석·커밋 메시지·문서에 근거를 적는
습관이 이 시리즈의 강점인데, 그게 **확인을 대체하는 순간 약점이 된다.**

### 27차 — 연속이 끊겼다. 그리고 "부류를 닫았다" 는 내 말이 거짓이었다.

```
수정 유도 결함 연속  5 → 0     ← 끊겼다
중(中)               1 (CE-27) ← 아홉 회차 만에 처음
```

**연속이 끊긴 것은 확인됐다.** 27차가 git 으로 확인했고 나도 확인했다 — 26차 diff 는
`finalizeCandidate` 를 **한 글자도 안 건드렸다**(`git diff 3df1a74 470232c` 에서 0 건).
CE-27 은 14차(`arrived`)·15차(`pendingEpochs`)부터 있던 층이지 26차가 만든 것이 아니다.

#### 그러나 26차의 핵심 주장이 거짓이었다

26차 커밋 제목이 "부류를 닫는다" 였다. **닫지 않았다.** 좌표에 서명을 붙이고
`reachedPhase` · `failAll` · commit 재요청 셋을 서명으로 바꿨으면서, **"내가 놓았는가"
를 묻는 자리를 다 세지 않았다.** `finalizeCandidate` 의 `arrived` 는 그대로 닮음이었다.

```
X 가 http 만 옮기고 stream 을 포기한다
Y 가 stream 을 옮긴다
→ 좌표는 둘 다 목표 epoch 에 있다 → 닮음으로는 "도착"
→ **혼합 저자 부분 활성화가 전체 활성화 기준(lastActivated)으로 승격된다**
→ 수렴이 그 세대를 정답으로 게시한다
```

**durable 기준 오염이라 중(中)이다** — 여덟 회차 만에 처음 나온 중이고, 부류를 닫았다고
선언한 회차 바로 다음에 같은 부류에서 나왔다.

23·25·27차가 **세 번 같은 진단**을 했다: *규칙을 세우고 일부 자리에만 적용.*
이번엔 그 진단을 인용하며 규칙을 만든 내가 저질렀다. **규칙을 만들 때 자리를 세는 것이
규칙을 만드는 일의 절반이다.** 이제 `authoredBy` 를 쓰는 자리가 넷이다.

#### 재현했다는 것과 그 수정을 지킨다는 것은 다른 일이다

27차가 실측으로 짚었다. **26차가 재현까지 해서 고친 두 자리를 통째로 되돌려도 전
스위트가 초록이었다.**

```
failAll 을 25차 닮음(epoch+digest)으로 되돌림   → 562 초록
commit 재요청의 서명 절 제거 (CE-26-C 되돌림)  → 562 초록
```

17차 교훈("재현 경로 없는 수정은 다음 회차에 조용히 되돌아온다")이 **그 교훈을 인용한
커밋 자신**에 걸려 있었다. CE-26-A·C 테스트가 있었는데도 그랬다 — **반례를 다른
디테일로 재현해 놓고 정작 수정이 막는 자리를 안 겨눴기** 때문이다(CE-26-A 는 digest 가
다른 시나리오라 epoch+digest 닮음으로도 통과한다). 셋 다 고정했고 뮤턴트로 확인했다.

#### 스윕 76.4% — 방패가 또 찍혔다

```
523 개 생성 · 110 개 실행 (seed 27027) · 죽었다 84 · 살았다 26 · 76.4%
```

**이 측정은 27차 수정 앞의 HEAD 다**(수정 셋은 표적 뮤턴트로 따로 확인했다). 점수는
시드마다 표본이 달라 회차 간 비교가 대략치라는 것을 다시 적어 둔다 — 86.4 → 81.8 →
76.4 는 코드가 자란 것과 시드가 섞인 결과이지 품질이 내려간 증거가 아니다.

**생존자 26 개 중 넷이 또 신원 비교다.**

```
agent.ts:1243  if (holder.operationId !== op.operationId || holder.transitionId !== ...)
agent.ts:1629  if (slot.op.operationId === holder.operationId && slot.op.transitionId === ...)
agent.ts:1053  && slot.op.transitionId === op.transitionId)
agent.ts:618   if (big(slot.op.leaderToken) > max)
```

23차부터 매 회차 같은 자리들이 찍힌다. **`completed` 캐시가 지우고 있다** — 그 캐시가
아니라 비교가 지켜야 할 것들이다. 가지치기 체크리스트가 이제 여섯 항목이다.

#### 여섯 번째 우연한 방패

26차에 "`by` 없는 좌표는 비관 판정한다, 대가는 구버전 열린 저널이 한 번 비관적으로
닫히는 것" 이라고 적었다. **그 대가가 지금 지불되지 않는다** — `completed` 캐시의
canonical 비교에 `by` 가 없어서 캐시된 ACK 가 먼저 받는다. 27차가 재현했다: 캐시가
있으면 `activated`, 캐시까지 지우면 `failed`. 가지치기 체크리스트에 올렸다.

### 26차 — 부류를 닫았다. 카운터를 버리고.

```
중(中) 이상   없음        ← 여덟 회차째
하            3 (CE-26-A 모순 기록 · CE-26-B 같은-내용 재점유 · CE-26-C 거짓 성공 ACK)
```

**열아홉 번째이고 25차 수정이 만들었다 — 다섯 회차 연속이다.** 25차는 digest 조임을
`reachedPhase()` 에만 넣고 **`failAll` 의 평면별 판정에는 안 넣었다.** 그래서 한
`ApplyResult` 안에서 phase 는 "다 실패했다"(참) 인데 progress 는 "다 커밋됐다"(거짓) 가
됐다 — **일관되게 틀린 것보다 나쁘다.** 25차가 스스로 진단한 병형("규칙을 세우고 일부
자리에만 적용")을 그 규칙을 쓴 커밋에서 또 저질렀다.

#### 그래서 인스턴스 대신 부류를 닫았다

25차가 물었다: 인스턴스를 다섯 번 닫은 것과 부류를 한 번 닫고 카운터를 잃는 것, 어느
쪽이 싼가. **26차가 답을 재현으로 줬다.**

```
CE-26-B  남이 같은 내용(같은 digest)으로 같은 좌표를 채우면 digest 를 봐도 못 가른다
         — 그리고 같은 설정의 재시도는 운영에서 가장 흔한 패턴이다
CE-26-C  commit 재요청 판정도 닮음이라, 남이 채운 뒤 내 지연된 commit 이 도착하면
         **성공 ACK 를 돌려준다** — 나는 커밋했다고 믿는데 아무것도 안 했다
```

닮음 판정 자리가 이미 여섯이었고, 하나 닫을 때마다 그 아래 층이 열렸다. **인스턴스를
여섯 번째 닫아도 일곱 번째가 있다는 것이 이제 증명에 가깝다.**

그래서 좌표가 **누가 놨는지** 기억한다.

```ts
export type PlaneAuthor = { operationId: string; transitionId: string; leaderToken: string };
export type PlaneState = Coordinate & { payloadDigest: string; by?: PlaneAuthor };
```

`movedByMe()` 한 함수가 추론을 그만두고 **읽는다.** `reachedPhase()` 와 `failAll` 과
commit 재요청 판정이 전부 그것을 쓴다 — 자리가 둘이면 언젠가 갈리기 때문이다.

**닮음 폴백을 안 남겼다.** `by` 가 없으면 "내가 옮겼다" 가 아니다. 남기면 부류가 안
닫히고, 업그레이드 창이 지나면 죽은 폴백으로 남아 스윕이 짚는다(24차 규칙). 대가는
구버전에서 이월된 **열린 저널**이 종단에서 비관적으로 `failed` 로 판정될 수 있는
**1 회성 창**이다 — 방향이 안전하다. 남의 공로를 흡수하는 것보다 비관이 싸다.

`health` 는 서명을 **보존한다.** `by` 의 뜻은 "이 epoch 으로 옮긴 커밋" 이고 health 는
epoch 을 안 옮긴다. 여기서 자기 신원을 쓰면 아직 안 닫힌 저널의 판정이 남의 것으로
뒤집혀 **정당한 활성화를 오살**한다.

#### 카운터를 버린 것에 대하여

```
표면  77 → 78 심볼 (+PlaneAuthor) · 해시 이동 · 동결 카운터 4 → 0
```

24차가 이미 "표면 카운터만으로는 동결하지 않는다" 로 격하했고, 진짜 게이트(수정 유도
결함 연속 0)는 지금도 빨강이다. **동결이 어차피 부결인 기간이라 카운터 4 의 현재
가치가 0 이었다.** 옮길 거면 지금이 제일 싸다 — 15차에 `Checked` 를 넣으며 일부러
움직인 것과 같은 논리다.

#### 계측기가 구현의 가정을 베끼고 있었다 (26차가 셋을 짚었다)

1. **P10 ≡ reachedPhase** — 진실 술어가 구현과 **같은 공식**이었다. 25차가 "갈랐다" 고
   적었지만 실은 **가정을 한 층 아래로 같이 옮긴 것**이었고, CE-26-B 는 구조적으로
   영원히 못 잡았다. 이제 P10 도 서명을 읽는다.
2. **P10 이 progress 를 안 본다** — `Seen.journal` 에 progress 가 없다. §3.4 의 절반
   (평면별로 어디까지)을 보는 계측기가 넷 중 하나도 없다. CE-26-A 는 P10 이 완벽해도
   안 잡혔다. **아직 안 고쳤다.**
3. **P3 의 `arrived` 가 `'0'` 하드코딩** — "완전한 후보" 의 뜻을 후보의 목표 epoch 이
   아니라 모델 시나리오의 0→1 전제에서 가져왔다. 1→2 전환이 부분 승격되는 뮤턴트는
   통과한다. **아직 안 고쳤다.**

그리고 남은 한계 하나를 적어 둔다: 이제 구현도 P10 도 `by` 를 읽으므로 **`commit` 이
`by` 를 잘못 쓰는 버그는 둘 다 못 잡는다.** 진짜 독립은 시나리오가 자기 장부로 "누가
커밋에 성공했나" 를 세어 대조하는 것이다.

### 25차 — 참말이던 하드코딩을 거짓이 될 수 있는 규칙으로 바꿨다

```
중(中) 이상   없음        ← 일곱 회차째
하            1 (CE-25-A — 24차 수정이 만들었다)
위생          2 (죽은 폴백 재발 · 감사 누락)
```

**열여덟 번째다. 네 회차 연속으로 직전 수정이 다음 구멍을 만들었다.**

22차까지 driveLoop 닫기 분기는 `'failed'` 하드코딩이었고 **그게 참말이었다.** 24차가
그것을 `reachedPhase()` 라는 **규칙**으로 바꿨는데, 그 규칙은 "좌표 epoch == 내 목표
epoch" **동일성만** 보고 `payloadDigest` 를 안 봤다. 그래서 내 전환이 전부 포기됐는데
남이 같은 epoch 을 **다른 payload** 로 채우면 내 저널이 `activated` 로 닫힌다.

**epoch 은 좌표의 번지이고 digest 는 거기 있는 것이다.** "내가 옮겼다" 를 번지만으로
판정하면 남이 같은 번지에 다른 것을 놓은 경우를 못 가른다. 이번 회차의 교훈은
일반화된다 — **하드코딩을 규칙으로 바꿀 때는, 하드코딩이 참말이던 이유를 규칙이
보존하는지 확인해야 한다.** 참말의 근거(`every` 라서 activated 평면이 있을 수 없었다)가
사라졌는데 규칙은 그 근거를 안 담았다.

#### 계측기가 구현과 같은 착란을 공유하고 있었다

**P10 도 못 잡았다.** 진실 술어가 똑같이 epoch 동일성으로 세고 있었기 때문이다.
즉 속성이 재려는 대상과 **같은 가정**을 쓰고 있었다.

**계측기가 구현과 같은 가정을 쓰면, 그 가정이 틀린 것은 영원히 못 잡는다.**
넷 중 셋(불변식·모델·스윕)이 구현에서 유도된 것이라 이 병에 취약하다 — 표면 해시만
바깥에서 온다. 이번엔 P10 의 술어에 digest 를 넣어 갈랐지만, **같은 병이 다른 속성에도
있는지는 안 봤다.**

#### 그리고 규칙을 쓴 커밋 안에서 그 규칙을 어겼다

24차에 `failAll` 의 죽은 폴백(`?? (committed === 0 ? ...)`)을 지우면서 **"못 오는 길에
방어를 세우지 말고 못 온다는 사실을 적어라"** 는 규칙을 세웠다. 그런데 **같은 커밋의
수정 ① 안에 새 죽은 폴백(`?? 'failed'`)을 써 넣었다.** 25차가 실측으로 짚었다 —
`'activated'` 로 뒤집어도 558 개 전부 초록이다.

24차가 진단한 병형("규칙을 세우고 일부 자리에만 적용")의 **축소판을 진단한 그 자리에서
저질렀다.** 지웠다.

#### 【제안】 부류를 닫으려면 좌표가 **누가 놨는지** 기억해야 한다

다섯 번 재발한 것(§3.4)의 뿌리는 하나다. **"내가 옮겼는가" 를 물을 독립적인 근거가
없다.** 좌표는 `{activationEpoch, membershipRevision, payloadDigest}` 뿐이라, 매번
**닮았으니 내가 옮겼다** 고 추론한다.

- 22차 R2 — 모수를 호출자 봉투에서 셌다
- 23차 CE-A — 같은 것
- 24차 CE-24-A — 하드코딩 전제가 깨졌다
- 25차 CE-25-A — 번지만 보고 내용을 안 봤다

매번 **닮음의 기준을 하나씩 더 얹어** 막았다(평면 집합 → epoch → digest). 그건 부류가
아니라 인스턴스를 닫는 것이고, 다음 회차에 또 다른 닮음의 구멍이 난다.

**제안**: `commit` 이 좌표를 쓸 때 **누가 놨는지**를 같이 적는다.

```ts
s.planes[op.plane] = { ...op.target, payloadDigest: op.payloadDigest,
                       by: { operationId, transitionId, leaderToken } };
```

그러면 `reachedPhase()` 가 추론을 그만두고 **읽는다** — `at.by?.transitionId === j.op.transitionId`.
닮음의 기준을 몇 개 얹든 못 가르는 경우가 남지만, 서명은 안 남는다.

**비용: 표면이 움직인다.** `PlaneAck = PlaneState & {...}` 이고 `PlaneAck` 은 내보내는
타입이라, 좌표에 필드를 더하면 **동결 카운터가 0 으로 리셋된다.**

**그래서 여기 적어만 두고 하지 않았다.** 계약을 옮기는 것은 이 시리즈가 검수 판정을
받아 온 종류의 결정이고, 재현된 반례(CE-25-A)는 digest 조임만으로 이미 닫혔다.
26차가 판정할 물음: **인스턴스를 다섯 번 닫은 것과 부류를 한 번 닫고 카운터를 잃는 것,
어느 쪽이 싼가.**

#### 감사가 전수가 아니었다

24차는 "범위를 주장하는 자리는 셋 + 넷째" 라고 셌는데 그건 `closeJournal` **호출부만**
센 것이었다. `write` 로 직접 적는 자리가 둘 더 있다(`reload_observed` 판정 ·
`stuck.length === 0`). 지금은 모수를 저널에서 가져와 **동치**라 결함이 아니지만,
"전 자리를 감사했다" 는 말은 틀렸다. 규칙 밖이라고 주석에 적었다.

### 24차 — 카운터는 3 을 채웠는데, 카운터가 엉뚱한 것을 재고 있었다

```
중(中) 이상   없음        ← 여섯 회차째
하            1 (CE-24-A — 23차 수정이 만들었다)
```

**열일곱 번째 구멍이고, 23차 수정이 만들었다. 22 → 23 → 24, 세 회차 연속이다.**

22차까지 `closed` 는 `every` 라서 참이면 `activated` 평면이 있을 수 **없었고**, 그래서
그 분기의 `closeJournal(j.op, 'failed')` 하드코딩이 항상 참말이었다. 23차가 CE-B 를
고치며 `includes('aborted')` 로 혼합 종단을 끌어들여 **그 전제를 깼는데 하드코딩은 그대로
뒀다.** §3.4 계열 **다섯 번째 재발**이다.

가장 뼈아픈 것: **23차가 판정을 한 곳으로 모으는 규칙을 만들어 놓고 자기가 손댄 자리를
빠뜨렸다.** 종단을 적는 자리는 셋인데 둘만 모았다. **규칙을 세우는 것과 전 자리에
적용하는 것은 다른 일이고, 안 한 쪽이 재발한다.**

전 자리를 감사했다. **네 번째 자리가 있다** — 예산 소진 경로다. 일부러 안 모았다:
그게 13차 ③ 이고 여섯 번 재현에 실패해 ▲ 로 남아 있다. 재현 없이 고치면 스윕이 못
지켜 다음 회차에 조용히 되돌아온다. 주석에 적어 뒀다 — **모았다고 말하면서 안 모은
자리를 남기지 않기 위해서다.**

#### 생존 뮤턴트의 길을 찾았다 — 17차 교훈이 또 맞았다

23차가 분류를 미뤄 둔 `await this.failAll(...)` 은 **동치도 도달 불가도 아니었다.**
남이 같은 (평면, epoch) 를 점유해 양 평면이 `slot_taken` 으로 막히면(종단이 아니라
`closed` 가 안 잡는다) 거기가 유일한 출구다. 지우면 `published` 에서 무한히 돌아
HARD_LIMIT 예외로 튀고 **실행권이 남아 영구 봉쇄**된다 — 등급으로 상(上)이 될 자리였다.
스윕이 "검사되지 않은 행동" 으로 정확히 짚었고, 이제 conformance 가 든다.

나머지 하나(`finishOperation(j.op, stuck)`)는 **동치**로 판정했다 — 다음 반복의 종단
조기 반환이 같은 집합으로 같은 호출을 멱등하게 한다. 다만 이건 분석이고 실측이 아니다.

#### 그리고 스윕이 이번 회차의 수정을 곧바로 짚었다

```
519 개 생성 · 110 개 실행 (seed 24024) · 죽었다 90 · 살았다 20 · 81.8%
```

생존자 중 하나가 **23차에 내가 넣은 줄**이었다.

```
src/dp/apply.ts:662   const phase = this.agent.reachedPhase() ?? (committed === 0 ? ... )
```

뒤집어도 아무 테스트가 안 죽는다. 당연하다 — `failAll` 은 저널을 읽은 호출자에게서 `j` 를
받으므로 `reachedPhase()` 가 `undefined` 를 줄 수가 없다. **폴백은 방어가 아니라 죽은
코드였고, "세는 자리를 하나로 모았다" 는 말을 반쯤 거짓으로 만들고 있었다.** 지웠다.

대신 `if (phase === undefined) return` 을 두지 않았다 — 그건 **또 하나의 도달 불가
분기**라 다음 스윕이 똑같이 짚는다. **못 오는 길에 방어를 세우는 대신 못 온다는 사실을
적는다.** 이번 회차에서 배운 것 중 이게 제일 일반적이다.

#### 【중요】 동결 카운터가 엉뚱한 것을 재고 있다

24차 판정은 이랬다: **카운터 3 자격은 있으나 동결 시작은 부결.** 이 둘이 갈린다는 것이
계측기의 문제를 드러낸다.

13차 기준은 "여러 적대적 회차 동안 **표면**이 안 변할 것" 이었다. 표면은 19차부터 여섯
회차째 안 변했다. 그런데 **같은 여섯 회차 동안 매 회차의 수정이 다음 구멍을 만들었다.**
카운터는 초록인데 초록인 이유가 틀렸다 — 계약이 안정돼서가 아니라 **결함이 전부 내부라서**
표면을 안 건드렸을 뿐이다.

그래서 **두 번째 눈금을 세운다.**

```
표면 안정          6 회차 (19~24차)     ← 13차가 준 기준. 초록.
수정 유도 결함 연속 3 회차 (22·23·24)   ← 실제로 물어야 할 것. 빨강.
```

동결이 뜻하는 것은 "이 계약 위에 다른 것을 쌓아도 된다" 이고, 그 근거는 **수정이 새
결함을 안 만드는 상태**다. 표면 무변동은 그것의 대리 지표일 뿐인데, 지금은 대리 지표만
초록이고 본체가 빨강이다. **대리 지표를 보고 동결하면 13차가 막으려던 바로 그 일이
일어난다.**

정지 조건을 다시 쓴다: **수정 유도 결함 연속이 0 인 회차를 최소 한 번 볼 것.**
표면 카운터는 계속 세되, 그것만으로는 동결하지 않는다.

### 23차 — 정지 조건은 유지, 그러나 서사는 하나 거둬야 했다

22차의 판정("정지 조건 충족")은 이 회차에도 **유지된다** — 중(中) 이상이 또 없었다.
그런데 22차의 수정 하나(R2)가 곧바로 **열여섯 번째 구멍**을 만들었다.

```
중(中) 이상   없음        ← 다섯 회차째
중하          1 (CE-B — R4 가 창의 절반만 닫았다)
하            1 + 문서   (CE-A · 눈금 낡음)
```

**CE-A — §3.4 의 네 번째 재발, 그리고 세 번째 재발의 수정이 만들었다.**

R2 는 `moved.length === all` 을 옳게 썼는데 `all` 을 **호출자가 넘긴 봉투**에서 셌다.
한 평면짜리 봉투가 오면 `all === 1` 이라 나머지가 옛 세대여도 "전부 넘어갔다" 가 된다.
저널을 닫을 자격(`ownsJournal`)은 id 와 토큰만 보고 **평면은 안 본다** — 좁은 봉투가
넓은 저널을 닫는다. 부분 봉투가 오용이 아니라는 증거는 바로 위 줄에 있었다:
`releaseHolderSlots` 주석이 12차 ④ 를 두고 **"넘어온 op 가 한 평면만 담고 있으면"**
이라고 적어 실전 입력으로 다룬다.

자리를 하나씩 고쳐 온 것이 네 번 재발한 이유라 **한 규칙으로 끊었다.** `reachedPhase()`
는 평면 집합을 **인자로 받지 않는다** — 저널에서 가져온다. 넘길 수가 없으니 잘못 넘길
수도 없다. `ownsJournal()` 이 "무엇을 비교하나" 를 모은 것과 같은 수법이고, 이번엔
"무엇을 세나" 다. `abortConfig` 와 `failAll` 이 같은 규칙을 쓴다.

**CE-B — R4 는 창의 절반만 닫았다.** `closed` 가 **전 평면** 종단을 요구하는데 abort 는
평면마다 도는 직렬 쓰기라, 루프 **도중**에 죽으면 `{http:'aborted'}` 하나만 남는다.
혼합 종단이 `every` 를 못 통과해 복구가 지나가며 **포기한 세대로 HUP 을 두 번** 보내고
남은 평면을 마저 커밋했다. `aborted` 는 하나로 족하게 했다 — 그걸 쓰는 자리는
`abortConfig` 와 고아 청소(전 평면을 한 쓰기에 적는다)뿐이라 **하나만 보여도 포기**다.
`failed` 는 `failAll` 이 평면별로 남기므로 전부일 때만 센다.

#### 네 번 재발한 진짜 이유 — 무대가 아니라 속성이 없었다

모델에는 **부분 봉투 abort 시나리오가 이미 있었고** 매번 그 자리를 지나갔다. 그런데
P0~P9 중 **저널의 말이 참인지 보는 속성이 하나도 없었다.** 넷 다 손으로 찾은 것이
우연이 아니다 — **속성 없는 축은 무대가 아무리 넓어도 안 잡힌다.**

그래서 **P10**(어디까지 갔는지 적은 말이 좌표와 맞는다)을 세웠다. 검출력을 쟀다.

```
CE-A 모양 (부분 봉투가 "전부")        → 기존 시나리오가 잡는다  ✅
21차 CE-C 모양 (넘어간 평면을 숨김)   → 잡는다                 ✅
22차 R2 모양 (전부인데 "부분")        → 못 잡는다 — 그 무대가 없다
```

**재지 않고 넣었으면 자구로만 이행한 것이다.** 22차가 P8 에서 그걸 배웠으므로 못 잡는
쪽도 그대로 적었다. R2 방향은 conformance 가 든다.

#### 스윕 86.4% — 그리고 생존자가 "우연한 방패" 를 확인해 줬다

```
519 개 생성 · 110 개 실행 (seed 23023) · 죽었다 95 · 살았다 15 · 86.4%
                                        직전 회차 75.7% → 이번 86.4%
```

살아남은 15 개 중 **둘이 같은 자리다**.

```
src/dp/agent.ts:908   && stale.op.operationId === op.operationId      (=== → !==)
src/dp/agent.ts:909   && stale.op.transitionId === op.transitionId    (=== → !==)
```

`reserveAll` 의 고아 청소다. **신원 비교를 뒤집어도 아무 테스트가 안 죽는다** — 22·23차가
말한 `completed` 캐시의 우연한 방패가 여기 그대로 찍혔다. 방패가 없어지면(가지치기)
이 두 줄이 곧바로 반례가 된다. 그래서 경고를 `completed` 필드 주석에 붙였다 —
STATUS 에만 두면 가지치기를 짤 사람이 여는 파일에는 아무것도 안 적혀 있게 된다.

나머지 생존자 중 눈여겨 둘 것:

```
src/dp/apply.ts:486   await this.failAll(j, '모든 평면이 더 진행할 수 없다');   ← 통째로 지워도 산다
src/dp/apply.ts:579   await this.agent.finishOperation(j.op, stuck);
```

**아직 분류하지 않았다.** 반례가 없는 것인지 도달 불가인지 길을 찾아봐야 안다 —
17차에 "도달 불가" 로 결론냈다가 뒤집힌 적이 있으므로 결론을 미뤄 둔다.

### 22차 — 정지 조건을 충족했다

21차가 정지 조건을 줬다: **"한 회차 더 받아 기능성 축에서 중(中) 이상이 안 나오면 표면
동결을 시작해도 된다."** 22차가 그 회차였고 **충족**이다.

```
중(中) 이상   없음
중하          1 (R4 — abort 크래시 창에서 복구가 포기 세대를 게시)
하            3 (라벨 거짓 둘 · 좌표축/서빙축 분리)
```

**그리고 열다섯 번째 구멍이 안 났다.** 21차 수정 넷을 전부 공격했는데 뚫리지 않았다 —
**열네 회차 만에 처음**이다.

> **다음 회차가 이 문장을 반쯤 거둬 갔다.** 22차 수정 자체(R2)가 곧바로 **열여섯 번째**를
> 만들었다(23차 CE-A). 즉 끊긴 것은 한 회차뿐이다. 이 문장을 그대로 두면 문서가 코드보다
> 낙관적이 되고, 그건 이 시리즈가 반복해서 잡아 온 병형이라 여기 붙여 둔다.

등급의 기준은 21차가 세웠다: 상태 오염·세상 되감김·영구 봉쇄 = 상 / 리더 교체 중 정당한
전환 오살, 진단이 거짓이라 운영자가 갈 곳 없음 = 중 / 자가 치유되는 기능성 상처 = 중하 /
플래그·phase 만 거짓 = 하. **네 회차째 상(上) 축의 반례가 없다.**

22차가 남긴 넷을 전부 처리했다.
- **R4** — 러너가 게시 앞에서 자기 전환의 `terminal` 기록을 확인한다. `abortConfig` 의
  직렬 쓰기 넷 중 마지막 직전에 죽으면 포기한 세대를 게시했다.
- **R2** — 전 평면이 넘어간 뒤 abort 하면 `activated` 로 닫는다. §3.4 계열의 **세 번째
  재발**이었다(13차 ③ · 21차 CE-C · 이번).
- **P8** — 21차에 넣은 `rejections` 절이 **독립 검출력 0** 이었다(조건이 기존 절의
  진부분집합). 자구로만 이행한 것이라 정직하게 고쳐 적었다.
- 이 문서의 회차 표기.

### 세 층 중 어디서 값이 나오는가 — 재봤다

버그를 하나씩 넣고 불변식을 켠 채와 끈 채로 돌렸다.

```
                    conformance+unit      전체(모델 포함)
 intent 가드 제거      ON 0 / OFF 0         ON 1 / OFF 1    ← 모델만 잡는다
 고아 재획득 제거      ON 0 / OFF 0         ON 1 / OFF 1    ← 모델만 잡는다
 finalizer 도착 검사   ON 5 / OFF 5         ON 1 / OFF 1    ← conformance 가 잡는다
```

**지금까지 시도한 어떤 버그도 불변식만이 잡은 것은 없다.** 그러니 "불변식이 잡았다" 고
말하면 안 된다. 정확히는 —

- 값이 나오는 곳은 지금 **모델**이다. 위 둘은 모델이 아니면 아무도 못 잡는다.
- 불변식은 **쓰기 지점에서** 터져 진단이 정확하고 465 개 테스트 전부에 걸린다.
- 그리고 **앞으로의 변경**을 막는다 — 지금 코드의 버그가 아니라 미래의 것을.

없앨 이유는 아니지만, 이 층을 근거로 "상태 축은 검사됐다" 고 넓게 읽으면 안 된다.
I7 은 아예 이빨이 없다고 이미 적어 뒀고, 이 측정은 그것이 I7 만의 문제가 아님을 말한다.

### 모델이 못 잡을 때는 이유가 둘 중 하나다

15차 수정 둘을 겨냥해 시나리오를 넣었는데 뮤테이션이 안 잡혔다. 원인이 서로 달랐다.

- **무대가 넓다.** `pendingEpochs` 버그는 "http commit → abort 전체 시퀀스 → stream
  commit" 이라야 나오는데 그 길이가 표본에 안 들어온다. **준비 단계에서 http 만 넘겨
  놓아** 무대를 좁혔다. 교차를 고르는 것이 아니다 — 누가 언제 끼어드는지는 스케줄러가 정한다.
- **속성이 없다.** reconcile lease 의 토큰 검사를 빼도 아무것도 안 깨졌다. 게시와 HUP 은
  **상태에 안 남고 바깥에서만 보이기** 때문이다 — P1~P5 는 전부 store 를 본다.
  **P6** 을 만들었다: 낡은 리더는 되돌릴 수 없는 연산을 내지 않는다.

둘 다 이제 잡힌다. 그리고 **preemption bounding** 을 넣어(문맥 전환 2·3 회로 묶는다)
같은 예산으로 더 촘촘히 본다 — 옛 버그 넷을 잡는 테스트 수가 1 개에서 2~3 개로 늘었다.

### 순서를 바꿨다 (11차 앞에서)

세 라운드 연속 같은 실수를 했다 — **고치고 나서 테스트를 썼다.** 그러면 이미 통과하는
테스트를 쓰게 되고, 뮤테이션을 돌려야 비로소 드러난다. 10차는 더 나쁜 것도 찾았다:
`review7` 픽스처를 **코드에 맞춰 고쳐** 놓고 통과시킨 흔적이 커밋 diff 에 남아 있었다.

원인은 습관이 아니라 순서다. `TESTS.md` 의 "검수 반례를 다루는 순서" 에 적었다.

```
1. 반례를 먼저 재현한다 — red 를 눈으로 본다
2. conformance 로 옮긴다 — 여전히 red 여야 한다
3. 그다음에 고친다
4. 뮤테이션으로 변별력을 확인한다 — 안 잡히면 테스트가 잘못된 것이다
5. 못 고친 것은 못 고쳤다고 적는다
```

**"해소" 라고 적으려면 뮤테이션이 잡아야 한다.** 그게 유일한 기준이다.

### 그래서 생긴 습관

- **뮤테이션이 기본이다.** 일부러 코드를 망가뜨려 테스트가 잡는지 본다. 안 잡히면
  테스트가 잘못된 것이다 — 실제로 여러 번 그랬고, 그때마다 테스트를 고쳤다.
- **거짓 신호부터 없앤다.** 통과하는데 아무것도 보증하지 않는 테스트는 없느니만 못하다.
- **못 증명한 것은 못 증명했다고 적는다.** fsync 순서, 락 생성 원자성, `nginx -t` 미수행
  배치 — 코드에는 있지만 검증되지 않았다고 문서·주석·게이트에 적혀 있다.
- **문서와 코드가 갈라지면 테스트가 깨진다.** `review6-design-abi.test.ts` 가 §6.2 단계
  표와 §9.2 드라이버 계약을 코드와 대조한다.

---

## 4. 열려 있는 것

### 9차 검수 반례 — 방향을 바꿔 처리했다

**막는 것을 그만두고 수렴시킨다.** 외부 효과는 취소할 수 없고 nginx 는 리더 토큰을
모른다. 검사와 착지 사이에는 어떤 식으로든 틈이 남는다 — 그걸 아홉 번 확인했다.

| | 문제 | 처리 |
|---|---|---|
| ① | `lease` 가 타입이 아니라 주석 | ▲ 창을 좁히는 장치로 남기고, **수렴이 덮는다** |
| ② | 같은 ID 로 다른 세대 → 조용한 거짓 성공 | ✅ 정본 튜플에 세대·digest |
| ③ | 실행권 없이 `lease` 가 유효 | ✅ holder 없으면 무효 |
| ④ | 불투명 저장소가 불투명하지 않다 | ✅ `{version, payload}` 봉투 |
| ⑤ | 부분 활성화 뒤 `abort` 중단 | ✅ 오퍼레이션 단위 정리 |

**수렴이 무엇인가.** 게시할 때 누가 게시했는지 함께 적고(`PublishRecord`), 관측이
소유자를 답하고(`PublishedState`), 활성화를 인정하기 전에 게시가 아직 내 것인지 다시
본다. 늦게 착지한 옛 게시는 막아야 할 것이 아니라 **관측되고 덮이는 것**이 된다.
`status()` 가 갈라짐을 드러내므로 컨트롤 플레인이 판단할 수 있다.

### 13차 검수 — 처리했다 (셋 실재 · 둘 미처리)

| | 처리 |
|---|---|
| ① 옛 리더의 reconcile 이 신임을 덮음 | ✅ 기준의 토큰이 낮으면 손대지 않고 `diverged` |
| ② partial 을 converged 로 판정 | ✅ 기준을 오퍼레이션 단위로 — 전 평면 commit 때만 승격 |
| ④ 소진 뒤 관측 없이 converged | ✅ 마지막에 한 번 관측하고 답을 만든다 |
| ③ 예산 초과가 부분 커밋을 숨김 | ❌ 미재현. `partial_exhausted`/`partial=true` 로 동작 |
| ⑤ lease 를 타입이 강제 못 함 | ▲ 사실이다. 11차에 이미 적었고 규약으로 남는다 |

**①④ 를 처음에 "재현 안 됨" 이라고 잘못 판정했다.** 시나리오가 약했던 것이다.
재현 실패는 반증이 아니다 — 그 자리의 코드가 왜 안전한지 말할 수 없으면 아직 모르는 것이다.

### 14차 검수 — 지목 하나가 맞았다 (판정은 못 받았다)

공급자 필터에 걸려 exit 1 로 죽었다(116k 토큰 소진). 그전에 코드 추적으로 한 후보를
지목했고 **그게 진짜였다** — `recover()` 의 종단 경로가 `promote` 를 안 넘겨서, 13차 ②
수정이 만든 구멍이었다. 재현하고 고쳤다(e280e93). 프롬프트에서 걸린 부분을 빼고 재실행 중.

### 12차 검수 — 처리했다

| | 처리 |
|---|---|
| ① 예산 정리가 남을 훼손 | ✅ 끊긴 러너는 **자기 것만** 정리한다 |
| ② 기준 없는 늦은 효과 | ✅ 게시 앞에 의도를 남기고 `dirty` 로 드러낸다 |
| ③ reconcile 이 안전하지 않음 | ✅ 기준이 바뀌면 처음부터 다시 본다(유한) + 예산 |
| ④ singleton abort 가 고아를 남김 | ✅ 실행권이 잡은 평면 전부로 정리, 토큰까지 확인 |
| ⑤ partial 소진 뒤 복구 붕괴 | ✅ `partial_exhausted` 종단 신설 |

**②에서 한 가지를 분명히 했다.** 활성화하지 못한 게시를 무엇으로 되돌릴지 DP 는 판단할
수 없다 — 그 게시가 좋은 것인지 알 근거가 없다. 되돌리지 않고 `dirty` 로 드러내고
컨트롤 플레인이 정하게 한다.

### 11차 검수 — 처리했다

| | 처리 |
|---|---|
| ① reconcile 이 포인터만 | ✅ 활성 상태까지 확인하고 HUP 을 보낸다. apply 와 같은 큐 |
| ② `planes` 가 delta 가 아님 | ✅ **설정 apply 는 항상 두 평면을 선언한다** |
| ③ manifest planes 변조 | ✅ digest 가 planes 를 덮는다 |
| ⑤ deadline·취소 부재 | ✅ 부작용에 예산. 끊으면 교착이 풀린다 |
| lease 강제 | ❌ **타입이 못 한다** — TypeScript 는 인자를 덜 받는 함수를 대입시킨다 |

### 10차 검수 — 처리했다

| | 처리 |
|---|---|
| ① 종단 뒤 재검증 없음 | ✅ `reconcileConfig()` — 활성화가 끝난 뒤에도 갈라짐을 되돌린다 |
| ② 세대 ↔ 평면 미결박 | ✅ 렌더러가 구성한 평면을 답하고 manifest 가 기록. 게시 전 대조 |
| ③ 낡은 abort | ✅ 실행권 해제가 토큰까지 본다 |
| ④ `publishedByMe` 토큰 | ✅ 같은 id 라도 옛 토큰의 기록은 내 것이 아니다 |
| ⑤ `Effects` ABI | ▲ lease 를 **필수 인자**로, owner/current 에 fsync. **deadline·취소는 없다** |

**평면 결박이 내 e2e 를 잡았다.** 세대는 http·stream 을 함께 구성하는데 오퍼레이션은
http 만 선언하고 있었다 — `plane_mismatch` 로 막혔다. 정확히 그 검사가 잡으려던 것이다.

**그리고 내가 테스트를 코드에 맞춰 고친 것이 드러났다.** 수렴 방식으로 바꾸면서
`review7` 픽스처를 "옛 리더가 게시했다" 에서 "내가 이미 게시했다" 로 바꿔 놓고
"다시 게시하지 않는다" 를 통과시켰다. 커밋 diff 가 증거다. 되돌렸다.

### 그 밖

- ~~**`exclusiveApply` 교착**~~ — **갚았다. 그런데 이미 갚혀 있었고 기록이 낡았을 뿐이다.**
  조건절("`Effects` 에 타임아웃이 없으면")이 11차 반례 ⑤ 이후로 성립하지 않는다 —
  부작용마다 예산이 붙고, 넘기면 `driveInner` 가 run 을 통째로 끝낸다. 재 봤다:
  매달린 부작용 뒤에 줄 선 호출이 들어간다. **대조군**(예산을 크게 준 러너)에서는
  같은 자리가 잠긴다 — 그것이 이 측정이 실재함의 증거다.

  재는 것은 **경과 시간이 아니라 큐가 풀리는가** 다(18차에 "마감의 효과" 대신 경과
  시간을 재다가 뮤턴트를 놓쳤다).

  **가는 길에 두 번 헛발을 짚었다.** ① 러너가 큐에 들어가기 **전**에 줄을 서서 아무것도
  안 재면서 초록이었다 — 대조군이 잡았다. ② 픽스처가 한 평면만 선언해 `run` 이 부작용에
  닿기도 전에 거부됐고, `catch` 가 그걸 삼켜 테스트가 타임아웃으로 죽었다.
  **대조군이 없었으면 ①을 통과로 셌을 것이다.**

- **`effectTimeoutMs` 를 드라이버 표면에서 못 바꾼다** (새로 발견) —
  `LocalDataplaneDriver.create` 는 `{store, effects}` 만 받는다. 운영자가 예산을
  조정할 길이 없고 테스트도 `ApplyRunner` 를 직접 세워야 한다. 표면을 움직이는
  변경이라 여기 적어만 둔다.
- **fsync 순서 · 락 생성 원자성** — **절반 갚았다.** 전원 차단이 필요하다고 적어 놓고
  **fault injection 없이 잴 수 있는 부분을 안 재고 있었다.** 이 코드가 주장하는 것은
  물리적 내구성이 아니라 **syscall 의 순서**다:

  ```
  내용 fsync → rename → 디렉토리 fsync
  락은 완성된 파일을 link 로 한 번에 건다 (wx 로 만들고 나중에 채우지 않는다)
  ```

  `node:fs` 를 감싸 호출 순서만 기록해서 잰다 — 프로덕션 코드는 한 줄도 안 바꿨다.
  뮤테이션으로 셋 다 갈리는 것을 확인했다: 내용 fsync 를 rename 뒤로 옮기면 잡고,
  디렉토리 fsync 를 빼면 잡고, link 뒤에 쓰기를 넣으면 잡는다.

  **못 잰 절반은 그대로 남는다** — 그 순서가 실제 디스크에서 뜻대로 되는가(배리어,
  파일시스템 구현, 전원 차단). 그건 여기서 못 잰다. **순서가 옳다는 것과 내구성이
  보장된다는 것은 다르다.**
- **lease 강제 불가** — 타입으로 못 막는다. 관례이고, 정합성은 수렴이 맡는다.
- **49차 — 새 런타임 반례 0. 그러나 게이트가 본체를 물어뜯고 있었다**

  **연속 카운트 0 → 1.** 49차가 여러 축으로 공격했고 **새 상·중급 런타임 반례를 못
  만들었다.** 48차 수정(캐시 키 토큰)의 현행 세계 의미론이 계약과 정합함이 실측으로
  확인됐고, "fence 를 건너는 정당한 재시도" 는 **원래 없던 경로**라 멱등성 회귀도 없다.

  #### 그러나 내가 만든 게이트에 넷이 나왔다 — 계측기 결함 일곱 번째

  ```
  본체 오염     지운 src 파일이 작업 트리와 인덱스에 부활한다
  작업물 파괴   미커밋 src 수정이 소리 없이 사라진다 — verify.sh 가 기본으로 돌린다
  신규 파일     checkout 이 새 파일을 못 지워 정당한 커밋을 오차단
  근거 없는 none  맨 `none` 도 통과 — 문서가 요구하는 형식조차 기계가 안 봤다
  ```

  **13차에 스윕이 정확히 이 사고를 냈다.** 본체를 제자리에서 변이시키다 커밋에 뮤턴트가
  섞였고, 그때 배운 것이 *"도구가 본체를 건드리는 한 사고는 반드시 난다"* 였다.
  **그 교훈이 이 레포에 적혀 있는데 같은 모양을 만들었다.** 별도 worktree 로 옮겼고
  미커밋 작업이 살아남는 것을 실측했다.

  **못 잡는 것도 게이트 안에 적었다**: 새 심볼을 만들고 그 심볼만 만지는 테스트를 핀으로
  걸면 부모에서 **무조건** 빨갛다. **기계는 "빨간가" 만 알고 "무엇을 지키는가" 는
  모른다** — 그 축은 검수가 본다.

  #### 공허한 단언을 같은 편집에서 재범했다

  48차에 `ledger` 단언을 두 포맷으로 고치며 그 함정을 주석에 적어 놓고, **세 줄 아래
  `cacheAlive` 를 같은 함정에 빠뜨렸다** — 키에 토큰을 넣었으니 옛 포맷 조회는 이제
  **절대** 안 걸린다. 49차가 짚었다.

  #### 그리고 레거시 창의 방향을 또 안 쟀다

  44차에 종단 원장의 창을 두고 "방향을 재라" 는 규칙을 만들었는데(45차에 안 재서
  뒤집혔다), 48차에 캐시 키를 바꾸며 **그 절차를 하나도 안 했다.** 49차가 재 줬다 —
  **비관·정직 방향**이라 반례는 아니다. 적었다.

- **48차 — 이름 원장은 둘인데 하나만 고쳤다 (CE-48-A, 중급)**

  43차 검수의 처방은 *"이름 원장에 신원을 넣어라"* 였고, 44차에 그것을 **종단 원장에만**
  적용했다. **이름 원장은 둘이다** — 종단(`terminal`)과 멱등 캐시(`completed`).

  그래서 45차에 적은 계약(*"신임 리더가 같은 이름을 다시 내면 DP 는 통과시킨다"*)이
  **현행 세계에서 거짓**이었다. 옛 전환의 캐시 기록이 남아 있는 한 신임의 같은-이름
  재발급이 `tuple_mismatch` 로 **결정적으로 거부**된다. 레거시 창이 아니라 **현행
  세계**이므로, 47차의 *"본체 부류는 닫혔고 잔여 표면이 레거시 창으로 좁혀졌다"* 는
  판정은 **기각**이다.

  "부류를 이름 붙이고 한 자리만 고친다" 의 재연 — 이번엔 **원장이 둘인 것을 안 셌다.**

  #### 계약을 바꾸면 그 계약을 핀하던 테스트도 같이 바뀐다

  캐시 키에 토큰을 넣자 34차 B 의 테스트가 갈렸다 — 그 테스트가 재던 `tuple_mismatch`
  는 **이름 단위 시절의 옳은 답**이었다. 지금은 통과가 옳다. **계약을 바꾸고 핀을
  안 바꾸면 둘 중 하나가 거짓말한다.**

  #### 그리고 계약 핀이 픽스처 통과였다

  45차에 계약을 못박는다며 넣은 테스트가 **재발급을 실행하지 않고** `terminalOf ===
  undefined` 만 봤다. 그 사이 두 번째 원장이 실제 재발급을 막고 있었고, **그 계약이
  거짓인데 테스트는 초록이었다.** 실제로 재발급하게 고쳤고, 이제 캐시 키에서 토큰을
  빼면 갈린다.

  #### "이주는 불가능하다" 도 과잉이었다

  47차에 DESIGN.md 에 그렇게 적었는데 — **키에서는 못 되살리지만 상태에서는 되살린다.**
  `completed` 의 canonical 과 저널의 op 가 둘 다 토큰을 들고 있고, 44차 이전은 이름
  단위 봉인이라 대응이 모호하지 않다. **조건부 이주가 가능하다.** 다만 그 재료는
  `completed` 가지치기가 점진적으로 파괴한다 — **닫히는 창**이라고 적었다.

- **47차 — HEAD 에 신규 런타임 반례 0. 그런데 내 수정에 재현물이 0 이었다**

  47차가 여러 축으로 공격했고 **HEAD 에서 새 상·중급 런타임 반례를 못 만들었다.**
  그러나 **46차의 런타임 수정 둘 다 커밋된 재현물이 0 이었다** — 직전 커밋에서 593 이
  전부 초록이었고, 46차 커밋은 src 와 문서만 바꿨다. **내가 40차에 세운 규칙("검증물은
  커밋된 산출물이어야 한다")을 또 어겼다.**

  47차가 그 재현물을 만들었고 편입했다. **두 다리가 각각 갈린다** — 되돌리면 하나씩
  빨개진다. 46차 수정은 이제 **사후 입증**됐다.

  #### 내가 못 연 것을 남이 열었다 — 그 이유가 기록될 값이다

  46차의 뒤집기를 나는 **신임 토큰**으로 시도했고, 그러면 `assertLeader` 가 먼저 막아
  하류 증상이 안 보인다. 요체는 **동일 토큰 창**이었다 — `transitionKey` 주석이 스스로
  *"`assertLeader` 가 못 막는 동일 토큰 경우"* 라고 적어 둔 바로 그 지점이다.
  **내가 쓴 주석이 답을 갖고 있었는데 내가 안 읽었다.**

  #### 계약을 적은 자리에 예외를 안 적었다

  45차에 *"신임 리더가 같은 이름을 다시 내면 DP 는 통과시킨다"* 고 DESIGN.md 에
  적었는데 — **레거시 기록에서는 거짓이다.** 옛 키에는 토큰이 없어 조회가 **이름
  단위**로 보고, 옛 `aborted` 가 있는 이름은 **어떤 토큰의 재발급도 영구히 거부**된다.
  **이주는 불가능하다**(옛 키에서 토큰을 되살릴 수 없다).

  46차가 "영구" 를 확정하고도 이 예외를 계약 옆에 안 적었다. **계약을 적은 자리에
  예외를 안 적으면 그 계약은 절반만 참이다.** 적고 못박았다.

  #### 【판정】 런타임 축은 수렴, 기록 축은 아니다

  47차가 41~46차를 세었다.

  ```
  런타임 반례   6/6 회차 각 1 건 — 그러나 성격이 이동했다
                41~44차  본체 현행 세계의 신원 비교 부류
                45~46차  업그레이드·레거시 창 한정 (부류가 닫힌 뒤 가격표 오류에서만 난다)
  기록·계측     6/6 회차 동반 — 발생률이 줄지 않았다
  ```

  **런타임 축에서는 41차의 "수렴" 판정이 유지된다** — 본체 부류는 닫혔고 잔여 표면이
  레거시 창으로 좁혀졌으며, 47차의 공격도 그 밖에서 새 구멍을 못 열었다.
  **기록 축은 수렴하지 않는다.** 회차당 거짓 서술 발생률이 그대로다.

- **46차 — "넷을 모았다" 가 여섯이었다 (CE-46-A, 중급)**

  45차에 *"읽는 자리 넷을 `terminalOf` 하나로 모았다"* 고 적었는데 **여섯이었다.**
  남은 둘(고아 스윕의 가드 · `candidateArrived` 의 수동 조립 키)이 **레거시 세계에서
  옛 포맷을 못 본다.** 스윕 쪽은 이미 `activated` 로 끝난 평면에 `aborted` 를 덮어
  찍고, `terminalOf` 가 새 키를 먼저 보므로 **그 거짓이 참을 영구히 가린다.**

  **눈으로 세서 틀렸다.** 이번엔 `grep '\.terminal\['` 로 기계로 셌다 — 읽기가 한
  자리(`terminalOf`), 쓰기가 둘, 불변식이 하나다. **다섯 회차 동안 "다 셌다" 를 네 번
  틀렸고, 네 번 다 눈으로 셌다.**

  뒤집기 자체는 **내 손으로 재현 못 했다** — 46차가 재현했다고 적었고 기전은 코드로
  확인된다. **못 연 것이지 없는 것이 아니다** 라고 적었다.

  #### 그리고 "1 회성 창" 이 영구였다

  45차에 `legacyTerminalKey` 주석에 *"구버전 상태를 읽는 동안만 살아 있고 새 쓰기로
  덮이면 자연히 사라진다"* 고 적었다. **거짓이다** — 새 쓰기가 옛 모양을 안 만드므로
  **옛 키는 아무것에도 안 덮이고 영원히 조회된다.** 방향은 의도대로 비관이지만
  **"1 회성" 이라는 말이 틀렸다.**

  #### DESIGN.md 가 거짓말하고 있었다

  44차에 원장의 범위를 (전환, 토큰)으로 바꾸고 45차에 계약을 코드 주석에 적었는데
  **설계 문서는 안 고쳤다.** DESIGN.md 는 여전히 *"`aborted`·`failed` 는 **모든**
  replay 를 거부한다"* 고 말하고 있었다 — 45차 주석이 경고한 바로 그 위험("다음 사람이
  DP 가 막아 준다고 믿는다")이 **설계 문서 자체에 실현돼 있었다.** 고쳤다.

  #### 미처리를 원장에 안 적었다 — 또

  45차가 "다음 회차에 반드시 닫을 것" 넷을 줬는데 둘을 안 하고 **부채로 기록도 안
  했다.** 41→42차에 지적받은 바로 그 패턴의 재발이다. 규칙 ①의 보완("계측기가 지키던
  런타임 주장이 무너지면 적립을 막는다")에 따라 **이 회차는 클린으로 못 센다.**

- **45차 — ③ 부류 마감 게이트가 닫혔다. 그리고 내 가격표가 또 거짓이었다**

  **부류가 닫혔다** (45차 판정). "같은 이름의 두 신원이 한 칸을 다툰다" 는 채널이
  구조적으로 사라졌고 뮤테이션이 지킨다(키에서 토큰을 빼면 셋이 빨개진다).
  **4/6 회차 안에 닫혔으므로 ④ 상한 시계는 멈춘다.**

  **그러나 CE-45-A (중급)** — 44차에 창의 대가를 *"26차 `by` 의 창과 같은 부류이고
  방향도 같다"* 고 적었는데 **방향이 반대였다.** `by` 의 창은 **비관**(덜 인정)이고
  34차 캐시 창도 비관이었는데, 이 창은 **낙관**이다 — 토큰 없는 옛 키가 조회에 안 걸려
  **운영자가 포기한 전환이 업그레이드 뒤 좌표를 옮긴다.**

  **이 시리즈가 1 회성 창을 축 밖 한계로 승인해 온 근거는 매번 "방향의 안전" 이었다.**
  그 근거가 무너졌으므로 가격표 오류가 아니라 반례다. 비관으로 되돌렸다 — 조회가 옛
  키도 본다. 새 쓰기는 옛 키를 안 만들므로 CE-43-A 는 다시 안 열린다. **양쪽을 다 쟀다.**

  #### 계약 판정: 포기는 전환에 붙지 이름에 붙지 않는다

  44차에 키를 바꾸며 **원장의 뜻이 바뀌었는데 그것을 안 적었다.** "이 **이름**은 끝났다"
  에서 "이 **전환(토큰 포함)**은 끝났다" 로. 45차가 *"어느 쪽이 계약인지 적고 지키는
  쪽을 핀해라"* 고 요구했고, 적고 핀했다.

  **전환에 붙는다** — 이름에 붙이면 정당한 승계가 오살된다(CE-43-A). 옛 리더의 포기는
  **그 리더의 시도**에 대한 판단이고, 신임의 재발급은 **컨트롤 플레인의 결정**이다.
  **대가**: 운영자 abort 뒤 신임의 같은-이름 재발급을 DP 는 통과시킨다 —
  **그 게이트는 v0.2 컨트롤 플레인에 있어야 한다.** 안 적으면 다음 사람이 "DP 가 막아
  준다" 고 믿는다.

  #### 그리고 CE-44-A 에 커밋된 재현물이 없었다

  44차에 *"커밋 전후 대조로 확인했다"* 고 적었는데 **그 대조가 커밋 밖에 있었다** —
  43차 휴리스틱을 되살려도 626 이 전부 초록이었다. 45차가 만든 재현물을 편입했다.
  **"확인했다" 는 커밋된 산출물이어야 한다** 는 40차 규칙을 내가 또 어겼다.

- **44차 — 채널을 닫으니 세 반례가 한꺼번에 사라졌다**

  **내 43차 휴리스틱이 새 중급을 만들었다**(CE-44-A). *"예약이 살아 있으면 안 찍는다"* 는
  전제 — *"같은 이름의 슬롯이 다른 토큰으로 살아 있으면 승계된 신임의 것이다"* — 가
  **거짓이었다.** "다른 토큰" 은 신임일 수도, **더 낡은 쓰레기**일 수도 있다. 낡은
  잔존 슬롯이 방패가 되어 **찍었어야 할 고아가 부활**해 좌표를 옮기고 제3자의 진행 중
  commit 을 깨뜨렸다. 커밋 전후 대조로 확인했다 — 43차 직전에는 통과한다.

  **휴리스틱은 채널을 닫는 모양이 아니었다.** 43차 검수가 정확히 진단했다 —
  *"닫으려면 이름 원장에 신원을 넣거나 스탬프 자체를 없애야 한다."*

  ```
  34차 B  자기-찍기만 막았다              → 제3자 경유가 열려 있었다
  43차    "예약이 살아 있으면 안 찍는다"   → 낡은 잔존 슬롯이 방패가 됐다
  44차    **키에 신원을 넣었다**           → R1·R2·R3 이 한꺼번에 닫혔다
  ```

  **하나씩 막던 것이 채널을 닫자 같이 사라졌다 — 그게 "부류를 닫는다" 의 모양이다.**
  뮤테이션으로 확인했다: 키에서 토큰을 빼면 셋이 빨개진다.

  대가는 **구버전 상태의 1 회성 창**이다 — 토큰 없는 옛 키는 조회에 안 걸려 그 전환의
  지연 RPC 가 한 번 통과할 수 있다. 26차 `by` 의 창과 같은 부류이고 방향도 같다.

  **그리고 아무 census 도 안 센 자리 하나**(`commit` 이 홀더 선언을 신원 대조 없이
  가져간다)를 44차가 찾았다. 주석은 *"완결의 뜻은 실행권이 안다"* 였는데 **어느
  실행권인지가 빠져 있었다** — 근거를 적을 때 주어를 빠뜨리면 그 자리는 검사되지 않는다.
  술어를 맞췄다. **다만 재 보니 검출력 0 이다**(키 수정이 그 경로를 이미 닫았다) —
  **"고쳤다" 가 아니라 "맞췄다" 라고 적었다.**

- **43차 — 부류는 직접 비교 축에서만 닫혀 있었다 (CE-43-A, 중급)**

  42차에 "부류를 다 셌다" 고 적었다. **직접 비교 축에서만 참이었다.** 부류가 앓는 병은
  "직접 비교" 가 아니라 **"신원 판정이 토큰을 빠뜨린다"** 이고, 그것이 **파생 키 축**
  (`transitionKey` = `opId:tid:plane`, 토큰 없음)에서 살아 있었다.

  고아 청소가 옛 X/10 을 닫으며 찍는 종단 스탬프가 **같은 이름의 신임 X/11 까지 죽인다.**
  신임은 stage 가 `terminal` 로 거부되고, `release()` 마저 종단을 보고 일찍 돌아가
  **예약을 반납할 수도 없다.**

  **그리고 42차 커밋 메시지가 그것을 이름 붙여 놓고 안 고쳤다** — *"34차 B 가 자기-찍기만
  막았고 제3자 경유는 열려 있었다."* **네 번째 재연**이다.

  **CE-42 테스트가 우연한 방패로 초록이었다.** 제3자가 같은 좌표를 겨눠 `slot_taken` 이
  임계구역을 통째로 롤백해 **종단 스탬프까지 되돌려** 줬다. 좌표를 한 칸만 비키면
  아무것도 안 던지고 전부 저장된다 — **이 레포가 이름 붙인 "우연한 방패" 가 자기 회귀
  테스트 안에서 재발했다.**

  고쳤다: **그 이름이 지금 쓰이고 있으면 안 찍는다.** 판정은 예약이 한다 — 같은 이름의
  슬롯이 다른 토큰으로 살아 있으면 승계된 신임의 것이다.

  **그리고 술어를 하나 더 갈랐다.** `sameTransitionName`(이름만 묻는다) 을 신설해
  `ownsSlotOp`(그 전환인지 묻는다) 과 분리했다. 여섯 번의 census 가 매번 뭔가 찾은
  뿌리는 "id-only 자리가 흩어져서" 가 아니라 **두 물음이 같은 모양으로 쓰여 자리마다
  의도를 다시 판정해야 했기** 때문이다. 이제 "왜 토큰을 안 보나" 가 자리마다가 아니라
  **함수 하나에** 붙는다.

  **census 가 제 몫을 두 번 더 했다** — 수정하며 자리가 늘자 즉시 빨개졌고(20→21),
  술어를 가르자 다시 맞았다. 판정 없이 지나가지 않는다.

- **42차 — 부류를 이름 붙이고 한 자리만 고쳤다. 세 번째다 (CE-42, 중급)**

  41차에 CE-41-A 를 고치며 *"9차 반례 ③ 부류다"* 라고 **부류를 지목해 놓고 한 자리만
  고쳤다.** 같은 대조가 `supersede` 에 남아 있었고, 제3자가 같은 좌표로 들어오면 고아
  청소가 그것을 불러 **신임의 살아 있는 예약을 지운다.** 덤으로 청소가 `terminal` 을
  찍는데 `transitionKey` 에 토큰이 없어 **신임의 전환까지 죽는다** — 34차 B 가 자기-찍기만
  막았고 제3자 경유는 열려 있었다.

  *"이름을 붙이는 것과 전 자리를 고치는 것은 다른 일이다"* 를 40차에 적고, 41차가 그
  재연을 짚고, **41차 커밋 자신이 세 번째로 재연했다.**

  **이번엔 부류를 다 셌다.** 슬롯 신원 대조 세 자리를 `ownsSlotOp` 하나로 모았고,
  `grep` 으로 id-only 가 **0** 임을 확인했다. census 가 22 → 20 으로 줄어든 것을
  잡았다 — **줄어드는 방향도 계측기가 판정을 요구한다.**

  세 번째 자리(`releaseHolderSlots` 내부)는 외곽 가드가 토큰을 봐서 **"구조적 동치"**
  로 보였지만 그대로 바꿨다. **"구조적 무해" 는 내가 두 번 틀린 논증**이고(37차 I1 ·
  40차 슬롯 전제), 둘 다 **같은 파일 안에 반박이 있었다.**

  **그리고 내가 41차 결과를 옮겨 적으며 과장했다.** TESTS.md 에 *"검출은 모든 모양에서
  살아 있다"* 고 썼는데 41차 보고에는 두 모양이 GREEN 으로 적혀 있었다 — 계측기 한계의
  과대 서술이 미래 서사의 원료가 되는 39차 병형이다. 정정했다.

- **미처리 부채 (42차가 "기록조차 안 했다" 고 짚었다)**

  회차당 수정 범위를 좁히는 것 자체는 옳은데, **미처리를 원장에 안 적었다.** 적는다.

  ```
  ⚠️ 계측기가 지키는 런타임 주장이 무너지면  그 주장을 "미입증" 으로 강등하고
     재입증 전에는 그 회차를 클린으로 안 센다  (43차 — 규칙 ①의 구멍이었다.
     CE-42 테스트가 잘못된 이유로 초록이었고 그 그림자에 CE-43-A 가 있었다.
     리셋은 안 시키되 **적립을 막는** 세 번째 상태가 필요하다)
  "동치/도달 불가" 오독 잔존   agent.ts 572 · 616 (40차 규칙상 "불가지" 로 강등돼야 함)
  규칙 소급 위반 최소 6 건      "재현으로 확인했다" 류의 재현물 미커밋
  census 4 파일 하드코딩         왜 store-fs·effects-fs·materialize 를 빼도 되는지 근거 없음
  원시 토큰 대조 일곱 자리       driver.ts:36 · operation.ts:177 · agent.ts 다섯 — 두 계측 모두 사각
  계측기 귀속 거짓말 세 모양     모듈 화살표 · getter · 최상위 함수 본문
  ```

  **이 목록이 없으면 다음 회차가 같은 것을 다시 찾는다** — 실제로 41차가 짚은 것을
  42차가 "기록조차 안 됐다" 고 다시 짚었다.

- **41차 — 불가지 라벨이 진짜 반례를 찾아 줬다 (CE-41-A, 중급)**

  40차에 `releaseStaleSlots` 의 슬롯 대조에 **"불가지"** 라벨을 붙이며 전제를 적었다:
  *"신임의 슬롯은 홀더 없이 존재할 수 없다."* **거짓이었고, 반박이 같은 파일 100 줄
  아래에 이미 있었다** — `reclaimOperation` 의 주석이 *"비종단 저널인데 실행권이 없는
  상태가 만들어질 수 있다 … **예약 슬롯은 그대로 남아 있으므로**"* 라고 적어 놓았다.
  **내가 쓴 전제를 이 레포가 이미 반박하고 있었는데 같은 파일을 안 읽었다.**

  그러면 같은 id 를 승계한 신임의 슬롯이 홀더 없이 존재하고, 낡은 러너의 지연 반납이
  id 만 보고 **그것을 지운다.** 재현했고 `ownsSlotOp`(삼중 비교)로 고쳤다.

  **그리고 이 자리를 찾아 준 것이 "불가지" 라벨이다.** "무해하다" 고 적었으면 아무도
  안 봤다. 41차의 판정: *"계측이 자기를 검수하는 게 아니라 계측이 본체 검수를 유도하고
  있다"* — **수렴(ⓐ)이라는 근거가 이것이다.**

  **census 계측기가 제 몫을 한 첫 사례**도 나왔다. `ownsSlotOp` 을 넣자 즉시 빨개졌다 —
  새 자리가 판정 없이 지나가지 않았다.

  다만 계측기의 **귀속은 세 모양에서 여전히 거짓말한다**(모듈 화살표·getter·최상위 함수
  본문). 검출은 살아 있지만 **39차 병("이름표에 맞는 서사를 지어냄")의 무대가 그대로**다.
  이름표를 서사의 원료로 쓰지 말 것 — 그렇게 적었다.

  **그리고 40차가 자기가 이름 붙인 병을 재연했다.** *"이름을 붙이는 것과 전 자리를
  고치는 것은 다른 일이다"* 라고 쓴 커밋이, **같은 파일 8 줄 아래에 새 규칙을 추가하면서
  TESTS.md 의 거짓 서사를 안 고쳤다.** 41차가 짚었고 지웠다.

- **40차 — 이 시리즈에서 제일 나쁜 실패**

  39차에 **"작성자가 새로 쓴 근거가 거짓" 병이 처음으로 재발하지 않았다** 고 적었다.
  **그 커밋 안에서 재발했고, 그 병을 끝내려고 만든 계측기가 원료를 댔다.**

  census 계측기의 첫 판이 최상위 `function` 선언만 찾아서, 850 줄짜리 `DpAgent` 클래스
  구간의 자리를 **직전 최상위 함수 이름으로 오귀속**했다. 잡힌 것은 `finishOperation` 의
  대조(10차부터 있던 것)인데 이름표는 `assertInvariants` 로 붙었고 —
  **나는 그 잘못된 이름표에 맞는 이야기를 지어 냈다**: *"계측기가 I1 토큰 대조를 찾았다.
  일곱 번째다. 계측기가 나를 고쳤다."* `assertInvariants` 에는 `normalizeNumeric` 이
  **하나도 없다**(I1 은 `big(x) !== max` 형이다). 전부 거짓이다.

  그리고 **계측기의 초록이 계측기 자신의 버그에 의존했다.** 귀속을 고치자마자 빨개졌다.
  40차가 뮤턴트로 실증하기를, 그 850 줄 안에 새 토큰 대조를 넣어도 집합이 안 변해
  **검출력이 정확히 0** 이었다. 지문이 `!==` 형을 빠뜨려 `releaseHolderSlots` 도 놓쳤다 —
  "셋" 이라는 숫자마저 틀렸다. **넷이다.**

  **그리고 커밋 메시지가 안 한 일을 적었다.** 주석 둘을 넣는 python 이 `assert` 대신
  `if ... in s:` 였고, 앵커가 안 맞자 **조용히 통과**했다. 나는 그것을 확인 안 하고
  "적었다" 고 커밋했다. 40차가 diff 를 세어 짚었다 — `상류` 라는 낱말이 src 에 없었다.

  고친 것: 계측기의 귀속(최상위 함수 + 클래스 메서드 둘 다) · 지문(`!==` 포함) ·
  기대 집합(사실인 넷) · 거짓 서사 · 안 들어간 주석 둘(이번엔 넣고 확인했다) ·
  같은 파일에 남아 있던 "도달 불가" 오독 · 규칙에 **"검증물은 커밋된 산출물이어야 한다"**.

  **상·중급 0 세 회차 연속이라는 카운트는 리셋한다.**

- **38차 — 빚갚기 확정. 그리고 내가 쓴 근거 둘이 또 거짓이었다**

  상·중급 반례 **0**, 두 회차 연속 — **정지 조건 충족.** 새 상태 오염·되감김·봉쇄는
  없다. 대신 하급 넷이 나왔고 **둘이 37차에 내가 새로 쓴 근거**다.

  ① **"동치다" 가 거짓** — 물러남 자리는 **낡은 러너 + 같은 id 신임 홀더**로도 도달하고,
  그때 하류 `finalizeCandidate` 의 id 게이트가 **신임의 미완 후보를 durable 하게 지운다.**
  토큰을 더하면 그 변이 자체가 없다 — **관측상 동치일지언정 기계적으로 동치가 아니다.**
  무해한 진짜 이유는 동치가 아니라 **자가 치유**다. **35차에 같은 모양의 "동치" 주장이
  거짓으로 판명됐고, 그 사건을 인용한 회차에서 또 적었다.**

  ② **"I1 이 보장하므로 불능" 이 거짓** — I1 은 홀더 = **최신** 토큰만 보장하지 홀더 =
  **저널** 토큰은 보장하지 않는다. `reserveAll` 을 `opening` 없이 부르면 저널 정리·개방이
  통째로 건너뛰어져 둘이 갈린다. "구조적" 이라는 구분 자체가 틀렸고, 이 자리도 하류
  위임이다.

  ③ `completed` 표 현행화의 **구성원이 틀렸다** — 36차가 거짓 기록을 고친 회차에 같은
  부류를 남겼고, 37차의 현행화가 또 남겼다. **세 회차째다.**

  ④ **`#stillBaseline` 은 죽은 코드**였다(호출부 0). 지웠다.

  **위임 대상을 정확히 적었다**: `finishOperation` 의 홀더 토큰 검사 · `ownsSlot` 의
  canonical 대조 · 그리고 `finalizeCandidate`/`candidateArrived` 의 **수렴성**.
  셋째는 토큰 검사가 아니라 **비국소 성질**이라, commit 의 후보 재생성이나
  `candidateArrived` 를 바꾸면 그 자리들이 **조용히 열린다.**

- **`writeJournal` 의 `assertLeader` 는 검출력이 0 이다** (38차 실측) — 지워도
  unit 217 + conformance 365 + 모델 13 이 전부 초록이다. 겹치는 층들(seq CAS · 홀더
  검사 · `assertOwnership`)이 최종 상태를 같게 만들기 때문이다. **죽은 방어는 아니다**
  (거부의 종류가 바뀐다) — 그러나 그 자리를 가르는 무대가 없다. I6(b)·I7 과 같은 부류다.

- **census 를 다섯 번 세고 다섯 번 다 뭔가 나왔다** — 37차 표에 없던 세 자리를 38차가
  더 찾았다(`releaseHolderSlots` 내부 슬롯 · `supersede` 슬롯 · `supersede` 저널 대조).
  전부 상류 가드로 현재 무해하지만, **"완전한 census" 라는 주장은 다섯 번 깨졌다.**
  다음에 이 표를 인용할 때는 그 사실을 함께 인용해야 한다.

- **37차 — 상태 결함은 없었고, 내가 "못 만들었다" 던 재현이 만들어졌다**

  36차 수정 넷과 정정한 서술 셋이 **전부 참**으로 확인됐다. 새 상태 오염·되감김·봉쇄
  반례는 없다. 대신 셋이 나왔다.

  ① **증거 오염 재현은 가능했다.** 나는 게이트를 **첫 번째** 관측에 걸었고, 그러면 낡은
  러너가 깨어난 뒤 phase 전이 쓰기에서 죽어 증거 쓰기에 못 간다. **한 칸 옮겨
  `awaitActivation` 안의 두 번째 관측에 걸면** HUP 까지 끝낸 뒤 정지하므로 깨어나면 곧장
  증거 쓰기다. **"못 만들었다" 는 불가능이 아니라 내 조립이 틀린 것이었다** —
  이 레포에서 그 둘을 뒤바꾸는 것이 병이라고 세 번 적고 또 그랬다.

  그 재현을 편입하니 **`driveLoop` 자리까지 계측된다** — 35차부터 "검출력 0" 이라 적어
  온 자리다. 하나의 무대가 두 자리를 함께 든다.

  ② **내 CE-36-A 테스트는 검출력 0 이었다.** 지우지 않고 **무엇을 지키는지 좁혀
  적었다**(agent 층 fence 뿐) — 안 갈리는 테스트를 자산인 척 두는 것보다 낫다.

  ③ **census 가 세 번 다 놓친 자리 둘**(`driveLoop` 물러남 홀더 · 종단 분기 홀더).
  둘 다 현재 무해하지만 근거가 다르다 — 하나는 **하류 위임**(`finishOperation` 의 토큰
  검사가 바뀌면 열린다), 하나는 **구조적**(I1 이 보장). **근거를 코드에 적었다.**
  토큰을 더하지 않은 이유도 적었다: 그 자리에서는 동치이고, **동치를 넣으면 다음 스윕이
  그것을 짚는다**(24차 규칙).

  ④ 그리고 **`completed` 표 주석이 낡아 거짓**이었다 — 열거된 다섯 중 셋은 이미 토큰을
  보고 가지치기는 이미 실물이다. 36차가 거짓 기록 셋을 고친 그 회차에 같은 부류를
  남겼고 37차가 짚었다. 현행화 표시를 붙였다.

- **스윕이 네 번째 자리를 독립적으로 짚었다 (83.6%)**

  ```
  531 개 생성 · 110 개 실행 (seed 35035) · 죽었다 92 · 살았다 18 · 83.6%
  측정 HEAD: 04b4634 (35·36차 수정 앞)
  ```

  생존자 18 중 **둘이 `apply.ts:547` — 증거 쓰기의 신원 비교**다. 36차가 **읽어서** 찾은
  바로 그 자리를 스윕이 **재서** 짚었다. 두 계측기가 독립적으로 같은 자리를 가리켰다는
  뜻이고, 내가 "재현 못 했다" 고 적은 것과도 정합한다 — 그 자리를 지나가는 테스트가
  없었다는 것이 스윕의 말이다.

  나머지 생존자에도 신원 비교가 또 있다(`agent.ts:1410` 홀더 대조 · `836` I6(b) 절).
  23차부터 매 회차 이 계열이 찍힌다.

- **신원 비교 술어가 네 자리에 흩어져 있었다 (34·35·36차)**

  같은 병형("쓰는 토큰이 저널의 것 — 즉 **피해자의 것**이라 agent 층 검사를 통과한다")을
  **세 회차에 걸쳐 네 자리에서** 고쳤다.

  ```
  34차  driveInner 의 mine · driveLoop 의 소유 검사     ← 재현 뒤 수정
  35차  reserveAll 의 저널 개방 분기                    ← 34차가 안 센 자리
  36차  reload_intent 의 증거 쓰기                      ← 34·35차가 안 센 자리
  ```

  **세 회차 연속으로 "자리를 세는 것이 규칙을 만드는 일의 절반이다" 라고 적고 세 번 다
  빠뜨렸다.** 규칙을 아는 것과 전 자리에 적용하는 것은 다른 일이라는 것을, 규칙을 쓴
  사람이 세 번 증명했다.

  `reserveAll` 의 홀더 검사는 **일부러 id 만 본다**(같은 id 승계가 정당 경로다).
  주석이 없어서 36차가 짚었다 — 안 적으면 다음 사람이 "통일" 하면서 승계를 막는다.

- **내가 35차에 적은 "동치" 가 거짓이었다 (36차)**

  `driveLoop` 절을 두고 *"fence 뒤에는 낡은 토큰의 쓰기가 agent 층에서 전부 막히므로
  낡은 러너는 이 검사에 닿기 전에 죽는다"* 고 적었다. **`awaitActivation` 은 토큰이
  없는 관측 경로다** — 낡은 러너는 거기서 fence 를 **산 채로 건넌다**(직접 확인).

  그리고 근거로 든 뮤테이션(아무도 안 죽는다)이 뜻하는 것은 **"스위트가 못 가른다"**
  이지 "도달 불가" 가 아니다. **그 둘을 뒤바꾸는 것이 이 레포가 반복해 앓은 병이고,
  나는 그 병의 이름을 적은 회차에서 그것을 했다.** 그 자리는 지금 **검출력 0 이고
  도달 가능성은 열려 있다** — 그렇게 적었다.

  36차가 준 증거 오염 재현(CE-36-A)은 **내 손으로는 못 만들었다.** 낡은 러너가 게이트를
  건넌 뒤 첫 쓰기에서 죽는다. 술어는 모았지만 **못 연 것이지 없는 것이 아니다.**

- **낡은 러너가 신임의 전환을 오살했다 (34차 C — 재현 뒤 수정)**

  `driveInner` 의 `mine` 판정과 `driveLoop` 의 저널 대조가 **토큰을 안 봤다.** 그래서
  낡은 러너가 **같은 id 로 재발급된 신임 저널**을 제 것으로 읽고, 예산 만료 시
  `failAll` 로 **진행 중인 신임 전환을 닫았다.** 그때 쓰는 토큰이 저널의 것(= 피해자의
  것)이라 `assertLeader` 도 `writeJournal` 도 `finishOperation` 도 전부 통과한다 —
  **아무도 못 막았다.**

  후폭풍이 더 나빴다: 슬롯이 지워져 신임은 자기 publish 에서 `not_reserved` 로 쫓겨나고,
  `terminal` 은 안 잘리므로 **그 operationId:transitionId 가 영구 봉쇄**된다. 부작용
  하나 못 낸 정당한 전환이 이름째 죽는다. **등급 중, 봉쇄 꼬리.**

  9·10차가 `releaseHolderSlots`·`finishOperation` 에서 고친 것의 **마지막 잔재**였다 —
  그 둘은 토큰을 보는데 이 둘만 안 봤다. `ownsJournal()` 하나로 모았다.

  **처음엔 정적 사실만 있어 안 고쳤다.** 재현 없는 수정은 다음 회차에 조용히 되돌아온다
  (17차). 재현 전용 회차를 돌려 결정적 재현을 받고 나서 고쳤다.

- **가지치기 검수(34차)가 내 결론 둘을 뒤집었다.**

  ① **"여섯 회차의 방패 서술이 확인되지 않았다" 는 내 판정이 틀렸다.** 방패는 실재한다 —
  `tuple_mismatch` 거부가 캐시 잔존 덕이다. 내가 표면 경로만 밟고 결론을 넓혔다.
  **"못 연 것과 안전한 것은 다르다" 는 내 경고에 내가 걸렸다.**

  ② **"콜론 결함은 도달 불가" 도 틀렸다.** 내 전제 둘이 어긋났다 — `applyHealth` 는
  **실행권을 잡지도 검사하지도 저널을 열지도 않는다**(그래서 config 전환이 실행권을 쥔
  채로도 그룹이 는다), 그리고 진행 중 전환을 **먼저** 열면 그것이 가장 **오래된** 그룹이라
  잘릴 자리 맨 앞에 온다. 검수자가 준 순서로 밟으니 수정 전에서 재현되고 수정 후에
  통과한다. **스위트에 편입했다.**

  ③ 그리고 검수가 **`ownsJournal` 교체 단독의 결함**을 찾았다(가지치기와 무관).
  `transitionKey` 에 토큰이 없어(`opId:tid:plane`), 같은 id 를 새 토큰으로 재발급하면
  고아가 된 옛 전환과 **키가 같다.** 청소가 그 키를 `aborted` 로 찍고 몇 줄 아래
  `admit` 이 그것을 읽어 **자기를 거부한다.** 게다가 그 쓰기는 예외와 함께 버려져
  **원장에는 남지도 않는다** — 진단이 거짓이고 재발급이 결정적으로 반복 거부된다.
  자기 이름은 안 찍게 고쳤다. (검수자도 첫 보고의 "영구 원장 오염" 서술을 정정했다.)

  ④ **구버전 `completed` 항목의 1 회성 창** — `transition` 필드가 없으면 그룹명이
  어긋나 업그레이드 직후 한 번 무보호다. 26차 `by` 부재의 창과 같은 계열이고 방향은
  안전하다(덜 보존할 뿐 오답을 안 만든다).

- **빚이 하나인 줄 알았는데 셋이었다** — `completed` 를 자르고 재 봤더니
  `terminal` 과 `activationEvidence` 도 선형으로 자라고 있었다.

  ```
  n=50   completed=150  terminal=50   evidence=50
  n=200  completed=192  terminal=200  evidence=200   ← completed 만 유계였다
  ```

  `activationEvidence` 는 잘랐다 — **사후 감사용이고 프로덕션 독자가 없다**
  (`evidenceFor` 를 부르는 것은 테스트뿐이다). 지금 서 있는 좌표의 근거는 창 밖이어도
  안 지운다. 그건 과거가 아니라 현재에 대한 물음이다.

  **`terminal` 은 못 잘랐다** — 다만 **"자를 규칙이 없다" 는 과장이었다**(35차 검수).
  얼개는 있다: 목표 epoch 이 좌표에 추월된 항목은 **구조적으로 부활 불가**다(재획득은
  `epoch_not_monotonic`/`coordinate_mismatch`, commit 은 `not_staged` 에 막힌다).
  걸리는 것은 키에 epoch 이 없다는 것이고 값에 적으면 된다 — `completed` 의 `transition`
  과 같은 모양이다. **설계 스케치이고 프로토타입은 안 했다.** 이 표를 잘못 자르면
  운영자가 포기한 전환이 되살아나므로 재현 없이 넣지 않는다.

  그것이 감사 기록이 아니라 부활 방지 장치라는 것 자체는 맞다 — `admit` 이
  지연 도착한 RPC 를 `aborted`/`failed` 로 거부하는 근거이고, 지우면 운영자가 포기한
  전환이 되살아난다. "토큰이 낡은 것만 자른다" 도 안전하지 않다: 후보가 fence 를 건너
  승계될 수 있어(14차 고아 방지) 낡은 토큰의 종단 기록을 지금 후보가 읽는다
  (`candidateArrived` 의 원장 폴백). **자를 규칙을 못 찾은 것이지 안 자라는 것이 아니다.**

- **I7 의 근거 절을 좁혔다** — "지워지지 않는다" 에서 "**서 있는 자리의** 근거는 지워지지
  않는다" 로. 원래 절이 무한 성장의 원인이었다(하나도 못 지우게 했다).

  **검출력을 쟀다.** 서 있는 자리를 지우는 뮤턴트를 잡은 것은 이 절이 아니라 conformance
  테스트였다 — `prune` 이 `assertInvariants` **앞에서** 돌아, 근거가 만들어진 그 쓰기에서
  지워지면 `before` 에 없어 비교가 못 본다. 원래 절(검출력 0)보다는 낫지만 **"이빨이
  있다" 고 적을 수는 없다. 검출기는 테스트다.**

- **I6(b)는 검출력이 0 이다** (29차 실측) — 통째로 꺼도 570 전부 초록이다. 14차에 진짜
  버그를 잡은 절이고 28차 CE-28 에서 실제로 발화도 했지만, **지금은 어떤 테스트도 그것을
  지나가지 않는다.** I7 과 같은 부류로 기록한다: "미래 변경을 막는 철사" 이되 그 철사가
  당겨지는 것을 본 적이 없다.

  **지우지 않는 이유**는 발화한 이력이 있어서고, **자랑하지 않는 이유**는 28차가 이것을
  안전 근거로 쓰다가 CE-29 를 만들었기 때문이다. 검출력 없는 가드를 논증에 쓰면 그
  논증은 비어 있다.
- **API · DB** — REST 서버 · OpenAPI · PG DDL · changeset · auth/audit 이 **없다.**
- **`completed` 무한 성장 — 갚았다가 내가 다시 열었고 다시 갚았다** (빚갚기 재검수)

  34차에 유계로 만들었는데 **48차에 캐시 키에 토큰을 넣으면서 다시 열렸다.** 키의 단위는
  (전환, 토큰)이 됐는데 **자르는 쪽의 그룹 필드는 이름에 남아** 재발급이 한 그룹으로
  붕괴했다 — 그룹 수가 안 늘어 가지치기가 **한 항목도 안 지운다.** 실측: 리더 교체
  80 회에 240 항목, 160 회에 480 — **정확히 선형.**

  45차 계약이 명시적으로 허용한 경로(같은 이름 재발급)라 **정상 운영에서 리더가 교체될
  때마다** 자란다. 그룹에도 토큰을 넣어 닫았고, 재현 둘을 커밋했다.

  **"자리를 다 안 센다" 의 재연이다** — 원장의 단위를 바꾸면서 그 원장을 **자르는 쪽**을
  안 셌다. 34차부터 세어 온 census 는 *비교 자리*를 셌지 *단위를 쓰는 자리*를 안 셌다.

- ~~**(34차 기록) `completed` 캐시가 무한히 자란다**~~ — **갚았다.** 전환 64 개까지만 들고 간다
  (`COMPLETED_RETENTION`). 진행 중인 전환과 저널이 가리키는 전환은 안 자른다. 잘린 뒤
  늦게 온 재요청은 `terminal` 을 답한다 — **거짓말이 아니다.** 그 전환은 실제로 끝났고,
  캐시가 있을 때와 다른 것은 "성공했다" 대신 "이미 끝났다" 로 답한다는 것뿐이다.
  유계성은 **숫자가 아니라 두 지점 비교**로 잰다(90 전환과 180 전환의 기록 수가 같다).

- **그런데 "우연한 방패" 서술은 여섯 회차 동안의 추정이었고, 실물 가지치기로 확인되지
  않았다.** 22~27차가 "가지치기를 넣는 순간 id-only 비교 자리들이 열린다" 고 적었고
  23차는 캐시 삭제를 **모의**해 영구 봉쇄까지 재현했다고 했다. 실물로 넣고 그 경로를
  다시 밟으니 **안 열렸다.** 이유가 있다 — 가지치기가 돌려면 전환이 64 개 지나가야 하는데
  **그 전환들이 각각 옛 저널을 청소한다.** "캐시는 잘렸는데 옛 저널은 남아 있는" 상태가
  표면 경로로는 안 만들어진다. 23차의 모의는 캐시만 지우고 시간이 안 흐른 것으로 쳤다.

  네 자리(`reserveAll` 고아 청소 · 슬롯 주인 · holder 대조 · `ownsSlot`)를 뮤테이션했더니
  **가지치기 뒤에도 넷 다 살아남는다.** 즉 상태는 가지치기 전과 같다 — 미검사이되,
  **가지치기가 그것을 열지는 않았다.**

  그래도 `reserveAll` 고아 청소는 `ownsJournal()` 로 바꿨다. 21차에 신원 비교를 그
  함수로 모아 놓고 정작 그 자리가 안 쓰고 있었다 — 열리는 것을 못 보였어도 **모은 것을
  안 쓰는 것은 그 자체로 부채**다.

  **못 연 것과 안전한 것은 다르다.** 여기 남기는 것은 "탐색이 실패했다" 이지 "없다" 가
  아니다 — 17차에 그 둘을 뒤바꿨다가 뒤집혔다.

- ~~**`completed` 캐시가 우연한 방패다** (22차)~~ *(위로 대체 — 아래는 그때의 기록)* — 21차에 `ownsJournal()` 로 신원 비교를
  한 곳에 모았지만, 그건 저널을 보는 자리들만이다. **id 만 비교하는 자리가 아직
  여럿이다** — `reserveAll` 의 고아 청소 · `driveLoop` · `EffectTimeout` 정리 ·
  `epochsFromJournal` · `finalizeCandidate`. 이들이 지금 안전한 이유는 비교가 옳아서가
  아니라 **`completed` 가 전환마다 영구 누적돼서 같은 id 의 옛 전환이 늘 남아 있기**
  때문이다. 방어가 아니라 부작용이다.

  그래서 두 가지가 묶여 있다. 캐시는 **무한히 자라고**(전환 수만큼), 그런데
  **가지치기를 넣는 순간 저 자리들이 열린다.** 지금 고치지 않는 것은 판단이다 —
  고칠 자리가 다섯인데 전부 재현 경로가 없어서, 고쳐도 스윕이 지워 준다. 가지치기를
  설계할 때 **같은 커밋에서** 신원 비교로 바꾸는 것이 옳다. 그 전에는 캐시를
  건드리지 마라.

  **여섯 번째가 생겼다** (27차). 26차에 `by` 없는 좌표는 "내가 옮긴 것이 아니다" 로
  비관 판정하게 했고, 그 대가를 "구버전 열린 저널이 한 번 비관적으로 닫힐 수 있다" 고
  적었다. **그 대가가 지금 지불되지 않고 있다** — `completed` 캐시의 canonical 비교에
  `by` 가 없어서 캐시된 ACK 가 재요청을 먼저 받기 때문이다. 27차가 재현으로 확인했다:
  캐시가 있으면 `activated`, 캐시까지 지우면 `failed`. **비관 폴백 부재도 캐시에
  기대고 있다** — 가지치기 체크리스트에 이 항목을 올린다.

### 동결 판정

```
A. 타입 · DP ABI    No-Go — 다만 이제 기준이 숫자다 (아래)
B. API · DB 스키마  No-Go — 만들지 않았다
```

### 종료 조건을 다시 쓴다 (42차)

**현행 조건("상·중급 0 인 회차 연속 2 회")은 42 회차 동안 0 회 충족됐다.** 38차가 유일하게
근접했고 39·40·41·42차가 리셋했다.

구조적 이유가 있다: **매 회차의 수정 커밋·서사·계측기가 새 결함 표면을 만든다.** 최근
결함의 다수가 코드가 아니라 **기록·계측기·미이행**이고, 그것들은 검수 과정 자체가
생산한다. **카운터가 자기 먹이를 만드는 루프**다. 그래서 축을 나눈다.

```
① 종료 게이트는 본체 런타임 축만
   "커밋된 재현물(빨간 테스트)로 입증된 상·중급 런타임 반례 0, 연속 2 회"
   기록·주석·계측기 결함은 **정오표 원장**으로 처리하고 카운터를 리셋하지 않는다.
   (다만 같은 회차 내 커밋 의무 — 미루면 위 "미처리 부채" 가 된다)

② 리셋은 재현물 커밋으로만
   재현 안 되는 주장으로는 카운터를 못 움직인다. 기존 규칙의 형식화다.

③ 부류 마감 게이트가 먼저다
   신원 비교 부류(9·10·20·21·23·34·35·36·41·42차 — 상·중급의 대부분)는 인스턴스가
   아니라 부류로 닫는다. id-only 자리 각각에 "왜 id-only 인가" 를 census 가 기계적으로
   읽는 표로 만들고, 근거 없는 자리가 있으면 빨갛게. **이 게이트 전에는 연속 카운트를
   시작하지 않는다.**

④ 상한을 둔다
   N(=6) 회차 안에 ③ 이 안 닫히면 "종료" 대신 **"동결 불가" 를 공식 판정**으로 채택하고
   13차 처방 ⑤(표면 재설계)로 전환한다. **무한 연장을 명시적 결정으로 바꾼다.**
```

**왜 이 개정이 정직한가**: 현행 조건은 사실상 *"작성자가 완벽한 기록을 쓸 때까지"* 를
뜻했고, 그건 도달 불가에 가깝다. 그렇다고 기록 결함을 안 세면 39~42차가 찾은 것들(거짓
서사·과장·미이행)이 묻힌다. **세되 카운터에서 분리하는 것**이 두 요구를 다 만족한다.

---

13차가 준 동결 기준: **여러 적대적 회차 동안 표면이 변하지 않을 것.** 이제 그걸 잰다.

```
동결 카운터  3 회차 — **그런데 이것만으로는 동결하지 않는다 (24차)**
             26차에 계약을 옮겨 **0 으로 리셋됐다**(+PlaneAuthor). 수정 유도 결함
             연속은 **5 회차**(22~26) 다 — 다섯 개가 전부 §3.4 한 부류였고, 그래서
             26차에 인스턴스 대신 **부류를 닫았다.** 카운터가 초록인 이유가 "계약이 안정돼서" 가
             아니라 "결함이 전부 내부라서" 다 — 대리 지표만 보고 동결하면 13차가
             막으려던 일이 그대로 일어난다.
             **새 정지 조건: 수정 유도 결함 연속이 0 인 회차를 최소 한 번 볼 것.**
표면         77 심볼 · SURFACE.txt 가 기준 · verify.sh 가 매번 대조
             15차에 lease 강제(`Checked`)로 **일부러** 움직였다 — 그때 0 으로 리셋됐다.
             16·17차 수정은 전부 내부였고, 18차에 `unfinished` 변종을 가르며 다시 움직였다.
             19~23차 수정은 **전부 내부다.** 22차가 정지 조건 충족을 판정해 1 로,
             23차가 조건 셋(CE-A 내부 수정 · 표면 무변동 · 눈금 재측정)을 걸고 2 로 올렸다.
```

카운터가 0 으로 리셋되는 조건은 하나다 — `node scripts/surface.mjs --write`. 즉
**계약을 옮기는 순간.** 그래서 "표면을 바꿔야 고쳐지는 반례" 와 "내부만 고치면 되는
반례" 를 구분해 세는 것이 이제 의미를 갖는다.

---

## 5. 정직한 자기 평가

**이 레포는 아직 컨트롤 플레인이 아니다.** 렌더러와 DP 상태기계다. API 도 정본 DB 도
reconciler 도 없다. 6차 검수의 표현이 정확하다 — "현재 산출물을 완성된 컨트롤 플레인이라
부르기는 이르다."

**잘 된 것.** 엔진 동작을 추측하지 않고 실측했다. 실물 nginx 에 물렸다. 반례를
conformance 로 고정해 회귀를 막았다. 못 증명한 것을 못 증명했다고 적었다.

**안 된 것.** apply 경로의 동시성을 **국소 수정으로 아홉 번 시도했고 여섯 번 뚫렸다.**
"이번엔 막았다"는 판단이 반복해서 틀렸다. 외부 효과는 취소할 수 없고 nginx 는 리더
토큰을 모르는데, 계속 "부작용 앞에서 막는" 방식으로 접근했다.

**방식을 전환했다** — 막는 대신 탐지하고 수렴시킨다. ①은 이제 "막는 문제"가 아니라
"**늦은 착지가 관측되고 덮이는가**"이고, 그건 테스트한다.

다만 이것도 **내 판단이고, 지금까지 이런 판단이 여러 번 틀렸다.** 10차 검수가 볼 것이다.

**14차 시점에서 가장 정직한 문장.** 여덟 회차 연속으로 직전 수정이 구멍을 만들었다.
그런데 이제 그 구멍이 **어떤 종류인지** 말할 수 있다 — 오늘 넷은 전부 표면을 안 건드렸고,
전부 불변식 밖의 '점' 이었다. 사고는 **외부 효과와 상태의 이음매**에서만 난다.
그 이음매를 상태 술어로 끌어올 수 있는지가 다음 질문이다.

**그리고 13차 때 같은 실수를 했다.** 9차의 평범한 버그 셋(②③⑤)을 고치고 반례를
고정하지 않아서 뮤테이션이 셋 다 놓쳤다. conformance 를 추가한 뒤에야 잡혔다.
스파이크 S8 에서는 **간헐적으로 깨지는 검사**를 발견했다 — `hup` 직후 바로 관측하고
있었다. e2e 에서 이미 배운 것인데 스파이크에는 남아 있었다. 조건 폴링으로 바꾸고
6회 연속 통과를 확인했다.
